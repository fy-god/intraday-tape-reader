# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] — 2026-09-18

四轮**对项目自己的审计**（告警文案 / 看板视觉 / `tools/` 脚本 / 长跑内存）
查出并修掉了一批真缺陷。都是"数值算对了、但用户看到的字不对"这一类 ——
不崩溃、不报错、测试全绿，只有盯着输出逐条手算才能发现。

### 修复 — 告警文案（11 条全部修复，见 `docs/TEXT_AUDIT.md`）

* **`快速反弹` / `高台跳水` 标题印错了量**（最严重的一条）。
  标题用的是**窗口涨跌幅**，而分级用的是**信号强度**，两者不同源。
  于是一条被评级为「会弹窗」的告警，标题可能写着 `+0.00%`、甚至
  一个**负数**（"快速反弹 -0.98%"）。现改为标题与分级同源。
* **`急跌` 告警的详情印「距涨停 +13.40%」**。急拉/急跌共用同一行 detail，
  而距离只算了涨停口径 —— 方向正好相反。现按方向分别报「距涨停 / 距跌停」，
  取不到限价时**省略**该字段（而不是兜底 0.00%，那看起来像"已经涨停了"）。
* **`快速回封` 把当日最低价说成「近 10 分钟内」的最低价**。
  实测当日最低 10.30（约 5.4 小时前）、窗口内最低 10.90，
  却印成「近 10 分钟内回落 6.36%」（真值 0.91%，差 7 倍）。现取窗口内最低。
* **`炸板` 告警印「买一封单 126万」**。板已经炸开了，此时不存在封单 ——
  那个数是**买一挂单额**。文案与 `metrics` 键名一起改（`seal_amount_wan`
  → `bid1_amount_wan`），因为该键会原样展示在看板悬停里。
* **`封涨停` / `封跌停` 详情硬编码「5 分钟」**，与同一行的可配窗口自相矛盾。
* **跌停侧一整条状态机是死的**：「打开跌停」「触及跌停」永远不会出现
  （`_check_side` 在跌停侧直接短路），而看板却宣称有 11 个涨跌停信号。
  现补齐跌停侧，并删掉 3 个确实无产出点的旧信号名。
  看板计数从虚报的 11 变成真实的 8。
* **一字跌停被标成「非一字」**（跌停侧的一字板识别是硬编码 `return False`）。
* **指数信号提示硬编码「5 分钟内」**，窗口可配。
* **「机构吃货/吐货」提示写「成交单巨大」**（单笔口径），
  而实现只有两快照区间增量。改为「区间内主动买入/卖出量巨大」。
* **看板悬停的 `extra` 原样拼「键 + 值」，没有单位**：
  `amount 102000000`、`turnover 5`。而同一屏的股票表用「成交额(亿)」显示
  同一个量 —— 两套单位差 8 个数量级。现补上中文名与单位。
* **配置键 `rebound_off_low_pct` 注释称「等价于 rebound_pct（保留兼容）」
  但从未被读取**，照它配过的用户会静默失去设置。现真正实现别名。

### 修复 — 其他审计查出的问题

* `storage.series_len` 配置被静默忽略。
* 又找到 5 个"声明了但没人读"的配置键（1 个补上接线、4 个删除）。
* 盘中实跑发现的标题单位差 1 万倍；`--config` 静默丢配置。
* 中文 Windows 上 `tools/` 下 13 个脚本直接崩（GBK 控制台无法输出 ✓）。
* 截图检查把"还没生成"误报成失败；东财限流时整体退出。

### 新增 — 防漂移检查

* `tools/check_config_consumed.py`：找出"声明了但没人读"的配置键。
  修好了一个盲区 —— 它原先只挖 `config.py` 的 `DEFAULTS`，
  漏了六个规则模块各自的 `DEFAULTS`，于是 `rebound_off_low_pct` 恰好躲过检查。
  **看起来在查、其实查不到，比没有检查更危险。**
* `tools/check_spirit_mapping.py`：注册表里的信号必须有产出点。
* 两个"文档不许悄悄漂移"的检查。

### 文档

* 新增 `docs/TEXT_AUDIT.md` —— 告警文案审计，11 条缺陷逐条含实跑证据、
  手算过程与修复记录。文末诚实记录了写测试时踩的坑，
  其中包括一条**第一版是假的**回归测试（它在测试里抄了一份白名单，
  于是改源码验证时照样通过 —— 假护栏比没有护栏更危险）。
* README 补上这份审计；修正工具清单与 `TOOLS_AUDIT` 的条数描述。

### 验证

* **1047 项测试全过**（0 失败 / 0 错误 / 0 跳过），且在**干净克隆**上复验过。
* 8 个 `tools/check_*.py` 与 `dash_render_check.js` 全部退出码 0。
* 每条修复都经**独立复现**确认，并回退修复验证过回归测试确实会报红。
* 真实盘中 soak：2026-09-17 早盘 720 秒 / 144 轮，看板 576 次请求全 200，
  WARNING 1 / ERROR 0，内存收敛（见 `docs/MEMORY_AUDIT.md`）。

## [1.0.0] — 2026-09-16

首个发布。用公开行情数据复刻同花顺/大智慧「短线精灵」的滚动播报效果。
纯 Python 标准库实现，唯一依赖 PyYAML。

* 急拉急跌 / 涨跌停 / 放量 / 形态异动检测，两层去重。
* 30 个短线精灵信号（其中 6 个 Level-2 信号**不实现也不伪造**，
  因为没有逐笔委托数据 —— 见 README「已知局限」）。
* 自带看板（SSE 实时推送）、终端播报、可选通知推送。
* 交易日历与各板块涨跌幅限制（主板 ±10%、双创 ±20%、北交所 ±30%、
  主板 ST ±5%）。

[1.0.1]: https://github.com/fy-god/intraday-tape-reader/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/fy-god/intraday-tape-reader/releases/tag/v1.0.0
