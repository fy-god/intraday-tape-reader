# 本地 Agent 执行任务书 — 2026-09-20_00-09-21_JST

基准产品 SHA：`34778204dee41575697357f5d2e20a40aad3bc9d`。云端当前 HEAD 只有审计文档变化；本任务不得把文档提交当产品修复。

## 目标

把本轮新增 `IT-P1-CAPABILITY-001` 从静态+机制证据推进到真实 checkout 的红测/候选修复/回退验牙，并将 capability-aware metadata 接入 live_session 与研究 feature mask。若真实多日数据已存在，则在同一 TaskSpec 下只做 HGB base vs capability-dropout 第一轮真实对照；没有数据也必须完成工程与数据盘点，不得停在“无数据”。

## 允许修改

仅在你的隔离工作区内修改候选代码/测试/研究目录；不得自动 push、合并 PR、改 Actions、真实通知或交易。最终回传 diff 和产物，由下一轮审计核验。

## 工作顺序

### A. WP01 — 新 capability 红测

先定位现有 source/rule/fake。新增真实测试，至少覆盖：

1. 同一 Quote 业务状态，Tencent capability 提供 turnover，`volume_burst` 可评估；Sina capability 不提供 turnover 时不能静默变成普通“no hit”。
2. `SourceManager` 切 Tencent→Sina 后 rule coverage 必须显示 `unavailable_capability` 或明确降级策略。
3. 恢复 Tencent 后 evaluable 恢复。
4. `spirit_order` 若启用：Sina 缺 L5/outer-inner 时必须明确哪些子信号 unavailable，不得整体 health 仍显示满能力。

修复前红、修复后绿，并做回退验牙。回传完整 pytest 命令、exit code、日志。

### B. WP02 — Sidecar contract

优先新增非破坏性 sidecar，不改冻结 Quote 字段：

- `SourceCapabilities`
- `BatchResult`
- `ObservationDecision`
- `RoundObservationSet`

至少字段：requested、returned、explicit_unavailable、unknown_missing、source、source_epoch、turnover/volume_ratio/depth_l1/depth_l5/outer_inner/float_cap/provider_time capability。

先影子记录，不立刻改变所有规则行为。

### C. WP08 — live_session observability

每轮增加：

```text
requested
returned
admitted
unknown_missing
stale_rejected
out_of_order_rejected
source_mix
```

并增加每规则：

```text
evaluable
unavailable_capability
rejected_quality
evaluated_no_hit
hit
```

验收：模拟 Tencent→Sina failover 时，整体服务可健康，但 `volume_burst` capability coverage 必须下降且原因明确，不能只看到 alerts=0。

### D. WP03 — 35 因子真实接线

读取本轮任务书中的 `provider_factor_capability.csv` 思路，按真实 Quote 字段实现 capability mask：

- Tencent：A/B/C/D/E/F；G 需指数对齐；H 来自 observation metadata。
- Sina：A/B/F + L1 的 C/D 子集；L5/E blocked。
- Eastmoney：A/B/F；盘口 C/D/E blocked。

不要把 blocked 字段填 0 当业务值。缺字段使用 mask/NaN，并记录 source capability。

### E. WP02/05 — 真实数据与第一轮真实训练

先运行数据盘点：股票数、交易日、采样间隔、每字段 coverage、provider epoch、missing/stale/out-of-order。若不足 30 股×20 日，也输出真实 manifest。

真实数据足够时，冻结 TaskSpec 后只跑两组：

```text
HGB base
HGB capability-dropout
```

超参沿用开发期冻结值，不在最终测试上重新挑。输出：train/calib/test 或 walk-forward、checkpoint hash、predictions、provider 分组指标、family ablation。

TCN 本轮不扩网：只有 HGB 与数据/标签稳定后才恢复。

### F. WP09 — shadow

只输出：

```text
raw_score
model_version
feature_hash
capability_status
fallback_reason
latency_ms
```

不要采用本轮 synthetic 的 0.407 阈值；该策略在新锁箱没有提升事件 precision，已淘汰。不要把 Platt/Isotonic 设为默认；本轮新锁箱校准恶化。

## 继续保留的工程开放项

同时若时间允许推进：`IT-P0-002` 写前时间 gate + window 上界；`IT-P1-WINDOW-001` observation interval；`IT-P1-LIMIT-001` qualified seal 状态；`IT-P1-006-R1` max_pages truncation；SSE committed/sent/received/applied。

## 必须回传

- 基准 SHA、实际 diff、diff SHA256
- 红测/绿测/回退验牙日志
- `source_capabilities.json` 或 schema 定义
- `dataset_manifest.json`
- `task_spec.json`
- `split_manifest.json`
- `factor_catalog.csv`
- `factor_diagnostics.csv`
- `provider_coverage.csv`
- `trials.jsonl`
- `training_log.jsonl`
- checkpoint hashes
- `predictions.csv`
- `ablation.csv`
- `event_reconcile.json`
- `latency_memory.csv`
- `RUN_MANIFEST.json`
- `NEXT_STEPS.md`

没有真实训练条件时，训练相关写 `blocked_real_data`，但 A-D 工作仍必须推进。不得用 synthetic 成绩冒充实盘结果。
