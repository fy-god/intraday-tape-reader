# 本地 Agent 任务书 — 2026-09-22 08:08:19 JST

## 基线

- repo：`fy-god/intraday-tape-reader`
- 云端固定产品证据：`2406d818168da9e642e73b73b254762050e229e5`
- 开工前必须重新读取实时 main；若有新产品提交，先 diff 再执行。
- 当前开放 PR：0。
- 禁止：真实通知、交易、自动 push、Actions 修改、付费服务、持续抓取。
- 允许：隔离 checkout、FakeSource、假时钟、RecordingNotifier、有限离线重放、授权本地真实 universe 单次测量。

## WP01 — R-12 Universe denominator【P1，第一优先】

### RED
固定 provider fixture，不写死成生产市场常量：
```text
expected_total=5913
active_scan_codes=4500
```
当前绝对 gate 会 `OK`；新合同必须输出：
```text
denominator_kind=provider_declared_total
active_coverage≈0.761
```
并按候选 policy 转 FAIL。

再测：
```text
4100/4200
4100/5913
```
必须给不同 coverage 结论。

### GREEN
建立 UniverseTruth：
- expected_total
- denominator_kind
- raw_unique_codes
- usable_quotes
- active_scan_codes
- transport_complete
- transport_coverage
- usable_coverage
- active_coverage
- round_coverage
- mode
- last_attempt/applied/complete

### rollback tooth
回退产品实现后 ratio RED 必须重新红。

---

## WP02 — membership 与 quote usability 分离【P1】

### RED
```text
expected=100
raw_unique_codes=100
usable_quotes=94
transport_complete=true
```
目标：
```text
active membership = 100 codes
usable_coverage = 94%
```
不能：
```text
active membership = 94
```

### 实现
Eastmoney `ClistPage.codes` 已有 transport code。
新增正式 membership 输出，不要靠 parsed Quote list 定义 market membership。

可以选择：
- `UniverseMember` 对象；或
- source 的 `universe_members()` / metadata code set；
但接口必须明确、可测试，不能在 Engine 猜。

---

## WP03 — UniverseTruth → Store/status/live-session + 真实 reconcile

Store/status：
```text
universe_truth
active_scan_universe
latest_quote_cache_size
```

live-session：
同时报告：
```text
transport_coverage
usable_coverage
active_coverage
round_coverage
denominator_kind
mode
```

授权本地环境真实测量一次并落：
`universe_coverage_reconcile.json`

必须带：
- timestamp
- source
- config/request hash
- raw response hash 或可复算 manifest
- expected/raw/usable/active
- complete/reason

旧 73%/83% 禁止引用。

---

## WP04 — zero-return R1【P2，可并行】

当前 empty 分支不要再手写第二套 request arithmetic。

RED：
默认配置：
- 5 个 `index_codes`
- `spirit_index.enabled=false`
- 500 stock code
- stock provider 返回 `[]`

必须：
```text
actual index dispatch=0
index_requested=0
requested=500
```

连续 6 次 empty：
- observation_seq 增长；
- poll attempt identity / poll_count 也必须单调；
- 不能 6 个 observation 都绑定同一 poll_count。

missing：
如果 source API 只提供 Quote-object 级事实，明确命名该语义；
不要把 parser-drop 伪装成 provider raw missing。

---

## WP05 — partial universe retry/backoff【P1】

RED：
- 旧 full pool；
- TTL 已过；
- 连续 smaller partial；
- 120×5s poll。

目标：
- 不允许 120 次 universe refresh；
- old active pool 保留；
- last_attempt 前进；
- last_complete 不伪造。

起始 `universe_retry_seconds=60`，配置化，不宣称已最优。

---

## WP06 — per-code provenance/capability

建立：
```text
(route, code)
→ source
→ source_epoch
→ advertised capability
→ observed field availability
```

这是 mixed-source targeted fallback 与 H 类质量因子的前置。

---

## WP07 — 研究数据与因子脚手架【无真实数据也 ready】

待新增：
```text
research/intraday_quality/
  dataset.py
  labels.py
  splits.py
  factor_catalog.py
  features/
  factor_eval.py
```

把云端本轮 32-factor catalog 变成可执行 registry。

测试：
- 公式数值；
- 因果性（未来修改不得改变过去特征）；
- missing mask；
- source capability blocking；
- full-data normalization 禁止；
- daily same-slot baseline 只读此前交易日。

并扫描已授权目录：
若真实连续数据存在，就生成真实 manifest；不存在就 blocked，不抓付费数据。

---

## WP08 — TaskSpec + tabular 闭环【数据满足后】

冻结：
- committed candidate event
- primary H=180s
- 60/300s 只辅助
- unknown mask
- label_end / known_at
- overlap purge

同数据比较：
rule / Logistic / HGB / small MLP。

每个模型：
fit → checkpoint → reload → predict → evaluate。

---

## WP09 — ObservationInterval + SSE durable

继续关闭：
- fresh-flat vs true gap
- committed→sent→received→applied
- cursor/gap
- slow client 不得 silent drop-oldest
- bounded store/series

---

## WP10 — TCN / shadow

只有：
UniverseTruth + real dataset + TaskSpec + time-forward tabular baseline
全部具备后再跑。

TCN：
6 blocks / k=3 / dilation 1..32 / ch32 / dropout .1 / params<=200k。
先多 seed/time-forward；无稳定增量就淘汰。

---

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

universe_denominator_red.log
universe_denominator_green.log
universe_denominator_rollback.log

universe_membership_red.log
universe_membership_green.log
universe_membership_rollback.log

empty_round_reconcile_red.log
empty_round_reconcile_green.log

universe_partial_backoff_red.log
universe_partial_backoff_green.log

universe_truth_cases.json
universe_coverage_reconcile.json
empty_round_reconcile.json
per_code_provenance.json

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
predictions.csv
ablation.csv
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md

full_pytest.log
check_*.py logs
dash_render_check.log
```

未适用项用 `not_run/blocked + 原因`；禁止伪造空成绩。
