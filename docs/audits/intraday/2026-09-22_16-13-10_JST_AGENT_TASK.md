# 本地 Agent 任务书 — 2026-09-22 16:13:10 JST

## 固定基准

- repo：`fy-god/intraday-tape-reader`
- 当前固定产品 SHA：`c4f2ce107f9c1dafaefbec8310934d8e0fdd2ee2`
- 当前 main 在产品 SHA 之后只有审计文档；开工前必须重新核实。
- 不真实通知、不交易、不改 Actions、不自动 push、不持续抓取、不租卡。
- 探针必须显式注入/固定 universe source，禁止像 13:00 产品轮那样意外命中真实网络。

## WP01 — Health Consumer Contract + tri-state【最高优先】

### RED A：transport tri-state
真实 `Engine.universe_truth()` 冷启动/未刷新：
- `transport_complete` 必须是 `None`；
- `active_snapshot.transport_complete` 必须是 `None`；
- `make_round_sample` 必须继续保持 `None`；
- `_universe_session` 不得把它计入 `rounds_transport_incomplete`；
- verdict 必须 `not_measured`，不得写“provider 声明截断”。

### RED B：session transport consumer
green t0 + `rounds_transport_incomplete=1`：
- 完整 verdict signature 必须变化；
- `universe_transport` 必须 fail。

### RED C：session without t0
setup 缺 `universe_truth` + session stale：
- 必须 fail；
- 不能因为 `_ut` 缺失整段跳过。

### RED D：watchlist ratio
30 轮：
- 0/30 → full-market soak clean；
- 1/30 → 必须可见；
- 29/30 → 必须 fail；
- 30/30 → 必须 fail；
- intentional `--watch-only` → 不误报。

不要只新增 `setup OR current fell_back bool`；当前 bool 只有 30/30 才 True。

### RED E：unknown freshness
`states={"unknown":30}`：
- detail = 未测量；
- 不能叫“新鲜”。

### GREEN
新增/整理：
- `_optional_bool`
- `_optional_float`
- `ever_watchlist_only`
- `watchlist_only_ratio`
- session facts 独立消费块
- consumer-effect mutation tests

---

## WP02 — Universe freshness clock / config / defensive exit

- `universe.refresh_seconds` 改为正式 `poll.universe_refresh_seconds`。
- 区分：
  - active scope applied time
  - last full-market complete time
- watchlist fallback 不得把“2h 没拿到全市场”改写成 “0s”。
- refresh orchestration 外层加 ledger finalizer。

**严重度纪律**：
outer `_universe_sources()` crash 当前只按 P2 robustness；普通 provider 网络异常、build_source 异常、正常 Settings.get 已各自被 catch/安全处理。
只有找到一个仓库真实产品输入能触发 outer crash，才允许升级。

---

## WP03 — SourceManager snapshots soft-empty failover【P1】

RED：
```text
requested codes 非空
primary.snapshots -> []
backup.snapshots -> valid quotes
```
backup 必须被尝试。

负控制：
- caller no-dispatch 不进入 SourceManager；
- primary raises 既有 fallback 不回归；
- universe empty 不套 snapshots policy。

---

## WP04 — Timezone【P1】

- `app.timezone` 接 `zoneinfo.ZoneInfo`；
- same epoch 在 UTC / Asia/Shanghai host 得到相同 phase/is_open；
- injected replay clock 的 aware/naive 合同固定；
- provider timestamp 不决定 session phase。

---

## WP05 — ObservationInterval【P1】

新增 interval：
```text
first_observed_at
last_observed_at
price
cum_volume
source_epoch
```

fresh-flat：延长 `last_observed_at`。
true gap：不延长。
source epoch change：切段。

RED：
- t0 10.0，t5..60 fresh-flat，t65=10.3；
- 60s coverage 与 price_change 必须一致；
- 真 gap 对照不得伪造 coverage。

---

## WP06 — Universe membership / quote usability【P1】

单独 diff，不和 health consumer 混一起。

provider replay：
```text
raw codes=100
usable quotes=94
transport complete=true
```
目标：active membership 仍 100；6 个 code 在 snapshot 阶段再判 unavailable/suspended/zero。

---

## WP07 — per-code provenance/capability

完成 `(route,code)->source/source_epoch/capability/observed fields`，给 targeted fallback 与质量因子使用。

---

## WP08 — Research dataset / labels / factor registry

把现有 32-factor catalog 落成可执行 registry：
- 公式单测
- 因果性
- missing mask
- capability gate
- train-only scaling
- same-time-of-day prior-day baseline

真实数据不存在就 blocked，不买、不持续抓。

---

## WP09 — Tabular + attribution【真实数据满足后】

TaskSpec：H=180 主标签，60/300 辅助；known_at / label_end / unknown / purge。

模型：
rule → Logistic → HGB → small MLP。
都要 fit→checkpoint→reload→predict→evaluate。

---

## WP10 — SSE durable + TCN / shadow

先关闭：
committed→sent→received→applied、cursor/gap、silent drop-oldest、bounded state。

之后才跑 TCN；time-forward 多 fold 无稳定增量直接淘汰。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

transport_tristate_red.log
transport_tristate_green.log
transport_tristate_rollback.log

consumer_effect_red.log
consumer_effect_green.log
consumer_effect_rollback.log

watchlist_ratio_red.log
watchlist_ratio_green.log
freshness_unknown_red.log
freshness_unknown_green.log
freshness_clock_red.log
freshness_clock_green.log

source_empty_red.log
source_empty_green.log
timezone_red.log
timezone_green.log
observation_interval_red.log
observation_interval_green.log

health_consumer_cases.json
universe_refresh_cases.json
source_empty_cases.json
timezone_cases.json
observation_interval_cases.json
per_code_provenance.json

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md

full_pytest.log
check_*.py logs
dash_render_check.log
```

未适用项必须 `not_run/blocked + 原因`，禁止伪造 0 成绩。
