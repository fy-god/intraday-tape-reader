# 本地 Agent 任务书 — 2026-09-23 20:09:44 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- product SHA：`ff8fd8f125cd785340c6fd62f3d15db61b02f333`
- 审计开始 docs HEAD：`014435893a63d02b35ae48c34f3179c9a4fa8845`
- 开工前重新 fetch；docs-only commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取、付费服务。
- probes 使用固定 payload / FakeSource。

## WP01 — Source v4 自洽性修复【第一优先】

### RED 1：Coverage 双轴
fixture：
```text
R=3
P=3
Q=1
```

必须：
```text
raw_return_coverage = 1.0
usable_coverage = 1/3
```

禁止同一个 generic coverage 同时服务 RoundObservation raw coverage。

### RED 2：Tencent explicit-prefixed stock
```text
requested = ['sh600000']
raw = sh600000 普通股票
```

必须：
```text
R = {600000}
P = {600000}
Q = {600000}
missing = empty
unexpected = empty
```

显式 prefix 不是 index 身份的充分条件。

### RED 3：merge invalid→valid
同 code：
```text
part A raw present / invalid
part B raw present / valid
```

必须：
```text
admitted={code}
quality=empty
duplicate={code}
identity_holds=True
```

`quality` 必须在 global merge 后按 P-Q 重算。

### rollback
逐项注入旧错误实现，三条行为 RED 必须恢复。

---

## WP02 — SourceManager detailed route【P1】

新增 `call_detailed` 或同等接口。

要求：
- 结果与 actual serving source 原子绑定；
- built-in Tencent/Sina/Eastmoney 用 snapshots_detailed；
- legacy source 包成 quote_projection outcome；
- legacy `raw_presence_known=False`；
- failover 后 result.source 必须是实际服务源。

测试：
- primary detailed exception -> backup exact outcome；
- 同一 raw fact failover 前后 terminal semantics 不变；
- source epoch 使用 actual serving source。

---

## WP03 — Engine unified Round Attribution【P1】

不要 normal / empty 两套记账。

新增统一 helper，输入：
```text
stock SnapshotFetchResult
index SnapshotFetchResult
state time-admission result/deltas
```

输出 terminal buckets：
```text
admitted
unknown_missing
rejected_quality
future
out_of_order
unattributed_requested
```

不变量：
```text
Requested = disjoint union(terminals)
```

Exact built-in：
`unattributed=0`。

Legacy：
不能把 R-Q 叫 exact missing。

`RoundObservationSet.returned=|P|`。
原 coverage 继续 raw P/R。

---

## WP04 — live-session / Store schema

新增：
```text
unattributed_requested
unattributed_total
unattributed_rounds
raw_return_coverage
usable_coverage
```

保持：
```text
missing_total = exact provider absence
```

不要让 quality/unattributed 污染 missing。

---

## WP05 — Eastmoney unknown-total pagination【独立小包】

落此前实际 RED/GREEN：
- 已请求 raw page 先记账；
- stop based on raw/member empty；
- pages_requested/pages_ok 与实际 fetch calls 对齐；
- rollback tooth。

不要夹 Membership/Timezone 大改。

---

## WP06 — correctness/performance residual

当前 median-of-7 已显著降低 flake，x5 full pytest 绿。

后续有余力再将 wall-clock benchmark 从 default correctness gate 完全拆开。
不再把旧单样本 P1 当本轮最高优先。

---

## WP07 — Membership 5923/5553

仅 WP01-04 绿后启动。

---

## WP08 — SourceManager exact soft-empty

仅 exact:
```text
raw_presence_known=True
P empty
R nonempty
```
触发 soft failure。

---

## WP09 — Timezone / ObservationInterval / provenance

继续关闭。

---

## WP10 — Research

真实 multi-day manifest 后：
labels/splits -> factor diagnostics -> rule/Logistic/HGB/MLP -> SSE durable -> TCN。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

coverage_semantics_red.log
coverage_semantics_green.log
coverage_semantics_rollback.log

tencent_key_axis_red.log
tencent_key_axis_green.log
tencent_key_axis_rollback.log

merge_terminal_red.log
merge_terminal_green.log
merge_terminal_rollback.log

source_manager_detailed_red.log
source_manager_detailed_green.log
round_attribution_red.log
round_attribution_green.log
round_attribution_rollback.log

pagination_red.log
pagination_green.log
pagination_rollback.log

snapshot_outcome_cases.json
round_attribution_cases.json
coverage_cases.json
pagination_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若训练）
predictions.csv（若训练）
ablation.csv（若训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须写 `not_run/blocked + 原因`。
