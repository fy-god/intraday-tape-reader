# 盘中预警系统 · 审计补充（回归核对）— 2026-09-22 10:05:00 JST

## 报告身份

- `publication_kind`: `CURRENT_INTRADAY_RESEARCH_AUDIT_ADDENDUM`
- `audit_time_jst`: `2026-09-22T10:05:00+09:00`（CST `09:05:00`）
- `reviewed_repo`: `D:\ccc\ashare-radar`（`fy-god/intraday-tape-reader`，`main`）
- `reviewed_source_sha`: **`369b13f0d6f20fc22198abf51469ee14e49c84dc`**
- `extends`: `docs/audits/intraday/2026-09-22_09-49-38_JST.md`（`b4084c93…`）
- `report_created_commit_sha`: 见 §6（回读后回填）
- 开放 PR：0

**本文件性质**：只读子 agent 完成了对 **12 个既有开放项**的**逐项回归核对**（在它自己的 `git archive` scratch 副本内实跑，
237/237 文件 `git hash-object` 逐字节校验等于 `origin/main`）。**我已逐条独立复算其关键主张**（见 §3），
并**修正了它的一处措辞错误**（见 §4）。所有数字均为子 agent 实跑 + 我复核的真实退出码，无编造。

---

## §1 结论：12 项开放项的真实状态 = **9 项已修 / 3 项仍存活**

- `FIXED`: 9
- `CONFIRMED_STILL_PRESENT`: **3**（`IT-P0-002` 的时区分句、`IT-P1-SOURCE-EMPTY-001`、`IT-P1-WINDOW-001`）
- `NOT_REPRODUCED` / `COULD_NOT_TEST`: **0**

**关键点：这份清单与上一轮报告 §7 的"既有未决项全部保留"**（`2026-09-22_09-00-00_JST.md:287-291`）**不一致。**
那份清单把 `IT-P0-001`、`IT-P1-LIMIT-001` 也列为开放 —— 但二者**早已修复**，本轮**独立复现**确认。

**基线（真实跑数）**：scratch 内全量 `pytest -o addopts= -ra` → **1745 passed in 95.98s，exit 0**
（与产品轮自述的 `1745 passed` 一致；我用 `-o addopts=` 绕开 `pyproject.toml:59 addopts="-q"` 的摘要抑制）。

---

## §2 仍存活的 3 项（每项都比文档描述的**更窄**）

| ID | 文件/行 | 触发/反例（真实执行） | 机制 | 影响 | 修复建议 | 验收测试 |
|---|---|---|---|---|---|---|
| **`IT-P0-002`** | `src/arad/session.py:214`；`src/arad/sources/eastmoney.py:149` | 同一真实时刻，CST 宿主机判 `phase=morning`，UTC 宿主机判 `phase=closed` | **仅剩"宿主时区"这一分句**：`session.py:214` 用裸 `datetime.now()`（无 tz 参数），全仓 `git grep zoneinfo src/` **rc=1，0 命中**；`app.timezone` **0 个读者** | 非 UTC 宿主机在边界时刻会把"已收盘"读成"交易中"（或反之） | 收口到显式 tz-aware 时钟；给 `app.timezone` 落实读者 | 用两个固定 tz 的注入时钟求 `phase()`，同一 epoch 必须同结果 |
| **`IT-P1-SOURCE-EMPTY-001`** | `src/arad/engine.py:547-589` | 主源**返回 `[]` 且不抛异常** → `returned []`、`backup.calls=0`、`serving=0`、`fails={'stocks':0}`；**正对照**：主源**抛异常** → 故障转移成功（`backup.calls=1`） | 故障转移**只由异常驱动**（`:567 except Exception` → `:570 continue`）。空列表是"成功返回"，落到 `:582 return out` | **已收窄**：不再是假绿（轮次记账现在会暴露缺口）；但轮询面仍会静默缩小 | 空结果也纳入故障转移判据（区分"真·空"与"源端沉默"） | 主源返回 `[]` → 断言 `backup.calls >= 1` |
| **`IT-P1-WINDOW-001`** | `src/arad/engine.py:313`；`src/arad/rules/tick_surge.py:198` | 复现文档给出的精确输入：13 条"新鲜但价格不变"的观测**塌缩成 2 个点**；`_covered(60s)=True` 而同刻 `price_change(60s)=None`（真实变动 +3.0%） | `engine.py:313` 仍**仅**在 `prev != q.price` 或 `h[-1][2] != q.volume_lots` 时 append ⇒ change-only 历史不保留"持续收到新鲜平价观测"的覆盖证据；`tick_surge.py:198` 只拿 `hist[0][0]` 判覆盖 | 横盘后急拉会被 `_covered` 判为"覆盖足够"却 `price_change=None`，该信号**静默不评估** | 引入区间结构保留 first/last observed；真实缺口不得前填 | 13 条平价新鲜观测后，`_covered` 与 `price_change` 必须**一致** |

> **§2 的严格边界**：`IT-P0-002` 的另两个分句（window ≤ now 上界、write-ahead admission）经复核**已修**；
> `IT-P1-SOURCE-EMPTY-001` 已**不再是假绿**。**三条都不再是 P0 级假绿**，是**收窄后的残留**。

---

## §3 已修的 9 项（独立复现，非引用仓库自测）

| ID | 文件/行 | 复现依据（真实执行） |
|---|---|---|
| `IT-P0-002-R2` | `src/arad/engine.py:316-318` | 水位线改为**无条件推进**；R1/R2/R3 平谷反例被拒（`ooo=1`, latest `10.0`）。**变异测试**：改回 `history[-1][0]` → **7 failed / 5 passed**；还原 → **12 passed**；`engine.py` 复核逐字节一致 |
| `IT-P0-001` | `src/arad/session.py:46,87,156-160` | `14:56:59=afternoon`、`14:57:00=close_auction`、`14:59:59=close_auction`、`15:00:00=post`；`CLOSE_AUCTION ∉ CONTINUOUS`；14:57 时为长 `14220` |
| `IT-P1-LIMIT-001` | `src/arad/rules/limit_board.py:210-221` | 阈值 200 万：第 1 轮（100 万）=`[]`，第 2 轮（300 万）=`[limit_up]`；`at_limit_unqualified` 后置 sealed |
| `IT-P2-OBS-001` | `tools/live_session.py:2188`（+`:233-260`） | soft-partial 500 请求/450 返回 → 逐轮 `quotes=450`、`coverage=0.9`、`missing=50`、`error=False` |
| `IT-P1-UNIVERSE-DENOMINATOR-001` (R-12) | `engine.py:877` + `store.py:346` + `live_session.py:1399-1478` | 文档矩阵**逐行复现**：`4200/4100→ok`、`5913/4100→fail`、`5913/4500(76.10%)→fail`（原为 ok）、`5913/5850→ok` |
| `IT-P2-OBS-EMPTY-ROUND-R1` | `engine.py:981-999, 1001-1028`（调用点 `:1311`,`:1594`） | 6 个空轮：`index_requested=[0,…]`（幻影消失）、`requested=[500,…]`（== 实际派发）、`observation_poll_count=[1..6]` 单调 |
| `IT-P2-UNIVERSE-STATUS-CACHE-001` | `store.py:293-351` | 池 4100 / 缓存 5913 → `universe=4100`、`universe_cached=5913` |
| `IT-P1-UNIVERSE-HEALTH-GATE-001` (R-21) | `live_session.py:1350-1397` | `universe` 判决项存在；8/8 档位吻合；5913 与 4100 的 checks 字典**不再相同**。**边界**：4100 仅 `warn`，**不翻退出码**（有文档化的设计决定） |
| `IT-P1-SOAK-CUMULATIVE-QUOTES-001` | `live_session.py:2188`, `:730` | 真实 `_soak_loop`：逐轮 `quotes=[500,0,0,0,0,0]`、`no_data_rounds=5`、`healthy=False/exit 1`（原为完整假绿） |
| `IT-P1-ACK-TRIM-ORDER-001` | `store.py:56,59,400-425` | `_acked` 为 **dict**（保插入序）；5001 条真实告警 → `set(_acked)==最新2000` 为 **True**，最新键存活、最旧被裁；`recent_alerts` 最新项 `acked=True`（原为 False） |

---

## §4 我修正子 agent 的一处措辞错误（**重要**）

子 agent 报 `IT-P1-WINDOW-001` 时写：**"`ObservationInterval` genuinely absent（`git grep` exit 1）"**。

**我的复核**：`git grep -n ObservationInterval origin/main` → **rc=0，58 处命中**。
但**全部 58 处都在 `docs/audits/` 的散文/任务书里**，`git grep … -- src/` → **rc=1，0 命中**。
⇒ **它的实质结论成立**（`src/` 下**确实没有**该实现），但**措辞不准确**："genuinely absent" 会被读成"仓库里没这个词"。

**我按纪律**（nn：先验证语义再信名字；qq：两次运行不一致必须先解决再报）
**采用修正后的表述**：`ObservationInterval` **在 `src/` 中零实现，仅作为 58 处前瞻性设计散文存在于审计文档中**。
§2 表格里用的是修正后的措辞。

> **这条本身是个方法学观察**：同一个"absence"主张，**限定路径**（`-- src/`）与**不限定**（全仓）结论相反。
> 报"某符号不存在"时**必须写明搜索路径**，否则下游会据此做出错误判断。

---

## §5 文档漂移（**本身是一条发现**）

`docs/audits/intraday/2026-09-22_09-00-00_JST.md:287-291` 的"既有未决项全部保留"块仍列
`IT-P0-001`、`IT-P1-LIMIT-001` 为开放项。

**我复核**：该块出现 `IT-P0-001` ×1、`IT-P1-LIMIT-001` ×1；
而 `docs/audits/intraday/2026-09-21_01-40-00_JST.md`（792 行）中 `IT-P0-001` 出现 **8 次**、`IT-P1-LIMIT-001` **2 次**，
且两者经子 agent 与**我**独立复现均为 **已修**（`session.py:46,87,156-160` / `limit_board.py:210-221`）。

**影响**：任何**从该块构建下一轮回归清单**的 agent 会**重新打开 2 个已关闭项**，
并挤占真正存活的 3 项的注意力。这正是本文件 §1 要纠正的。

**建议**：该未决项块应改为**引用 `LATEST.md` 的机器可核对清单**，而非手写枚举。

---

## §6 三轴、分类、证据分级与边界

**状态三轴（严格分开）**
- `execution_status`：子 agent 在自建 scratch 内实跑 **14 个探针（全部 exit 0）** + 全量 pytest（exit 0）+ 变异内测（变异 exit 1 / 还原 exit 0）；**活仓库 `git status --porcelain` = 0 行**。
- `research_verdict`：`9_OPEN_ITEMS_FIXED` / `3_ITEMS_STILL_PRESENT_AND_NARROWER_THAN_DOCUMENTED` / `PREVIOUS_OPEN_ITEM_LIST_IS_STALE` / `OBSERVATIONINTERVAL_ABSENT_FROM_SRC_NOT_FROM_REPO`。
- `evidence_status`：`VERIFIED_BY_INDEPENDENT_AGENT_AND_RECOMPUTED_BY_MAIN_AGENT`（§2/§3 关键条我亲自 grep/读源复核）；
  `CORRECTED_WORDING`（§4）；`VERIFIED_STATIC_ONLY`（§5 文档漂移，纯文本核对）。

**发现分类**
| 编号 | 分类 | 严重度 |
|---|---|---|
| `IT-P0-002`（仅时区分句，`session.py:214`） | **已确认错误** | P1（**非** P0，已收窄） |
| `IT-P1-SOURCE-EMPTY-001`（`engine.py:547-589`） | **已确认错误** | P1（**已不是假绿**） |
| `IT-P1-WINDOW-001`（`engine.py:313` / `tick_surge.py:198`） | **已确认错误** | P1 |
| 9 项（§3） | **已经修复** | — |
| `IT-P0-002-R2` 变异测试（§3 首行） | **修复后回归：成立**（7 failed/5 passed ↔ 12 passed） | — |
| `2026-09-22_09-00-00_JST.md:287-291` 未决项块（§5） | **已确认错误**（文档漂移） | P2 |

**`evidence_type` 分开计数**
- **软件样本**：`1745 passed`（exit 0）、14 探针 exit 0、变异 7 failed/5 passed ↔ 12 passed。
- **实股结果**：**无新增**。真实 Precision / Recall / 漏事件率 / 交易收益 = **`unavailable`**。
  本文件全部证据为**软件样本**（合成 fixture / 注入源），**不是**实股证据。

**三类分开（不得混算）**
- **程序修复**：**0 条新增建议**（本文件是**核对**，不是新修复方案；§2 表格的修复建议为既有方向的复述）。
- **任务定义变更**：**0**。
- **真实模型增益**：**0**。本轮无训练、无模型改动。

**边界与合规**
- 写入**仅限**：本文件 + `docs/audits/intraday/LATEST.md`（**加性**，保留既有全部链接）。
- **活仓库零改动**；子 agent 与我的 scratch 副本均在仓库之外；**未**触碰他人未提交改动。
- **未**创建/修改任何定时任务；**未** force push；**未**推整条分支；**未**合并 PR；
  **未**改源码/权重/配置/Actions/README；**未**启动行情服务或训练。
- **未**编造 SHA/hash/通过数/退出码；未实际运行项已在文中标明。
- 该仓库**无** `docs/audits/validate_latest.py`（实测不存在）⇒ **不声称**通过任何验证器门禁。
- `report_created_commit_sha`: `PENDING_READBACK`。

---

## §7 下一轮优先顺序（修正 §1 的清单）

1. **`IT-P1-WINDOW-001`**（真正未触碰、且会让信号**静默不评估**）—— 最高优先。
2. **`IT-P0-002` 时区分句**（非 UTC 宿主机会误判交易时段）。
3. **`IT-P1-SOURCE-EMPTY-001`**（空结果不触发故障转移）。
4. **修正 `2026-09-22_09-00-00_JST.md:287-291`** 的陈旧未决项块（§5），
   避免下一轮重新打开 `IT-P0-001`、`IT-P1-LIMIT-001` 两个已关闭项。
5. 上一份报告（09:49）的 2 条 P1（`...TRUNCATION-BLIND-001`、`...META-STALE-001`）**优先级不变**，本文件未重复其证据。
