# 盘中预警 审计追加 — 2026-09-22 16:20:44 JST（零轮路径的产物级证据）

- `publication_kind`: `CURRENT_INTRADAY_AUDIT_ADDENDUM`
- 追加对象：`docs/audits/intraday/2026-09-22_16-20-44_JST.md`（`c5b1080d492eaecec26584399e545e063c13671c`）
- `reviewed_repo`: `https://github.com/fy-god/intraday-tape-reader.git`（`D:\ccc\ashare-radar`）
- `reviewed_source_sha`（被测产品提交）：`6c8f11e363b3efcd21e410a5af7a718e08e8564d`
- `execution_status`: `COMPLETED`
- `research_verdict`: `ZERO_EVIDENCE_FULL_MARKET_CLAIM_ARTIFACT_VERIFIED / DEAD_STORE_CONFIRMED / TRANSPORT_CONTROL_CLEAN`
- `evidence_status`: `VERIFIED`
- `evidence_type`: `SOFTWARE_SAMPLE`

一个**只读**子 agent 受命复核本轮的 P1。它**独立确认**了该缺陷，并提供了**比我原报告更强**的触发路径。
我按规则 (vvv) **逐条独立复算**它的新主张 —— 它当时测的是**提交前**的工作区副本（3,041 行，与实际不同），
因此我**在提交版 `6c8f11e`（3,051 行）上重做**，行号以提交版为准。**结论：它的新主张全部成立**，
其中一条把影响面从「真实 Engine 运行中」**提升到「产物级、且发生在 harness 失败的场景」**。

---

## 1. 【新增 · 产物级证据】零轮路径：**报告 JSON 里写着「全程全市场扫描」**

我原报告 §3 的复现需要真实 Engine 跑到「池被清空」。子 agent 指出一条**更短、更普通**的路径，
并给出 `build_report` 序列化证据。我独立复算：

```text
finalize_metrics([])  (rounds=[])
  -> fm['universe_session'] = {'measured': False,
                               'watchlist_only_rounds': 0,
                               'watchlist_only_ratio': None,     <-- 正确地表达了「未测到」
                               'ever_watchlist_only': False}     <-- 却是 bool，把「未测到」压成「没有」
  -> universe_scope = ok | 会话期间未降级为仅自选股（全程全市场扫描）
```

**我实测的产物级结果**（真实 `finalize_metrics` + 真实 `evaluate_health` + 真实 `build_report`）：

```text
report built OK; serialized bytes = 4,627
POSITIVE CLAIM present in the report JSON = True      <-- 「全程全市场扫描」
  rounds           fail   完成 0 轮（下限 1）
  data             fail   单轮最多拿到 0 只行情
  api              fail   请求 0 次：非 200 0，JSON 解析失败 0，连不上 0
  sse              fail   连接 否，事件 0 条，断流 否
  universe_scope   ok     会话期间未降级为仅自选股（全程全市场扫描）
verdict healthy = False
verdict fail    = ['rounds', 'data', 'api', 'sse', 'memory']
```

**为什么这比我原报告严重**：原报告的复现需要 Engine 真的把池清空；
而这条路径只需要 **soak 一轮都没跑成**（`rounds=[]`）——
例如 `create_server` 抛异常、或第 1 轮前就被 Ctrl+C。
此时**报告是一份「harness 级失败」的报告**（`rounds=0`、`healthy=False`，
`fail` 里是 `rounds/data/api/sse/memory`），
**却同时断言「会话期间未降级为仅自选股（全程全市场扫描）」**。
**一份承认自己什么都没跑成的报告，不允许声称自己全程扫了全市场。**

**精确分支**（提交版行号）：
`tools/live_session.py:552-554` 的 `"ever_watchlist_only": bool(watch_only)`
⇒ `:1576` 的 `elif _w_ever is None:` **永不成立**（实测 `_w_ever = False`）
⇒ 落到 `:1590 else` ⇒ `:1591-1592` 输出肯定句。
**生产者已经把「未测到」正确编码为 `watchlist_only_ratio = None`，消费者却从不读它。**

## 2. 【新增 · 自相矛盾，且两个都是 `ok`】同一份判决同时说「无法判定」和「全程全市场扫描」

零证据输入下，**同一份判决**里：

```text
universe         ok   无股票池规模记录（旧报告/未采集），无法判定（跳过不算失败）
universe_scope   ok   会话期间未降级为仅自选股（全程全市场扫描）
```

**「我没有扫描池记录，无法判定」** 与 **「全程全市场扫描」** 同时成立，且**两条都是 `ok`**
⇒ 这个矛盾在 `healthy` / 退出码里**完全不可见**。

## 3. 【已确认 · 新增】`_sess_measured` 在 `universe_scope` 里是**死赋值**

子 agent 报告该变量是死存储。它用的是**全文件** AST 扫描（会把不同函数混在一起），
我改用**函数归属**的 AST 扫描在提交版上复算：

```text
L 1555  STORE in evaluate_health()  | _sess_measured = bool(_sess.get("measured"))
L 1804  STORE in evaluate_health()  | _sess_measured = bool(_sess.get("measured"))
L 1810  LOAD  in evaluate_health()  | if _sess_measured and _ti > 0:
L 1819  LOAD  in evaluate_health()  | elif _sess_measured and _ti == 0 and _tmeas > 0:
L 1834  LOAD  in evaluate_health()  | if _sess_measured:
```

**`L1555` 的赋值在任何读取（最早 `L1810`）之前就被 `L1804` 覆盖 ⇒ 死存储成立。**
**精确表述**：`universe_scope` 块（`:1568-1592`）**从不读取** `measured`，
所以块内那个「我测到了吗」的守卫值从未被用上；
`:1804` 之后的值**是**被 `:1810/:1819/:1834` 使用的（传输块）。

## 4. 【对照 · 未发现同类缺陷】`universe_transport` **没有**零证据肯定句

同一份零证据输入下：

```text
universe_transport  ok  transport 完整性**未测量**（无 transport_complete 字段 / 冷启动未声明 / 旧报告）—— 跳过不算失败，但也不代表完整
universe_freshness  ok  无活跃快照时间/刷新尝试信息，无法判定（跳过不算失败）
universe_coverage   ok  无 universe_truth（旧报告/未采集），无法判定（跳过不算失败）
```

**这三项都正确地说了「未测量 / 无法判定」——  epistemic 门控在它们身上是存在的。**
`:1819` 的肯定分支要求 `_sess_measured and _ti == 0 and _tmeas > 0`，
**被测量计数门控**，因此**不可能**从零证据发出完整断言。
⇒ **`universe_scope` 缺的正是这个模式**（子 agent 的方向判断，我复算确认）。

## 5. 子 agent 报告中我**未采纳**或**修正**的部分

- **行号全部不可用**：它测的是提交前副本（`tools/live_session.py` 3,041 行，实测 sha 前缀 `95916b9`），
  提交版是 3,051 行。**本追加全部行号以提交版为准**（例如它说 `universe_scope` 在 1543-1582、
  肯定句在 1580-1582；提交版是 1568-1592、肯定句 1591-1592）。
- **`_sess_measured` 的扫描口径**：它的全文件扫描结论方向正确，但我在**函数归属**下复算，
  才能给出「`universe_scope` 块内是死的、传输块内是活的」这一精确结论。
- **`finalize_metrics([])` 的键**：我原写入探针用 `.get()` 打印，
  会把「键不存在」和「值为 `None`」混为一谈；已改为直接检查 `universe_session` 是否存在
  （实测**存在**，且 `ever_watchlist_only` 是 **bool `False`**）。
- **标 `NOT_PROVEN` 的部分我不上报为缺陷**：输入分类的 truthiness 问题
  （`:532-539` 把 `'garbage'`/`[]`/`0` 计为已测量）被 `_optional_bool`（`:453`）缓解、
  **未证明生产可达**，故**仅记录、不列为缺陷**；t0 截断被会话全完整抑制同样**未证明可达**。

## 6. 状态与合规

- 本追加**只新增本文档并更新 `LATEST.md`**；**未改任何源码/权重/配置/Actions/PR**。
- **未创建、修改或删除任何定时任务或排程**；**未 force push**；未推整个分支。
- 作者工作区：**只读**，未覆盖（其改动已由作者自行提交为 `6c8f11e`）。
- 全部命令在 `D:\it_h16_c6`（`6c8f11e` 的 `git archive` 纯净导出）内执行，**未在真实工作树内运行任何测试**。
- 本轮**未跑**真实网络 soak、**未做**任何训练/评估；**无**真实模型增益。
- 归档交叉核对仍**不可得**：`docs/audits/intraday/` 下全部已提交 JSON 均不含 `universe_session`，
  故**仍不给真实发生率**。
- `程序修复`：**0**（本追加为纯审计，未提交代码）　`任务定义变更`：**0**　`真实模型增益`：**0**
