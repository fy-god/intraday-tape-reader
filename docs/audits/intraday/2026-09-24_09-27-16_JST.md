# 盘中预警审计报告 — 2026-09-24 09:27:16 JST（h=8 CST）

- `audit_time_jst`: `2026-09-24 09:27:16 JST`
- `audit_time_cst`: `2026-09-24 08:27:16 +08:00`
- `round_h_cst`: **8**（`dispatch.md` L22 → 盘中预警 ✔；h=8 ∉ {3,7,11,15,19,23}）
- `reviewed_repo`: `https://github.com/fy-god/intraday-tape-reader.git`（`D:\ccc\ashare-radar`）
- `reviewed_source_sha`: `7a4f549a15e78fe87db7de00796eff71b707ae9b`
  （**产品代码** `fix(sources,engine): WP01/WP02 身份单一 role + WP03 call_detailed 合同校验`，`arad-bot`）
- `audit_start_head`: `1a0bd7fa10e5847ac14d9262698e611636680536`（fetch == ls-remote ✔）
- `evidence_type`: `SOFTWARE_SAMPLE`（**无实股结果、无联网取数**）
- `本轮没有新增实股结果`

---

## §0 头条：**我上一轮的报告附录 A 是错的，本轮公开更正**

**我 h=4 轮补发的 `2026-09-24_05-57-34_JST_APPENDIX_A.md` 声称被修的提交「未推送」，这是错的。**

事实（本轮实测）：

| 对象 | SHA | 是否在远端 main |
|---|---|---|
| 我在附录 A 里引用的提交 | `24a982b96910759676a4e02e97392a7ca28f5782` | **否**（`merge-base --is-ancestor` = False） |
| **同一修复内容、已推送的孪生提交** | **`7a4f549a15e78fe87db7de00796eff71b707ae9b`** | **是**（祖先，且直接坐在我的索引提交 `1c69e5a` 上） |

两者**同一个作者（`arad-bot`）、同一时间戳（04:57:42）、同一提交标题**，
且 **5 个代码 blob 逐字节相同**（`engine.py` `e2980dd9`、`tencent.py` `030891cb`、
`test_source_manager_contract.py` `01ffecd0`、`test_sources_tencent.py` `a7adf2af`、
`test_tencent_identity_matrix.py` `0269d49a`），`git diff 24a982b9 7a4f549` **只差
`LATEST.md` 与我自己的报告文件** ⇒ 是**同一修复被 rebase 到我的索引提交上**。

**时间线（远端顺序，老→新）**：

```
26265dd  fy-god     04:59:10  我 h=4 报告
1c69e5a  fy-god     04:59:10  我 h=4 索引
7a4f549  arad-bot   04:57:42  ← 修复，就在我的索引之上
a6512b4  arad-bot   04:58:06  另一条审计的报告
834b782  fy-god     05:03:03  ← 我的附录 A（声称「未推送」）
fd403c3  fy-god     05:03:03  我附录 A 的索引链接
```

⇒ **修复在我写附录 A 之前就已经在远端**。我的断言是**假阳性错误声明**。

**根本原因（我的方法错误，必须点名）**：我用
`git merge-base --is-ancestor 24a982b9 1d438e50` 判定「未推送」——
但 `1d438e50` 是我**开工时**审计的尖端，而当时远端**已经前进到 `a6512b4`**。
**用陈旧的 base 做「是否在远端」的判定，答案必然错。**
这与本系列既有纪律「白名单 diff 必须用你读到的那个确切实尖做 base」是**同一条**——我上一轮违反了它。
**正确做法**：先 `ls-remote`/`fetch` 拿到**当下**尖端，再判祖先关系。

**影响与处置**：附录 A 的**结论**（该修复确实有效、且**没有**修 route isolation）**是对的**，
我在同一份文档里用净副本实测过；**错的只是「未推送」这一定性**。
我不改写已发布的历史文档（不可变），改为**在本轮新报告中公开更正并登记**。

---

## §1 本轮实际跑过的命令、退出码与真数

| # | 命令/动作 | 退出码 | 真实结果 |
|---|---|---|---|
| 1 | `Get-Date` | 0 | `2026-09-24 08:27:16`，**h=8** → 盘中预警 ✔ |
| 2 | `git fetch --no-tags origin` | 0 | `origin/main` 前进到 `1a0bd7fa…` |
| 3 | `git ls-remote origin refs/heads/main` | 0 | `1a0bd7fa…`，**== fetch 值 ✔** |
| 4 | `git rev-parse 24a982b9^{tree}` / `7a4f549^{tree}` | 0 | 树不同（`51c8448f` vs `d5f53269`）⇒ 需逐 blob 比 |
| 5 | **5 个代码 blob 逐一 `rev-parse <c>:<path>` 比对** | 0 | **5/5 完全相同** ⇒ 同一修复 |
| 6 | `git diff --name-status 24a982b9 7a4f549` | 0 | 仅 `A` 我的报告 + `M LATEST.md` |
| 7 | `merge-base --is-ancestor 24a982b9 <远端>` | 1 | **False**（我附录 A 的依据） |
| 8 | `merge-base --is-ancestor 7a4f549 <远端>` | 0 | **True** ⇒ 修复在远端 |
| 9 | `git archive` 净提取当前尖端（Python bytes） | 0 | **315** 文件 |
| 10 | **`python -m pytest . -p no:cacheprovider`**（净副本内） | **0** | **1919 passed in 81.96s** |
| 11 | **实跑真 `SourceManager`**（当前尖端） | 0 | route-isolation 缺陷**仍复现**（见 §2） |
| 12 | 实跑错类型源臂（当前尖端） | 0 | `raised=None`、`failover_to_good=True` ⇒ **已修** |
| 13 | 代码探针：`_preferred_by_route` 等 4 个模式 | 0 | `_preferred_by_route` **0 命中**、`self.idx` **11 命中**、`_fails.clear()` **3 命中** |
| 14 | `git diff --name-status 1d438e50 <尖端> -- . ':(exclude)docs'` | 0 | 5 个代码文件（他人在我审计后推送） |

**未跑及原因**：未联网取行情、未启动服务/通知/交易；**未在 `D:\ccc\ashare-radar` 工作树内跑
任何测试或脚本**（该树有他人改动）；未跑 mutation testing（**故不声称测试有判别力**）。

---

## §2 已确认错误 #1（**仍在，且为本轮主要发现**）：
`IT-P1-SOURCE-MANAGER-GLOBAL-IDX-BREAKS-ROUTE-ISOLATION-008`

**当前尖端实测（`1a0bd7fa`，含已落地的 `7a4f549` 修复）**：

```json
{ "after_2_stock_failures": { "idx": 1, "fails": {} },
  "index_served_by": "backup",
  "primary_called_for_index": false,
  "primary_index_calls": 0 }
```

`threshold=2`、primary **仅对 `stocks` 失败**、`index` 从未失败 —— 结果 **index 由 backup 服务，
primary 连一次都没被调用**。**缺陷在已落地的修复之后依然完全成立。**

**代码面（当前尖端）**：
- `self.idx` **11 命中**（`:514`/`:521`/`:539`/`:561`/`:572`/`:579`/`:622`/`:623`/`:679`/`:686`…）
  ⇒ **仍是唯一全局首选源**。
- `self._fails.clear()` **3 命中**（`:540`/`:580`/`:687`）⇒ **仍清整张表**。
- `_preferred_by_route` **0 命中**、`preferred_routes` **0 命中** ⇒ 逐路由首选源**未被引入**。

**为什么这条最重要**：他人在我上一轮之后**确实推了修复**（`7a4f549`，5 文件、含新增测试），
但**修的是另一个缺陷**（`call_detailed` 合同校验），**route isolation 完全没被触及**。
读者极易把「有人推了 fix(sources,engine)」误读成「route 问题已解决」。**必须明确：没有。**

**合同冲突（上一轮已确认，本轮不变）**：`tests/test_engine.py:259`
`assert primary.served == [1, 1]`，注释 `:256-258` 称「这恰好说明切换真的生效了」
⇒ **现有测试把跨路由耦合钉成了期望值**。修它必须一并改写
`test_engine.py:251/255/259`、`:293`、`test_source_manager_detailed.py:235`
（子 agent 变异实测 `4 failed, 75 passed`）。

**验收测试**：`stocks` 达阈值、`index` 零失败 ⇒ 断言 `serving_of("index")` 仍是**原 primary** 下标、
且 index 调用后 `primary` 被调用过。**阳性对照**：`threshold=1` 且 index 也失败 ⇒ 断言此时**确实**换源。

---

## §3 已确认错误 #2：`IT-P2-CALL-DETAILED-TYPE-ERROR-ABORTS-FAILOVER-004` —— **已在远端修复**

**当前尖端实测**：错类型源 + 健康备用 ⇒ `raised=None`、`served_by="good"`、`failover_to_good=True`。
对照我 h=0 轮在 `1d438e50` 上的实测：`AttributeError: 'dict' object has no attribute '_replace'`、
备用源调用 **0** 次。

**修复提交**：`7a4f549a15e78fe87db7de00796eff71b707ae9b`（`arad-bot`，父提交 = 我 h=4 的索引 `1c69e5a`），
文件 5 个：`M src/arad/engine.py`、`M src/arad/sources/tencent.py`、
**`A tests/test_source_manager_contract.py`**、`M tests/test_sources_tencent.py`、
`A tests/test_tencent_identity_matrix.py`。

⇒ 状态：**已确认错误 → 已在远端修复并回归通过**。**这是他人**的修复，**不是我**的（我本轮程序修复数 = 0）。

---

## §4 测试真数（我的测量，仓库外净副本内）

| 尖端 | 全量套件 | 退出码 |
|---|---|---|
| `1d438e50`（我 h=4 审的尖端） | **1891 passed** | 0 |
| `1a0bd7fa`（当前，含 `7a4f549`） | **1919 passed in 81.96s** | **0** |

净增 **28** 项。**README.md:265 写的 `1035 项` 已严重过期**（少报 **884**），
且文档命令 `python -m pytest -q` 在 `pyproject.toml:59` 的 `addopts="-q"` 下变成 `-qq`、
**静默不打印汇总行**（我 h=4 轮的受控对已证：单 `-q` → `1891 passed`；`-qq` → 无汇总行）。
**该缺陷在本轮尖端依然存在**（README 与 `addopts` 均未被改动），**仍未修**。

---

## §5 回归核对表

| 稳定 ID | 本轮状态（`1a0bd7fa`） |
|---|---|
| `IT-P1-SOURCE-MANAGER-GLOBAL-IDX-BREAKS-ROUTE-ISOLATION-008` | **仍存在**（实跑复现；已落地的修复未触及） |
| `IT-P2-CALL-DETAILED-TYPE-ERROR-ABORTS-FAILOVER-004` | **已修复**（`7a4f549`，实测 failover 成功） |
| `IT-P2-DOCS-PYTEST-QQ-SUPPRESSES-SUMMARY-003` | **仍存在**（README `1035`、`addopts="-q"` 均未变；真数 1919） |
| `IT-P2-AUDIT-REVIEWED-SHA-DOC-COMMIT-003` | **已修复**（上一轮索引已拆分 SHA；本轮延续该口径） |
| `IT-P1-SOURCE-EMPTY-001` | 未重测（标**未复现**，不声称已修） |
| `IT-P1-WINDOW-001` | 未重测（标**未复现**） |
| `IT-P2-LEGACY-FALLBACK-PHANTOM-MISSING-003` | 未重测（标**未复现**） |
| `IT-P0-002-TZ-R1` | 未重测（标**未复现**） |
| `IT-P2-OBS-001` | 未重测（标**未复现**） |

> 本轮因机器休眠导致时段跳跃（见 §6），我只重测了**与新增代码直接相关**的两项 +
> 文档/索引两项。**其余 5 项我明确标「未复现」而非「已修」**——**没有测量就没有结论**。

---

## §6 运行环境异常：机器休眠导致时段丢失（如实登记）

- 我 h=4 轮开工读数为 `04:57:34 CST`；本轮开工读数为 `08:27:16 CST`，**间隔 3.5 小时**。
- 期间的 `schedule-5` 提醒批次（h=5、h=6、h=7，对应 05:20 / 06:20 / 07:20 触发）
  **在我这一侧没有产生任何回合**——机器处于休眠/挂起，回合未被唤醒。
- ⇒ **h=5、h=6、h=7 三个时段未执行**（原模型 h=5、专家/ML h=6、EventNet h=7）。
  **我不补跑、不伪造**，如实登记为**未执行**。
- 旁证：远端在我休眠期间**有他人**完成两轮盘中预警（`497c440` 08:10 JST 审计、
  `6f02102` 任务书、`1a0bd7f` 索引），说明**其他写入者并未休眠**。

---

## §7 我本轮自己的缺陷（如实自曝，本轮 1 处）

1. **附录 A 的「未推送」定性错误（严重，已公开更正）。** 我用**开工时的陈旧尖端 `1d438e50`**
   做 `merge-base --is-ancestor` 的 base，而远端当时**已前进到 `a6512b4`**，
   于是把**已在远端**的修复 `7a4f549` 判成「未推送」。
   **根因**：违反本系列既有纪律「白名单/祖先判定必须用**你当下读到的那个确切实尖**做 base」。
   **教训**：任何「在不在远端」的判定，**必须先 `ls-remote`/`fetch` 取当下尖端，再判祖先关系**；
   并且**必须比对内容（tree/blob）而不只是比 SHA** —— 同一个修复被 rebase 后 SHA 会变，
   这正是我漏掉孪生提交 `7a4f549` 的原因。

---

## §8 研究/改进想法（各注明依据来自本轮哪条证据）

1. **把「未推送 / 已在远端」的判定做成带回读的自检。** 依据：§7 —— 我因陈旧 base 误判，
   且因为只比 SHA 而漏掉 rebase 孪生提交。规则应是：`ls-remote` 取当下尖端 → 判祖先 →
   **再比 5 个关键 blob 的 `rev-parse <sha>:<path>`**，三者一致才下结论。
2. **凡「他人在同一线推送了 fix」的时段，报告必须显式说明它修了什么、没修什么。**
   依据：§2 —— `7a4f549` 标题是 `fix(sources,engine)`，极易被读成 route 问题已解决，
   而实测 `_preferred_by_route` 仍 **0 命中**、缺陷仍复现。
3. **测试真数应写进唯一一处并被断言，而不是散落在 13 个文档里。** 依据：§4 ——
   README 写 1035、ARCHITECTURE 写 1064、实测 1919，且文档命令因 `-qq` 连汇总行都不打印。

---

## §9 三轴状态与三类增益（分开写，不混算）

| 轴 | 值 |
|---|---|
| `execution_status` | `PARTIAL`（净副本内 **1919 passed / exit 0**；当前尖端实跑 2 臂；blob 级等价性比对 5/5；**5 项回归未重测**；h=5/6/7 时段因机器休眠**未执行**） |
| `research_verdict` | route-isolation 缺陷**在已落地的他人修复之后依然成立**；`call_detailed` 穿透缺陷**已修**；文档 `-qq`/计数缺陷**仍未修**；**我上一轮附录 A 的定性错误已公开更正** |
| `evidence_status` | `VERIFIED`（两项结论均在当前尖端用真类实跑；修复等价性用 blob 级比对） |
| `evidence_type` | `SOFTWARE_SAMPLE`（**无实股结果**） |

- **程序修复（我做的）**：本轮 **0**。
- **程序修复（他人在本仓做的、已推送）**：**1** 个提交 `7a4f549`（5 文件）。**与本 agent 无关。**
- **任务定义变更**：本轮 **0**。
- **真实模型增益**：本轮 **0**；`本轮没有新增实股结果`。

---

## §10 声明

- 本轮**只写** `docs/audits/intraday/` 内的报告与索引；**未**改任何模型源码、权重、配置、
  Actions、PR、根 `README.md`、`SCHEDULE.md`、其他实验归档。
- **未**创建/修改/删除任何定时任务或排程（无 `schedule_*`、`arm_self_wake`、`schtasks`、
  `Register-ScheduledTask`）。
- **未**在 `D:\ccc\ashare-radar` 工作树内执行测试或脚本；一切在 `git archive` 提取的**仓库外副本**内进行。
- **未**对任何脏工作树做 `checkout`/`reset`/`stash`/`clean`/`merge`/`pull`。
- 所有 SHA/hash/通过数/退出码来自本轮**实际命令输出**；未亲跑者已标 **未复现**；**未编造任何数据**。
- **绝不 force push；只推 `main` 的 docs-only 提交；不推整个本地分支。**
