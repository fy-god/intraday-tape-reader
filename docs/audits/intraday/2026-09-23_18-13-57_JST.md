# 盘中预警「修复后回归」验证 — 2026-09-23 18:13:57

- `audit_time_jst`: `2026-09-23 18:13:57`
- `doc_kind`: **修复后回归验证**（`execution_status=已运行`）
- `follows`: `docs/audits/intraday/2026-09-23_17-58-08_JST.md`（原始报告）、`2026-09-23_18-07-42_JST.md`（更正）
- `reviewed_repo`: `https://github.com/fy-god/intraday-tape-reader.git`
- `new_product_commit_mid_round`: `9ed75efb50c41f9756c3fab2a0255685aea73868`（`feat(sources): Snapshot Outcome Contract v4 —— 三源精确 raw presence（WP01）`）
- `prev_reviewed_source_sha`: `9ec40c6740abdf9f480f4219490a891dcf7c0a11`
- `main_head_at_verification`: `a644a7c2d400af824e4545d7b52e2fc3439d3953`
- `report_created_commit_sha`: `PENDING_BACKFILL`
- `evidence_type`: **软件样本 = 有；实股结果 = 0**（`new_real_market_fits = 0`）

---

## 0. 本轮发生的三件事（都不在我 17:58 发报告时的视野内）

1. **一个并发 agent 在我审计期间提交了新的产品提交** `9ed75ef`（WP01 Snapshot Outcome Contract v4，三源精确 raw presence，5 文件 +1261 行）。**索引里写的「下一轮第一优先」正是这个 WP01**，它落地了。
2. **该 agent 随后在工作树里（未提交）实施了我 17:58 报告 §2.1 的建议**，`tests/conftest.py` 新增 `median_elapsed()`，并在注释里**逐字引用我的报告名与我实测的数字**（中位 31.566ms、最大 35.060ms、余量 6.34x、214.3ms 是 6.8 倍环境性停顿）。它同时按我 §2.2 的建议重写了我点名的四态测试。
3. **我因此对本轮自己的产出做了两次回归验证**：新提交是否使我的结论失效（§2）；并发 agent 的修复是否**真的有牙**（§3）。

**结论**：我的 §2.1 与 §2.2 findings 已被修复，且我用**阳性+阴性对照**证明修复**有效且未解除武装**；我 18:07 更正里的两条假绿**在新提交上逐字存活**（§2）。**三者互不冲突。**

---

## 1. 身份与新提交的影响面（**先证明审计对象没被换掉**）

| 文件 | `9ec40c67` | `9ed75efb` | 相同? |
|---|---|---|---|
| `tools/live_session.py` | `b2f791f0f4c8fb91b1a41a5382bee806679a6e77` | 同 | ✅ |
| `src/arad/engine.py` | `33862f36a30aa283d6821c409408b2b2fc793022` | 同 | ✅ |
| `src/arad/session.py` | `fd74113269abd83e8b630b6da183d1032f163d6e` | 同 | ✅ |
| `src/arad/rules/limit_board.py` | `3cbe986fcbcbfe4f40113d695022a37424c073c6` | 同 | ✅ |
| `src/arad/rules/tick_surge.py` | `f1b87d53e41232bda572d2f9c20ea126810fd791` | 同 | ✅ |

**新提交改的 5 个非 docs 文件**：`src/arad/sources/{eastmoney,sina,tencent}.py`、**新增** `src/arad/sources/outcome.py`（329 行）、**新增** `tests/test_snapshot_outcome_contract.py`（540 行）。

**新提交**新增文件里关键字计数：`raw_presence` **19**、`unknown_missing` **7**、`quality` **11**、`membership` **0**、**`transport` 0**、**`freshness` 0**。

→ 即：**新提交没有触碰我报告/更正所指认的任何一行**。

---

## 2. 我 18:07 更正的两条假绿：**在新提交上逐字存活**（`未复现修复`）

我以 **payload 逐字匹配**在新 tip `9ed75efb` 上复查（不是目视）：

| 机制记号 | 在 `9ed75efb:tools/live_session.py` | 行 |
|---|---|---|
| `elif _sess_measured and _ti == 0 and _tmeas > 0:`（CE1 存在量词门） | `STILL PRESENT` | `:2298` |
| `f"会话期间 {_tmeas} 轮 transport 均为完整（无截断）"`（CE1 全称句） | `STILL PRESENT` | `:2300` |
| `"freshness_measured_rounds": sum(states.values())`（CE2 数记录） | `STILL PRESENT` | `:737` |
| `_order = {"stale": 3, "aging": 2, "fresh": 1, "unknown": 0}`（CE2 排序） | `STILL PRESENT` | `:625` |
| `elif _fresh_measured < _rounds_total:`（永不为真的守卫） | `STILL PRESENT` | `:2337` |
| `f"会话期间股票池新鲜（{_fresh_measured}/{_rounds_total} 轮，"` | `STILL PRESENT` | `:2357` |
| `_rounds_total = _scope_measured`（潜在退化） | `STILL PRESENT` | `:1818` |
| `tests/test_snapshot_outcome_contract.py` 提到 `universe_transport` / `universe_freshness` / `rounds_transport_measured` / `freshness_measured_rounds` | **全部 0 次** | — |

→ **WP01 修的是 raw presence（另一个轴），没有修量词/分母那一类。** 我的两条反例**依然成立**。**新测试文件对我的两条判决行零覆盖。**

---

## 3. 并发 agent 对 §2.1 的修复：**我做了三重对照验证**

它落地的是**我建议的 (b) 方案**（多轮取中位数）。我**不因它引用我的报告就采信**，而是把它**未提交**的 4 个文件复制进**丢弃式克隆**（`clone` rc=0、`checkout 9ed75efb`、**断言 `HEAD == 9ed75efb…`**），再直接驱动它的 helper：

| 检查 | 预期 | **实测** | 判定 |
|---|---|---|---|
| **P1** 旧「单次采样」形式 + 注入 1 次 250ms 停顿 | 应**失败**（证明缺陷真实） | `elapsed = 250.5ms` → **FAILED** | ✅ 缺陷真实 |
| **P2** 新「中位」形式 + **同一次** 250ms 停顿 | 应**通过**（证明修复有效） | `median = 0.1ms` → **PASSED** | ✅ 修复有效 |
| **P3** 新「中位」形式 + **本来就慢**的函数（300ms） | 应**仍然失败**（证明未解除武装） | `median = 300.3ms` → **FAILED** | ✅ **断言仍有牙** |
| **P4** 判别力边界：7 次采样里允许几次停顿 | — | **≤3 次停顿通过；≥4 次失败** | 见下 |

**P3 是最关键的一条**：一个「怎么都不会红」的修复不是修复。它**会**红。

**P4 是我发现的新边界**（应回传给对方）：`median_elapsed(repeat=7)` 的判别力是**「多数样本正常」**。这意味着**3/7 ≈ 43% 的样本同时停顿**时它仍会通过。作为「抗单次环境停顿」的修复**完全够用**（这本来就是 my recommendation 的目的），但它**不是**「任意扰动都不翻转」的保证。若将来要更硬，可改用 `min` 或「中位数 + 上界 = 预算 * 8」。**这是我给出的边界，不是它的缺陷**——它明确写了「没有删除任何断言，也没有把性能用例移出正确性套件（那需要改 addopts/markers，超出本轮边界）」，**我认同这个边界交代**。

**两个真用例在克隆内实跑**：`test_rule_limit_board.py::test_performance_1000_stocks` **exit=0**；`test_rule_tick_surge.py::test_performance_1000_stocks` **exit=0**；`tests/test_health_consumer_contract.py` 全套 **exit=0**。

---

## 4. 我的 §2.2（四态测试）也被修复，且修法**优于**我的建议

我建议「改名并改成 5」或「由常量表驱动」。它选了**后者且更彻底**：**动态枚举** `dir(ls)` 里所有 `SCOPE_*`，断言**两两互异**，并额外断言 5 个必需常量**仍在**。

- **为什么这比我的建议好**：我原建议若只把 `4` 改成 `5`，**同一个硬编码问题只是推后一轮**（加第六态时又会陈旧）。它改为枚举后，**新增状态无需改测试**，而「两个状态被设成同值」——**真正的塌缩**——**立刻变红**。
- **我实测的验证**：该文件全套 `exit=0`；断言现在包含 `len(distinct) == len(values)` 与 5 个 `required` 常量的存在性双向检查。
- **定级**：`修复后回归` = **通过**。

---

## 5. 状态三轴

| 轴 | 值 |
|---|---|
| `execution_status` | 实跑：新 tip 身份比对 5 文件；payload 逐字匹配 7 处 + 新测试文件 6 个关键字计数；丢弃式克隆 clone/checkout/HEAD 断言；**P1/P2/P3/P4 四组行为对照**；3 个 pytest 目标实跑（exit 0）。 |
| `research_verdict` | 新提交 `9ed75ef`（WP01）**不触碰**我指认的轴 → 我的更正**依然成立**；并发 agent 对 §2.1/§2.2 的修复**经对照验证有效且未解除武装**。**模型真实增益 = 0。** |
| `evidence_status` | §2 = `未修复（在新 tip 上逐字存活）`；§3 = **`修复后回归` 通过**（P1/P2/P3 三对照，含「未被解除武装」阴性证明）；§4 = **`修复后回归` 通过**（且修法优于原建议）。 |
| 未跑 | 全套 pytest（仅跑了 3 个目标）、`check_*.py`、真实 soak、provider 回放、模型指标。 |

---

## 6. 诚实边界

- 我验证的是**工作树里未提交**的修复，不是任何已提交的东西。**若该 agent 改了主意或回退，本节结论随即失效。** 我记录的是 `mtime` 时刻的内容。
- **P1/P2/P3/P4 用的是我构造的函数**（`fast()` / `slow_always()` / 注入 `sleep`），**不是**那两个真实测试内部的 `evaluate()`。我因此**另跑了两个真实测试**（exit 0），但它们在本机余量很大（6.34x / 35.58x），**不会**自己触发停顿——所以**「真实测试在真实停顿下通过」这一条我未直接测到**；我用的是「helper 在注入停顿下的行为」+「真实测试在本机通过」两个分立的观测。**这个缺口如实写在这里。**
- 我**未**重跑 C 的 150 例可达性扫描；`:1817` 仍标 `潜在`。
- 我**未**改任何源码；未创建/修改定时任务；未 push 代码/权重/配置；未读 `D:\xm\60日预测`。
- **并发写入者**仍在活动（剩余脏行 `tests/conftest.py`、`tests/test_health_consumer_contract.py`、`test_rule_limit_board.py`、`test_rule_tick_surge.py`）；**非我所为**，我只读。
- 远端在我本轮期间**两次前进**（`fc235bf2` → `9ed75efb`），我每次都**重读 tip 后再挂接**。

---

## 7. 研究 / 改进想法（1–3 条）

1. **把「性能断言」与「正确性断言」在配置层面分开，而不是只在实现层面软化**（依据 §3 P4：中位数只保证「多数样本正常」）。`pyproject.toml` 已有 `[tool.pytest.ini_options]` 的 `markers` 与 `addopts = "-q"`，加 `perf` marker 并把 `addopts` 改为 `-q -m "not perf"`，另设一条性能 CI —— 这才是根治；当前修复是**在正确性套件里把门放宽**，方向对但停在半路。**这是可以立刻做的一小步**。
2. **把我的两条假绿（transport / freshness）变成会红的测试，并顺手修掉同因机制**（依据 §2：新测试文件对这两条**零覆盖**，而它们与已修好的 scope 轴**同因**）。具体：照抄 `_full_covers_all`（`:1819-1822`）的全称写法给 `:2298` 用，把 `:737` 的 `sum(states.values())` 改成只数有年龄证据的轮。
3. **用 `median_elapsed` 的边界反思「抗抖动」的度量选择**（依据 §3 P4）：中位数抗单次停顿但抗不住持续性降频（如热降频 / CPU 争抢）。若目标场景是 CI 机器噪声，`min-of-N` 比 `median-of-N` 更合适——**但必须同时保留一个「稳态上限」断言**，否则会变成 §3 P3 要防的那种「永远不红」。

---

## 8. 声明

- 本轮只对仓库做**只读**操作；所有代码执行在**丢弃式克隆**（`h16_fixclone`，`HEAD == 9ed75efb…` 已断言）内；真实工作树**未**被我写入。
- 未修改产品源码；未创建/修改/删除任何定时任务；未 push 代码/权重/配置/Actions/PR；未 force push。
- 所有 SHA / 计数 / 毫秒 / 退出码均为**实读实跑**；未直接测到的项（§6 第二条）已明确标出；未复现项未升级。
