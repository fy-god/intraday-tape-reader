# 本地 Agent 任务书 — 2026-09-23 12:11:56 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- 固定产品 SHA：`bf0b83bf212f169c720ca8a1c5108405c18a9712`
- 云端审计开始 docs HEAD：`bd5528485e045bb6fadadecb8fbcc0764baf3824`
- 开工前重读实时 main；docs-only commit 不算产品升级。
- 禁止真实通知、交易、自动 push、Actions 修改、持续抓取、付费服务。

## WP01 — Snapshot raw outcome【P1，第一优先】

不要先改 membership。

### RED A：raw-present-invalid vs raw-absent

用真实 `EastmoneySource` / parser + injected UList payload：

requested:
```text
600001
600002
600003
```

payload:
```text
600001: f12 有效；f2='-' 或 f18 无效
600002: 正常可用
600003: 完全不出现
```

修前必须证明：
```text
parse_ulist / snapshots 最终只剩 600002
Engine 无法区分 600001 与 600003
```

目标：
```text
600001 -> rejected_quality
600003 -> unknown_missing
```

### RED B：真正 raw missing
raw 只有 600002：
600001 / 600003 都 missing。

### RED C：质量阴性对照
600001 / 600002 都 valid：
只 600003 missing。

### 实现
新增：
```text
SnapshotFetchResult
requested_codes
raw_returned_codes
quotes
rejected_quality_codes
source
provenance
```

Eastmoney 增 `parse_ulist_page()` / 等价函数；先记录 raw code，再判 Quote usability。

`snapshots()` 旧 list[Quote] API 保留兼容。
Engine 新路径优先使用 detailed result。

---

## WP02 — Observation Ledger migration

目标：
```text
returned = raw returned code count
rejected_quality = raw-present unusable
unknown_missing = requested - raw returned
```

现有恒等式继续：
```text
admitted_stock
+ unknown_missing
+ rejected_quality
+ future
+ ooo
= stock_requested
```

不要新增 `membership_no_quote`，除非你能证明它表达了 rejected_quality 之外的独立 source fact。

---

## WP03 — Membership Truth【等 WP01/02 绿后】

再实现 UniverseMember：

```text
code
name
board
list_date
source
```

用现有 5923/5553 fixture：

```text
transport members = 5923
usable universe quotes = 5553
active membership target = 5923
```

验收：
- 370 old invalid-price members 留在 active membership；
- 10 new members 继续进入；
- 不造 fake zero-price Quote；
- list_date metadata 不回归；
- 下一 snapshots request 含 suspended/unavailable member。

注意：
`coverage_active = 1.0` 在 all-members active scheduling 下是合法语义。
不要为了保留旧 93.75% 数值而继续删 member。
availability 看 `coverage_usable / rejected_quality / provider_missing / round coverage`。

---

## WP04 — SourceManager soft-empty【detailed result 后】

只有：
```text
requested nonempty
raw_returned_codes empty
```
才定义 snapshots soft-empty failover 候选。

如果：
```text
raw_returned_codes nonempty
usable_quotes empty
```
这是质量问题，不应盲目切源。

---

## WP05 — 性能成本

在正确性通过后测：

```text
usable-only fast path
vs
all-members 5s
vs
two-tier availability probe
```

记录真实：
- HTTP batch count
- p50/p95/p99 latency
- CPU/RSS
- network error
- resume detection delay

不要用纯 batch 算术冒充 latency。

---

## WP06 — unknown-total pagination【P2】

修复：
- no-total stop condition 不能只看 `page.quotes`
- raw member page 必须入账
- pages_requested 真实计请求数

回归：
page2 raw members>0 / quotes=0 / page3 有数据，page3 必须被请求。

---

## WP07 — overlap coverage P2

正常 page overlap 可令 raw_unique > expected。
要求：
- raw overlap diagnostic 保留；
- health coverage 不得 >1 后静默变 not_measured；
- 不掩盖 denominator drift。

---

## WP08 — Timezone / ObservationInterval

分别独立提交。

---

## WP09 — provenance / dataset / factor diagnostics

Truth contracts 通过后开始真实 manifest。

---

## WP10 — tabular / SSE durable / TCN

先简单模型；TCN 最后。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

snapshot_outcome_red.log
snapshot_outcome_green.log
snapshot_outcome_rollback.log

membership_red.log
membership_green.log
membership_rollback.log

snapshot_outcome_cases.json
membership_reconcile.json
universe_truth_cases.json
source_empty_cases.json
pagination_cases.json
coverage_overlap_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须明确 `not_run/blocked + 原因`，禁止填 0。
