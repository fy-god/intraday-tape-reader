# 本地 Agent 任务书 — 2026-09-24 04:04:27 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`a0ca7e6caff972d22b903a80b1731e2b7d5a2f95`
- audit-start main/docs HEAD：`bd711c4a5a5a2a636717c7f883d2f9c5976aeabc`
- 开工前重新 fetch；docs commit 不算产品升级。
- 不真实通知、交易、自动 push、改 Actions、持续抓取或付费服务。

## WP01 — call_detailed 返回合同【P1】

目标文件：
- `src/arad/engine.py`
- `tests/test_source_manager_detailed.py`

先 RED：
1. primary `snapshots_detailed` 返回 `dict` / `list`
2. backup 返回正确 `SnapshotFetchResult`
3. 当前必须行为红：backup 未调用
4. 修后 backup 必须被调用并成功

实现：
- `isinstance(SnapshotFetchResult)` 校验放 per-source try
- source binding 放 try
- route validation 放 try
- contract failure 与 source exception 同样 `continue backup`

对 `result.route != caller route`：
- 推荐当 contract failure failover，不要静默吞，因为 source 可能按错误 route 处理过数据。

Rollback：
恢复旧位置后 RED 必须回来。

---

## WP02 — Tencent identity 单一角色源【P1 latent】

新增 table-driven：
```text
name-sensitive:
sh000922 sh000015 sh000903 sh000904 sh000906 sh000009 sh000133

safe index:
sh000001 sz399001 sz399006 sh000300 sh000688

explicit stock:
sh600000 sz000001
```

每例断言：
```text
requested_key == raw_key == Quote.code
```

生产合同：
```text
stocks route -> bare 6 digit
index route  -> prefixed symbol
```

混合 parser helper 必须显式 per-request role。

Mutation tooth：
- raw/Quote classifier 强制 name-blind
- suite MUST fail
- request 侧退回“显式 prefix 即 index”
- explicit stock MUST fail

---

## WP03 — SourceManager Route Isolation Policy【新 P1 candidate】

不要先改代码，先写策略 RED。

fixture：
```text
threshold=2
primary:
  stocks fail
  index healthy
backup:
  stocks healthy
  index healthy
```

执行 stocks 两次后调用 index。

推荐合同：
```text
stocks preferred = backup
index preferred = primary
```

镜像：
index failure 不改变 stocks preferred。

若采用 route-specific：
- `_preferred_by_route`
- threshold 只更新该 route
- 只 reset `_fails[route]`
- `serving_routes` 与 `preferred_routes` 分开
- legacy/no-route 调用保持兼容

若产品决定 global promotion：
- 必须写反向测试固定
- 修改“各 route 各有切源时机”的 EngineState / CHANGELOG 口径
- 明确跨 route epoch 切换是预期行为

---

## WP04 — legacy prefixed axis

RED：
```text
request sh600000
legacy Quote.code = 600000
raw_presence_known = False
```

不得：
- 正常 Quote 被过滤掉
- 生成 phantom exact missing

必须明确 legacy identity normalization。

---

## WP05 — Eastmoney unknown-total pagination

正式落此前 scratch RED/GREEN：
- raw/member nonempty + quotes empty 不能等于翻页结束
- 已请求 page 先进入 ledger
- pages_requested/pages_ok 对齐 fetch 调用
- rollback tooth

---

## WP06 — Engine WP03

只在 WP01–05 绿后：
- stock/index 使用 detailed
- normal/empty 共用 attribution helper
- `unattributed_requested`
- exact missing / quality / future / ooo 互斥
- raw coverage 与 usable coverage 分轴
- 新生产代码禁止 generic `coverage`

---

## WP07 — Membership

5923 transport / 5553 usable：
active membership 应来自 transport membership，不造 fake Quote。

## WP08 — exact soft-empty

仅：
```text
R nonempty
raw_presence_known=True
P empty
```
触发 source soft failure。

## WP09 — Timezone / ObservationInterval / provenance

继续关闭旧 P1。

## WP10 — Research

数据事实链稳定后：
manifest -> labels/splits -> factor diagnostics -> Logistic/HGB/MLP -> SSE -> TCN。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

call_detailed_type_red.log
call_detailed_type_green.log
call_detailed_type_rollback.log

identity_matrix_red.log
identity_matrix_green.log
identity_mutation_red.log
identity_rollback.log

route_isolation_red.log
route_isolation_green.log
route_isolation_rollback.log

legacy_axis_red.log
legacy_axis_green.log
legacy_axis_rollback.log

pagination_red.log
pagination_green.log
pagination_rollback.log

source_manager_cases.json
identity_cases.json
route_isolation_cases.json
legacy_cases.json
pagination_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须 `not_run/blocked + 原因`。
