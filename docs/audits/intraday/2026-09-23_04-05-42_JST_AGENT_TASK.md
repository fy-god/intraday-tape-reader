# 本地 Agent 任务书 — 2026-09-23 04:05:42 JST

## 基线

- repo：`fy-god/intraday-tape-reader`
- 固定产品 SHA：`bf0b83bf212f169c720ca8a1c5108405c18a9712`
- 开工前重新读取实时 main；`bf0b83bf212f169c720ca8a1c5108405c18a9712..main` 若仍仅 docs，则产品基线不变。
- 不真实通知、不交易、不自动 push、不改 Actions、不持续抓取、不付费。

## WP01 — Membership RED / GREEN / rollback【P1，最高优先】

复用现有：`tests/test_eastmoney_truncation.py::_suspend_every_fetcher(every=16,new_codes=NEW_CODES)`。

### RED

固定已知：

```text
base = 5913
new = 10
expected/raw unique = 5923
dropped invalid old = 370
usable Quote = 5553
transport_complete = True
```

当前 `EastmoneySource.universe()` 返回 usable Quote；Engine `_record_universe()` 从 Quote 抽 code，所以 active membership 预计只有 5553。

新增断言：

```text
len(eng._codes) == 5923
all raw member codes in eng._codes
all 10 new codes in eng._codes
all 370 invalid-price old member codes remain in eng._codes
```

修前必须行为 RED，不要靠 ImportError/缺字段红。

### GREEN

新增 UniverseMember 合同；禁止伪造 `price=0` Quote。

### 下一轮请求验收

任选一个 invalid universe-row member：必须出现在下一次 stock snapshots requested codes。

### rollback tooth

回退产品实现，RED 必须重新失败。

---

## WP02 — UniverseMember schema / source contract

建议：

```text
UniverseMember:
code
name
board
list_date
source
```

Eastmoney raw row：code/name/f26 与价格是否有效无关；ClistPage 增 members；source 保存 ordered dedup members；`universe_members()` 暴露。

Sina 可从其列表行/现有 Quote 投影，保持“停牌仍保留 code”的既有语义。

Legacy source 没有 membership API 时允许 quote projection，但必须标：`membership_provenance=legacy_quote_projection`。

---

## WP03 — Engine migration

- complete candidate size 用 member count；
- partial candidate size 也用 member count；
- `_codes_raw` 用 member.code；
- `state.universe` 仍保 usable Quote，不造假 Quote；
- `filters.list_dates` 从 member metadata 补；
- UniverseTruth 增 `transport_member_count / membership_provenance / active_scan_codes / usable_quotes`。

---

## WP04 — Eastmoney unknown-total pagination【P2，便宜红测】

fixture：

```text
total unavailable
page1: members100 / quotes100
page2: members100 / quotes0
page3: members100 / quotes100
```

当前 `if not page.quotes: break` 会停在 page2。修后应按 members/raw transport end 判定，page3 必须被请求。

保持：numeric total 不可得时 `transport_complete` 仍不能凭空变 True。

---

## WP05 — Membership reconcile

生成 `membership_reconcile.json`，至少包含：

```text
timestamp/evidence_type
expected_total
raw_members
usable_quotes
active_members
dropped_invalid
duplicate_codes
shortfall
membership_provenance
new_members
retained_old_members
missing_old_members
```

---

## WP06 — SourceManager soft-empty【membership 独立提交完成后】

nonempty snapshots：primary `[]` / backup valid -> backup。

---

## WP07 — Timezone

ZoneInfo + `_now_fn`；same epoch across host zones -> same SessionPhase。

---

## WP08 — ObservationInterval + provenance

先完成 fresh-flat interval；随后 per-code source/source_epoch/capability。

---

## WP09 — Research data / factor diagnostics

有真实授权多日数据才运行 diagnostics；无数据继续 blocked，不启动持续抓取。

---

## WP10 — models / SSE / shadow

先 tabular，TCN 最后；SSE committed→sent→received→applied/cursor/gap 继续关闭。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

membership_red.log
membership_green.log
membership_rollback.log
unknown_total_pagination_red.log
unknown_total_pagination_green.log
unknown_total_pagination_rollback.log

membership_reconcile.json
membership_cases.json
universe_truth_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

所有未运行项明确 `not_run/blocked + 原因`。

## 完成失败条件

- active membership 仍 == usable Quote 数 -> FAIL
- invalid universe row code 没进入下一轮 snapshot requested set -> FAIL
- 为保存 membership 伪造零价 Quote -> FAIL
- list_date 因 Quote 不可用被静默丢掉 -> FAIL
- rollback 后 RED 不重新变红 -> 测试无探测力，不能算完成