# 本地 Agent 任务书 — 2026-09-20 20:09:14 JST

## 基线

- 仓库：`fy-god/intraday-tape-reader`
- 必须从实时默认分支重新确认 HEAD。
- 本任务书生成时被审产品：`100e06aeda7dc5605ac20b3f8511ec34ea40e9b6`。
- 不把后续 audit-doc commit 当产品修复。
- 只在你自己的隔离 checkout 修改产品；云端审计者不会替你声称已运行。

## 总目标

本轮不要继续堆新模型。先把**股票池 transport 完整性 + route-specific source epoch + time policy/Observation Ledger**做成真实产品闭环，并用红测/绿测/回退验牙证明。

### 禁止
- 不改 Actions / 部署 / 通知 / 交易；
- 不开启真实通知或真实交易；
- 不用最终测试错例回训；
- 不因为 synthetic 指标好就上线 specialist；
- 不用 PowerShell `Set-Content` 做回退验牙；
- 不把 `role=unknown` 的 provider ts 无条件当权威 freshness 时间。

---

## WP01 — Eastmoney transport / usable 双账【P1，第一优先，ready】

### 现状
`EastmoneySource.universe()` 用 `len(out)`（post-parse usable Quote）和 API `total`（transport universe）比较，正常不可用/停牌行也令 `complete=False`。18:22 冻结树已经真实证明：这会让现有全市场池拒绝刷新，新上市代码可以 0/10 进入。

### 实现
建议新增/调整：
- `raw_rows`
- `raw_unique_codes`
- `usable_quotes`
- `dropped_invalid`
- `duplicate_codes`
- `transport_expected_total`
- `transport_complete`
- `usable_coverage`

兼容：
```python
meta["complete"] = meta["transport_complete"]
```

不要再以 `usable_quotes == total` 作为 transport 完整门槛。

### 红测
1. raw code 全覆盖、6% 行 parse 不可用 → transport complete **True**；
2. 每页真实缩水 → False；
3. raw 行数够但 unique code 不足 → False；
4. 现有池 + 10 新代码 + 正常无效行 → 10 个新代码可进入；
5. 回退旧 `shortfall == 0` → 1/4 必须重新红。

### 回传
`universe_transport_reconcile.json`、red/green/rollback logs、diff hash。

---

## WP02 — stock/index route-specific epoch【P1，ready】

### 必须先写红测
- stocks 不切、index 切源：index 新 source 首包必须接纳；
- stocks 切源、index 不切：不能清 index watermark；
- index-only 模式 source switch 也必须开 index epoch；
- A→B→A 各 route 都形成新 epoch；
- 同一 route 同一 serving source 重复轮询幂等。

### 建议接口
```python
begin_source_epoch(route: str, source_tag: str)
accepted_watermark[(route, code)]
```
或等价 route 子表。不要继续一个全局 `source_epoch` 清所有 code。

---

## WP03 — TIME-ROLE-003-R1 / policy contract【依赖 WP02】

当前 `first_seen` 是全局代码集合，换 source epoch 后同码 3 天前首包绕过 stale gate。

更根本地，合同三源都是：
```text
role=unknown
freshness_allowed=false
```
因此 provider ts 不应直接执行 hard freshness reject。

### 要求
- `source_time_contract.json` 驱动/约束 policy；
- raw provider ts 永久留痕；
- `freshness_allowed=false` 时只做 age 诊断，不做 hard stale reject；
- 如果以后某 source 获得权威语义，stale first-packet 应按 route/source-epoch，而不是全局 `first_seen`；
- 写出 `time_policy_matrix.json`。

---

## WP04 — Observation Ledger stale/route 互斥桶【依赖 WP02/03】

必须新增真实：
```text
provider_stale_rejected
provider_stale_rejected_codes
```

删除“stale alias future”的语义错误；若兼容旧 key，必须显式 deprecated，不能再让两者同值。

每个 provider object 必须且只能进入一个终态：
```text
admitted
explicit_unavailable
future
stale
out_of_order
quality
```

stock/index 按 route 分开 requested/returned/missing，并带 round_id/observed_at。

---

## WP05 — live_session observation 聚合【可并行，ready】

`make_round_sample()` 已采：
requested/returned/admitted/coverage/rejections/capability

但 `summarize_rounds()` 当前全部丢掉。

新增至少：
- coverage p05/p50/min；
- requested/returned/admitted totals；
- missing/quality/future/stale/ooo totals；
- source mix；
- capability-unavailable totals；
- 最差 N 轮摘要。

`evaluate_health()` 增加可配置阈值，不要求绝对 100%，但必须能让 93% soft-partial 或整类规则不可评估显式变黄/红。

---

## WP06 — index current-only【依赖 WP02/04】

不要清指数 history。

新增本轮 `current_indices=idx_admitted`，让 `SpiritIndexRule` 只消费 current view；`ctx.state.quotes` 仅可作为历史/latest cache，不能当 current。

集成红测：
- R1 指数正常；
- R2 index route 整体失败；
- R2 规则 candidates 必须为空；
- history 保留；
- index source switch 与 route epoch 组合测试。

---

## WP07 — soft-partial targeted fallback【依赖 WP04/06】

先 per-code provenance/capability，再混源补洞。

禁止：
```text
Round capabilities = Tencent
但其中部分 code 实际来自 Sina
```

需要：
```text
code -> source
code -> source_epoch
code -> capabilities
```

再只对 `unknown_missing` 做有界 fallback；`explicit_unavailable` 不无限补。

---

## WP08 — ObservationInterval【依赖 WP04】

解决：
```text
同价同量但新鲜 observation
```
被 change-only history 丢失的问题。

目标结构：
```text
first_observed_at
last_observed_at
price
cum_volume
source_epoch
```

新鲜同值只延长 last_observed_at；真实断线不延长；epoch 切换必须切段。

---

## WP09 — SSE durable delivery / bounded state

完成：
```text
committed -> sent -> received -> applied
```
cursor/gap 语义。

慢客户端队列溢出不得静默 drop-oldest 而客户端毫无感知。series 主体 dict 与索引队列必须一起真实驱逐。

---

## WP10 — 真实数据模型认证

只有真实连续数据 manifest 可用后才跑：

```text
HGB base
HGB global capability-mask
HGB provider-specialist
```

固定已有主参数，不重新大搜。

主指标改成**事件簇/固定时间块**，逐 tick 仅诊断。至少：
- 3 time-forward folds；
- unseen stocks；
- provider split；
- source-switch slice；
- event-cluster bootstrap；
- coverage 与 PR 同时报告。

淘汰 specialist：
- 事件级 PR 增量 < 0.005，或
- 主要 folds/provider 方向不稳定，或
- p99/运维复杂度明显增加却无对应收益。

TCN/Transformer 继续暂停，直到真实 tabular 基线与 Observation Contract 都稳定。

---

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

red.log
green.log
rollback-tooth.log

universe_transport_reconcile.json
source_epoch_route_cases.json
time_policy_matrix.json
observation_ledger_cases.json
index_current_reconcile.json
provider_coverage.csv

pytest full log
tools/check_*.py logs
dashboard check log

RUN_MANIFEST.json
NEXT_STEPS.md
```

每个未完成项标 `blocked/not_run + 原因`，不要填 0，不要把历史数字冒充本轮执行。
