# 本地 Agent 任务书 — 2026-09-24 08:10:43 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`7a4f549a15e78fe87db7de00796eff71b707ae9b`
- audit-start main：`fd403c377e835bb7816045177f2e239910222cba`
- 开工前重新 fetch；docs-only commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取、付费服务。

## WP01 — Per-route Observation Schema【第一优先】

新增/扩展：
- `src/arad/capabilities.py`
- tests：待新增 `test_route_observation_contract.py` 或等价

`RouteObservation`：
```text
route
source
capabilities
provenance
raw_presence_known
requested
raw_returned
usable_returned
admitted
unknown_missing
rejected_quality
future_rejected
out_of_order_rejected
unattributed_requested
raw_return_coverage
usable_coverage
```

`RoundObservationSet.routes`：
- stocks
- index

旧 aggregate 字段保留 compatibility。

RED：
```text
stocks = 5563/5563
index = 0/5
aggregate ≈ 99.9102%
index = 0%
```
必须保留 index=0%，不能只见 aggregate。

## WP02 — Production detailed integration

改：
- stock fetch
- index fetch

两条 route 都调用 `call_detailed`。

实际 `SnapshotFetchResult.source` 直接进入 route observation。

Legacy：
`raw_presence_known=False`，不得伪装 exact。

## WP03 — Unified Attribution

normal/empty 只用一个 helper。

Built-in exact：
```text
missing=R-P
quality=P-Q
```

time reject 从 Q 后进入 future/ooo。

Legacy 未解释项：
`unattributed_requested`。

不变量：
每个 requested identity 恰好一个 terminal bucket。

## WP04 — live_session route health

采：
```text
route_observations
stock_source
index_source
index_requested
index_returned
index_raw_return_coverage
index_usable_coverage
```

健康：
- index_requested=0 -> N/A
- index_requested>0 -> 独立 index gate
- 禁止 aggregate coverage 给 index outage 背书

## WP05 — Mixed-source provenance

固定：
```text
stocks=Sina
index=Tencent
```

Round 必须同时保存两条 provenance。
`ctx.capabilities` 保持 stock caps。
`ctx.current_indices` 保持 index current view。

## WP06 — Eastmoney Pagination

正式落已有 RED/GREEN：
raw/member empty stop + page ledger + rollback。

## WP07 — Membership

transport membership 与 current Quote usability 分离。

## WP08 — exact soft-empty

仅 exact raw empty 触发 source failure。

## WP09 — Timezone / ObservationInterval / provenance

继续关闭。

## WP10 — Research

真实数据后：
manifest -> labels/splits -> factor diagnostics -> Logistic/HGB/MLP -> SSE -> TCN。

## 当前明确禁止的一件事

不要在本包顺手把 SourceManager global promotion 改为 route-specific。

现有产品 tests 明确把 global promotion 固定为当前期望。
若产品要改策略，另开独立决策/RED，不夹进 WP07。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

route_observation_red.log
route_observation_green.log
route_observation_rollback.log

mixed_source_red.log
mixed_source_green.log
mixed_source_rollback.log

engine_detailed_red.log
engine_detailed_green.log
engine_detailed_rollback.log

index_health_red.log
index_health_green.log
index_health_rollback.log

pagination_red.log
pagination_green.log
pagination_rollback.log

route_observation_cases.json
mixed_source_cases.json
index_health_cases.json
pagination_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须 `not_run/blocked + 原因`。
