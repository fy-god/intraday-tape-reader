# 本地 Agent 任务书 — 2026-09-23 08:05:58 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- product SHA：`bf0b83bf212f169c720ca8a1c5108405c18a9712`
- 当前 main 在该产品之后只有 docs；开工前重新确认。
- 最新 05:35 独立审计已经真实复现：
  - 5913 -> 5553
  - 370 老成员退出
  - 10/10 新成员进入
  - full suite 1802 passed
  - unknown-total T3 终止页丢弃
- 不真实通知、不交易、不自动 push、不改 Actions、不持续抓取、不租卡。

## WP01 — Membership Truth RED/GREEN/rollback【最高优先】

复用：
`tests/test_eastmoney_truncation.py::_suspend_every_fetcher(every=16,new_codes=NEW_CODES)`。

RED 必须是行为失败：
```text
expected/raw member = 5923
usable Quote = 5553
dropped invalid = 370
transport_complete = True

assert len(eng._codes) == 5923
assert all(raw member code in eng._codes)
```

修前应得到 5553，不允许用新增类 ImportError 充当 RED。

修后：
- 370 invalid-price old member 留在 active scan；
- 10 new member 进入；
- 下一轮 snapshots request 包含一个 invalid-price old member；
- state.universe 不得放 fake zero-price Quote。

rollback：恢复旧实现后主 RED 重新失败。

## WP02 — UniverseMember metadata

新增轻量 schema：
```text
code
name
board
list_date
source
```

Eastmoney raw row 提取：
- `f12 code`
- `f14 name`
- `f26 list_date`
与 `f2/f18` Quote usability 解耦。

专门测试：
raw row `f2/f18='-'` 但 f26 有效；
Member 必须有 list_date，Quote 仍可为 None。

Engine `filters.list_dates` 优先由 member metadata 更新。

注意：ULIST_FIELDS 不含 f26，所以不能指望后续 snapshots 补 exact list_date。

## WP03 — 禁止两个 shortcut

### 控制 1：complete shrink guard
现有 exact fixture：
usable 5553 < prev 5913。
如果拒绝 whole candidate，新10 member 0/10 进入。
加入测试明确禁止此方案成为最终语义。

### 控制 2：union(prev, usable)
构造：
prev=100
true removals=5
new=10
raw members=105
20 个 raw member 当前 Quote 不可用
usable=85

union 结果=110，错误保留5个已移除成员。
目标=105。

只有 transport membership replacement 应 exact。

## WP04 — Source / Engine interface

推荐兼容式：
- `universe()` 保持 usable Quote[]；
- 新增 `universe_members()` 返回 Member[]；
- Engine helper 支持 SourceManager route serving child；
- source 不支持时退化 quote projection，但打：
  `membership_provenance=legacy_quote_projection`。

complete / partial candidate size 都改用 member count。

`state.universe` 仍是 usable Quote map。
`_codes_raw` 才是 Member code list。

## WP05 — unknown-total pagination P2

T3：
```text
page1 raw100 usable100
page2 raw100 usable0
page3 raw100 usable100
page4 true empty
```

当前：
page2 break-before-append；
raw=100；
page3 不请求。

修后：
- page2 raw 必须进账；
- page3 必须请求；
- raw_unique=300；
- usable=200；
- page4 真空仍停止。

不能只把 append 移到 break 前；那只能把 raw 100->200，仍不会请求 page3。

## WP06 — membership reconcile

输出 `membership_reconcile.json`：
```text
expected_total
raw_members
usable_quotes
active_members
membership_provenance
dropped_invalid
duplicate_codes
new_members
removed_members
retained_members
```

## WP07 — SourceManager soft-empty
membership 独立提交完成后再做。

## WP08 — Timezone + ObservationInterval
ZoneInfo / _now_fn；fresh-flat interval。

## WP09 — provenance / research data
membership 与 observation truth 绿后再建真实 manifest。

## WP10 — factor/model/SSE
先 tabular；TCN 最后；SSE durable 继续关闭。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

membership_red.log
membership_green.log
membership_rollback.log
membership_shortcut_controls.log
membership_metadata_red.log
membership_metadata_green.log

unknown_total_pagination_red.log
unknown_total_pagination_green.log
unknown_total_pagination_rollback.log

membership_reconcile.json
membership_cases.json
pagination_cases.json
universe_truth_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须 `not_run/blocked + 原因`。
