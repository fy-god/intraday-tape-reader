# 盘中预警审计 — 2026-09-24 05:57:34 JST 报告附录 A：并发写入者提交的**未推送修复**实测

- `appendix_to`: `docs/audits/intraday/2026-09-24_05-57-34_JST.md`
- `appendix_created`: `2026-09-24 06:02:27 JST`（**同一轮 h=4**；h 边界在报告定稿后跨过，不另起一轮）
- `reviewed_repo`: `D:\ccc\ashare-radar`
- `remote_main_at_appendix`: `1c69e5a49c1b7937fb00f2381f61cad940b2929d`（= 我本轮报告+索引推送后的尖端）
- `evidence_type`: `SOFTWARE_SAMPLE`

---

## A.0 为什么会有这份附录

我推送完报告**之后**才注意到：**同一仓库的本地工作树在我审计期间被另一个写入者改动**，
其本地 HEAD 从我开工时的 `bd711c4a` 变成了 `24a982b9`，且 `LATEST.md` 处于 **`UU`（未合并）** 状态。

这与我刚刚确认的缺陷**直接相关**，所以我按只读方式核了一遍，并把结果**如实附上**——
否则我刚发的报告会说「缺陷仍在」而不提「有人已经在本地修了一半」。

**我绝不把这当作权威**：该提交**未推送**（不是远端 main 的祖先），
且**我未在其工作树内执行任何命令**（见 A.4）。

---

## A.1 该提交的身份（只读读出）

```
commit   24a982b96910759676a4e02e97392a7ca28f5782
author   arad-bot
date     2026-09-24 04:57:42 +0800
subject  fix(sources,engine): WP01/WP02 身份单一 role + WP03 call_detailed 合同校验
parents  ['1d438e503077541781f23da4ac68c18b3e0b1b8a']    ← 正是我本轮审计的尖端
```

- `1d438e50..24a982b9` 共 **1** 个提交 ⇒ 相对远端 main **领先 1**。
- `merge-base --is-ancestor 24a982b9 1d438e50` → **不是**远端祖先 ⇒ **未推送**。
- 改动文件（5 个）：`M src/arad/engine.py`、`M src/arad/sources/tencent.py`、
  **`A tests/test_source_manager_contract.py`**、`M tests/test_sources_tencent.py`、
  `A tests/test_tencent_identity_matrix.py`。

---

## A.2 它**确实修好了** `IT-P2-CALL-DETAILED-TYPE-ERROR-ABORTS-FAILOVER-004`（实测）

**代码改动（我读的 diff）**：`call_detailed` 的 `try` 块从「只包住 `detailed(...)` 一行」
扩到**包住四件事**：①取结果 ②类型/合同验证 ③`source` 绑定 ④`route` 绑定。
新增 `:657` `if not isinstance(out, SnapshotFetchResult): raise TypeError(...)`，
并把 `out._replace(...)`（`:673`）移进 `try`；`except` 在 `:674` 继续 failover。
注释 `:628-640` 自述旧行为是「错类型源会让 `AttributeError` 冒出整个 failover 循环、
备用源一次都不会被调用（实测 `backup.calls == 0`）」——**与我 h=0 轮的三臂实验结果一致**。

**实测（我把该提交 `git archive` 到仓库外副本后执行）**：
错类型源 + 健康备用：

```json
{ "raised": null,
  "served_by": "good",
  "wrongtype_calls": 1,
  "good_calls": 1,
  "failover_happened": true }
```

⇒ **修前**（我 h=0 轮实测）：`AttributeError: 'dict' object has no attribute '_replace'`，`backup.calls = 0`；
**修后**：不抛异常、**failover 到备用源**。⇒ 该缺陷**在此本地提交中已修复**（**未推送**）。

**测试真数（同一净副本内）**：

| 套件 | 结果 |
|---|---|
| 全量 `python -m pytest . -p no:cacheprovider` | **1919 passed in 95.96s，exit 0** |
| 仅 `test_source_manager_contract.py` + `test_source_manager_detailed.py` + `test_engine.py` | **86 passed in 11.15s，exit 0** |

对照我审计的尖端 `1d438e50`：**1891 passed**。⇒ 新增 **28** 项（含新文件
`tests/test_source_manager_contract.py` 的 7 项）。

---

## A.3 它**没有**触碰我本轮确认的全局 idx 缺陷（实测）

在**该提交**的 `src/arad/engine.py` 上重新计数：

| 探针 | 在 `24a982b9` 上 | 结论 |
|---|---|---|
| `_preferred_by_route` | **0 命中** | **仍未引入逐路由首选源** |
| `preferred_routes` | **0 命中** | 同上 |
| `self.idx` | **11 命中**（`:514`/`:521`/`:539`/`:561`/`:572`/`:579`/`:622`/`:623`/`:679`/`:686`…） | **仍是唯一全局首选源** |
| `_fails.clear()` | **3 命中**（`:540`/`:580`/`:687`） | **仍清整张表**（`:687` 是原 `:660` 位移后的同一处） |

⇒ **`IT-P1-SOURCE-MANAGER-GLOBAL-IDX-BREAKS-ROUTE-ISOLATION-008` 在该本地提交中依然存在。**
它与本轮修的 `call_detailed` 是**两个不同缺陷**：本次改动把 **route 绑定/校验**收进了 `try`，
但**没有**把「首选源」按 route 分开。**不得把这次修复读成 route isolation 已解决。**

---

## A.4 我做了什么、没做什么（安全声明）

- **我未在 `D:\ccc\ashare-radar` 工作树内运行任何命令**（它处于 `UU` 未合并状态）。
  该提交的一切检验都在 `git archive` 解出的**仓库外副本**内完成。
- 我**未**执行 `checkout`/`reset`/`stash`/`clean`/`merge`/`pull`/`add`/`commit`，
  **未**对该树做任何写操作。
- 我推送的两份文档走的是**独立全新 clone**，与该写入者的进行中合并**完全无关**。
- 该树在我发布前后的 `git status --porcelain`：
  **发布前 6 条 = 发布后 6 条，逐条一致**；HEAD 前后一致。
- 我**没有**把该未推送提交的内容写进任何远端文件——本附录只描述它，不复制它。

**⚠ 需要人工决策的一点**：该提交与我的报告**共享同一个父提交**，且**未推送**。
若它随后被推送，远端 main 会与我推送的两个 docs 提交**分叉**，需要一次合并。
我**不代任何一方决定**，只如实登记。

---

## A.5 本轮结论的修正

- 我主报告中「`IT-P2-CALL-DETAILED-TYPE-ERROR-ABORTS-FAILOVER-004` 已确认（我 h=0 轮的三臂实验）」
  **依然成立**——那是对 `1d438e50`（当时也是当前远端尖端）的确认。
- 但应补一句：**该缺陷的修复已存在于本地未推送提交 `24a982b9` 中，且我实测它有效。**
  类别从「已确认错误」调整为：**已确认错误（针对远端尖端）／本地已有有效的未推送修复**。
- `IT-P1-SOURCE-MANAGER-GLOBAL-IDX-BREAKS-ROUTE-ISOLATION-008`：**状态不变，仍未修**，
  且**该本地提交也没有修它**。
- 我主报告 §9「程序修复：本轮 0」**仍然正确**——那是**我**做的修复数（0）。
  必须与**他人**在本地做的修复（1 个提交，未推送）**分开计**，不得混算。
