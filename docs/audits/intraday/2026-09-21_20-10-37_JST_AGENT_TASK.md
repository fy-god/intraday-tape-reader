# 本地 Agent 任务书 — 2026-09-21 20:10:37 JST

## 基准与目标

- 仓库：`fy-god/intraday-tape-reader`
- 先读取实时 `origin/main`；本任务生成时产品基线为 `6997487f0ec5d92f81299edbb8c68845e9e056a8`。
- 云端本轮没有完整 checkout，不能把本报告中的机制探针替代真实 repo 红/绿测试。
- 第一目标：**Universe Truth Contract v1**，让“没报警”第一次拥有可信的市场分母。
- 第二目标：交付账本坏掉时必须 fail/inconsistent，不能静默假绿。
- 不扩张 TCN/Transformer；没有真实标签时不重复 synthetic 拟合。

## WP01 Universe Truth Contract v1【P1，最高优先】

### 现状
- `EastmoneySource.universe_info()` 已有 `transport_expected_total/raw_unique_codes/usable_quotes/transport_complete/usable_coverage`。
- `Engine._universe_meta` 已保存这些事实，但 Store/status/live-session 不导出。
- `RoundObservationSet.coverage = returned/requested`，其中 requested 只是当前 `_codes`；partial `_codes` 可导致 round coverage=100%。
- `Store.status()["universe"] = len(state.quotes)`，它是 latest cache，不是 active `_codes`。

### 实现
新增有界 `UniverseTruth`（名字可等价）：
`refresh_id/observed_at/source/mode/expected_total/raw_unique_codes/usable_quotes/active_scan_codes/transport_complete/transport_coverage/usable_coverage/active_coverage/reason/candidate_was_applied`。

Store/status/once/soak 必须读同一份对象。旧 `status["universe"]` 一版兼容，但语义改为 active_scan_codes，并新增 `latest_quote_cache_size`。

### 红测
1. expected=5913, active=4100, returned=4100：round coverage=1.0，但 active coverage≈0.693，health 必须 degraded/fail，不能 green。
2. expected unknown：active coverage=None/not_measured，不得认证 full。
3. partial candidate 被更大旧 full pool 拒绝：同时暴露 candidate partial 与 active retained full。
4. index quote 进入 `state.quotes`：不能改变 active stock universe。
5. active codes 缩小、latest cache 有旧 code：status active universe 必须跟 `_codes`，不是 cache。

### 起始阈值
`warn <0.95`, `fail <0.90`，先作为可配置运维起点；报告里注明不是统计认证门槛。

## WP02 Delivery accounting false-green【P1，ready】

`engine.py` 的 `committed_alerts_total += len(fresh)` 失败不得裸吞。
新增计数/状态：`delivery_accounting_errors`（或等价）并 `log.exception`。
全局 reconcile 必须检查 `0 <= named <= total`，禁止 `max(total-named,0)` 把负数夹成 PASS。
输出 `accounting_status=ok/not_measured/inconsistent`。

红测：
- denominator update throws -> inconsistent；
- total=1, named=3 -> inconsistent；
- numerator write fails -> inconsistent/observable；
- 正常 66/66 -> ok。

## WP03 Delivery last-round/session + soak consumer【依赖 WP02】

- `signal_delivery` / `delivery_accounting` 加入 live_session 采样白名单。
- 区分 `delivery_last_round` 与 `delivery_session`。
- session 累计必须能重现 361 轮的累计值；末轮 0 alert 不得让运营面看起来“没有测量”。
- 实际调用全局 delivery invariants/reconcile，不要只在测试单实例调用。
- 删除/改名把 `evaluability_committed_total` 当真实交付的旧口径。

## WP04 Ring buffer acceptance + max_alerts wiring【ready】

- `tests/test_replay_capability_ledger.py` 累计 committed 对账改用 `store.alerts_total()`，不能对 `recent_alerts(1000)`。
- 加 >300 条 replay 测试，旧实现必须假红，新实现通过。
- `web.max_alerts` 要么真正传进 AlertStore，要么删除配置；不能静默无效。

## WP05 event_id + Typed NotificationResult

为 committed Alert 生成稳定 `event_id`；通知返回 typed result：
`sent/skipped_disabled/skipped_severity/skipped_dry_run/failed/legacy_true_unknown`。
Engine 记录 `event_id × channel`。

## WP06 per-code provenance/capability
建立 `(route,code)->source/source_epoch/advertised_capability/observed_fields`。

## WP07 targeted fallback
只补 `unknown_missing`；不无限补 explicit_unavailable/停牌/quality reject。

## WP08 ObservationInterval
同价同量新鲜包延长 last_observed_at；断流不延长；epoch 切段。

## WP09 SSE durable delivery
cursor/gap，慢客户端 drop-oldest 必须让 client 知道 gap；Store/series 真有界。

## WP10 真实标签与模型
只有 WP01/WP03/WP05 与真实多日数据齐全后，才做 committed-event T+5/T+30 matched-control 与固定简单模型。逐 tick 不是主统计单元。

## 必须回传

`git rev-parse HEAD`、`git status --porcelain`、完整 diff + SHA256；
WP01/WP02/WP03/WP04 的 red/green/rollback logs；
`universe_truth_reconcile.json`、`universe_health_cases.json`、
`delivery_accounting_cases.json`、`delivery_session_reconcile.json`、
`ringbuffer_acceptance_reconcile.json`、`notification_delivery_reconcile.json`；
full pytest、全部 check 工具、dashboard render；
`RUN_MANIFEST.json`、`NEXT_STEPS.md`。
