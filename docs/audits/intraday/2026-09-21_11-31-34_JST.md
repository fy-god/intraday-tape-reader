# 盘中预警审计 — 2026-09-21 11:31:34 JST

## 0. 元数据与证据边界

- `audit_time_jst`: `2026-09-21T11:31:34+09:00`
- `local_cst_time`: `2026-09-21 10:31:34`
- `reviewed_repo`: `fy-god/intraday-tape-reader`（工作树 `D:\ccc\ashare-radar`）
- 本轮开始本地 HEAD：`3615eed2c927ab56aa12866ac678a3f2107de7e6`
- 远端 `origin/main`：`ce989ca18e37115dc15c220bfcf16397c9b212aa`（tree `835540c5ae7d01d244790d6fedf036efec2c8c1c`）
- `reviewed_source_sha`：`ce989ca18e37115dc15c220bfcf16397c9b212aa`
- `reviewed_product_sha`：**`4e73ded6b1fe12e3d4eaa15fb8971098124bebe9`**（最新产品提交；其后的 `3615eed`/`1838bee`/`f7408c5`/`ce989ca` 四个提交全为 `docs/`）
- `previous_report`：`docs/audits/intraday/2026-09-21_03-38-00_JST.md`（blob `918ff02a9d64cd75c80ec656159a14dbd5c48ea8`）
- 开放 PR：0
- **隔离冻结工作树**：`D:\ccc\_sched\work\asr-h04`（detached @ `ce989ca`），全程只在此树读写与运行
- **本轮工作树含他人未提交改动**（8 个文件：`src/arad/{capabilities,engine,models}.py`、`src/arad/rules/{spirit_order,volume_burst}.py`、`tests/test_live_session_observation.py`、`tools/live_session.py`、`?? tests/test_alert_delivery_stages.py`）→ **一律未读未改未提交**，所有审计在冻结树进行
- `evidence_type`：`REAL_MARKET`（真实测试套件 + 真实代码路径探针）；本轮**无新增实股收益结果**

---

## 1. 本轮做了什么（先结论）

本轮上游有 **两个真实产品提交** 需要审：

| 提交 | 时间 | 内容 |
|---|---|---|
| `22a51a2` | 03:36:58 | 主板 ST 涨跌幅按交易日 5%→10% + 守护健康归属 |
| `4e73ded` | 03:55:32 | 状态文件按端口分文件 + `--replay` 常驻 |

我**先看它们声称修好了什么，再构造反例**，得到 **3 条新发现**，其中 **2 条是「声称修复但仍存在可达缺陷」**：

### 结论表

| ID | 级别 | 分类 | 一句话 |
|---|---|---|---|
| `IT-P1-ST-REPLAY-BYPASS-001` | **P1** | **已确认错误** | ST 日期感知**修好了 `Quote` 路径，但整条 replay 路径绕过它**：回放行情把 `limit_up` 预置为日期无关的 10%，`Quote.limit_up_price`（`models.py:349-350`）直接返回该值，`trade_date` 永不参与 → pre-2026-07-06 回放里真实 5% 封板**不被识别** |
| `IT-P1-UNKNOWN-DATE-FAILOPEN-001` | **P1** | **已确认错误**（§2.5，**根因**） | `models.py:64-72 _as_date` 把「类型不认识」与「确实是 None」折叠成同一个 `None`，而 `None` 被解释为「用现行制度」→ **任何日期丢失路径都静默套用今天的规则**，不抛错不告警不计数。`str`/`int` 日期实测落此分支；真实解析器（eastmoney/sina/tencent `_ts_of`）**确实会返回 `None`** |
| `IT-P2-LEGACY-STATE-SLOT-001` | P2 | **待验证风险**（当前不可达） | 按端口分文件**只做了一半**：`_write_state` 仍每次覆写旧单槽 `arad-daemon.json`（`run_daemon.py:136-138`）。**已如实降级**：`--port` 有 `default=8899`，`_read_state(None)` 分支当前不可达，且 `:105` 有 port 匹配保护（实测无串号） |
| `IT-P2-REPLAY-DOC-COVERAGE-001` | P2 | **已确认错误** | `replay.py:178` 文档声称默认股票池「板块覆盖…与 ST」，实测 `default_universe(30)` 中 **名字含 ST 的股票 = 0 只** |

> **关键**：`IT-P1-ST-REPLAY-BYPASS-001` 与「`ts` 为 `str`/`int` 时失效」**不是两个缺陷**，
> 而是 `IT-P1-UNKNOWN-DATE-FAILOPEN-001` 这一个根因的两种可达表现（§2.5 有完整论证）。
> 修复应针对根因，而不是分别打补丁。
>
> **讽刺之处（本轮最值得写下来的一点）**：修复自己的 docstring（`models.py:334-336`）写的动机正是
> 「否则**历史回放**会套用今天的制度（IT-P1-MARKET-RULE-20260706-001）」
> —— 而**恰恰是历史回放**这条路径整条绕过了这个修复。

外加 **1 条来自上游云端报告的独立复核**（其 3 项声明全部为真，见 §4）与 **1 条回归更正**（§5）。

---

## 2. `IT-P1-ST-REPLAY-BYPASS-001`（**已确认错误**，P1）

### 2.1 修复本身在 `Quote` 路径上是**正确**的

`src/arad/models.py`：

```python
L51  ST_LIMIT_RATE_LEGACY = 0.05          # 2026-07-06 之前
L52  ST_LIMIT_RATE_CURRENT = 0.10         # 2026-07-06 起
L53  ST_LIMIT_RATE_CHANGED_ON = date(2026, 7, 6)

L75  def st_limit_rate_on(when: date | datetime | None) -> float:
L81      d = _as_date(when)
L82      if d is not None and d < ST_LIMIT_RATE_CHANGED_ON:
L83          return ST_LIMIT_RATE_LEGACY
L84      return ST_LIMIT_RATE_CURRENT

L234      if b in (Board.MAIN, Board.OTHER) and ("ST" in (name or "").upper()):
L235          return st_limit_rate_on(when)
```

我实测边界（本机真实调用）：

```text
2026-07-03 / 07-04 / 07-05 -> 0.05   version cn-2026-07-05
2026-07-06                 -> 0.10   version cn-2026-07-06   (边界当日取新制，正确)
GEM/STAR 的 ST 仍为 0.20；BJ 仍为 0.30                     (纯板块口径，正确)
```

**这部分是真修好的**，`test_session_boards.py::test_st_limit_rate_is_trade_date_aware`
（10 参数）与 `::test_st_quote_uses_its_own_trade_date_not_runtime_clock` 都真实覆盖了它。

### 2.2 但 replay 路径**结构性地**绕过它

`src/arad/replay.py`：

```python
L152  def limit_rate(self) -> float:
L160      return limit_rate_of(self.code, self.name)      # ← 没有 when，永远取现行 10%

L249  rate = stock.limit_rate()
L250  limit_up = round(stock.start_price * (1 + rate), 2)
L251  limit_down = round(stock.start_price * (1 - rate), 2)

L361      limit_up=limit_up, limit_down=limit_down,
L362      ts=ts, seq=i,
```

`src/arad/models.py`：

```python
L349  if self.limit_up and self.limit_up > 0:
L350      return self.limit_up                              # ← 直接返回预置值
L353  return round(self.prev_close
L354               * (1 + limit_rate_of(self.code, self.name, self.trade_date)), 2)
```

**机制**：`L349-350` 只要 `limit_up > 0` 就**提前返回**，`L354` 那行**日期感知的重算永远不会被执行**。
而 replay 在 `L361` **总是**把算好的 `limit_up` 塞进 `Quote`。

### 2.3 反例（本机真实执行，端到端）

同一只主板 ST 股、同一 `start_price=10.00`，只因回放日不同：

```text
--- 回放日 = 2025-03-10（pre-2026-07-06，真板率 5%）---
    Quote.ts              = 2025-03-10 09:30:00
    Quote.trade_date      = 2025-03-10
    Quote.limit_up        = 11.0    ← replay 预置
    Quote.limit_up_price  = 11.0    ← 规则真正消费的值
    正确的 limit_up       = 10.5
    与当日真实制度一致？  False

--- 回放日 = 2026-09-16（post，真板率 10%）---
    Quote.limit_up_price  = 11.0
    正确的 limit_up       = 11.0
    与当日真实制度一致？  True
```

**规则级后果**（`limit_board` 真实消费该值）：

```python
limit_board.py:115   up = self._limit_up_price(q)
limit_board.py:155   def _limit_up_price(q): return float(q.limit_up_price)
```

实测：

```text
Quote(price=10.50, prev_close=10.00) 于 2025-03-10
    当日真实 5% 涨停价 = 10.50
    Quote.limit_up_price = 11.0     ← 10% 带宽，错
    is_at_limit_up()     = False    ← 应为 True
    距（错误的）上限仍有 -4.55%
```

即：**在 2026-07-06 之前的回放里，一只真实封在 5% 涨停价上的主板 ST 股不会被识别为「贴板」**。

### 2.4 影响面（如实说明边界）

- **实盘当日不受影响**：今天是 2026-09-21，`CURRENT == 0.10`，而 `when=None` 也返回 `0.10`，两者一致。
- **仅三条件同时成立时出错**：① 历史回放；② 走 `replay.py` 预置 `limit_up`；③ 回放日在 2026-07-06 之前。
- **未计数**：我没有真实多日连续 ST 语料，**未统计**受影响的真实 ST 股票数/告警条数 → 该数字标 `未计数`，不编造。
- **`default_universe()` 当前不含任何 ST 股票**（见 §3.3），所以**默认参数下这条路径今天打不到**；
  但它对**用户传入自定义 `ScriptedStock`** 立即成立，且这正是该修复 commit 自己写进 docstring
  要求保持 5% 的场景（`models.py:45-47`）。

### 2.5 根因统一（本轮追加）：两个表现，**同一个根因** —— `_as_date` 的静默 fail-open

上面 §2.2 的反例（replay 不传日期）与我先前记下的「`ts` 为 `str`/`int` 时失效」**不是两个缺陷**，
而是**同一个根因的两种可达表现**。根因在 `models.py:64-72`：

```python
L64  def _as_date(when: date | datetime | None) -> date | None:
L66      if when is None:
L67          return None                      # ← 「未知日期」
L68      if isinstance(when, datetime):
L69          return when.date()
L70      if isinstance(when, date):
L71          return when
L72      return None                          # ← 「给了个我不认识的类型」也当成「未知日期」
```

而「未知日期」在 `st_limit_rate_on` / `market_rule_version` 里被解释为
**「就用现行制度」**（`L59`/`L82` 的 `if d is not None and d < CHANGED_ON`）：

| 输入 | `_as_date` | `st_limit_rate_on` | `market_rule_version` | 判定 |
|---|---|---|---|---|
| `date(2025,3,10)` | `2025-03-10` | **0.0500** | `cn-2026-07-05` | 正确 |
| `datetime(2025,3,10)` | `2025-03-10` | **0.0500** | `cn-2026-07-05` | 正确 |
| `"2025-03-10"`（str） | `None` | 0.1000 | `cn-2026-07-06` | **静默 fail-open** |
| `20250310`（int） | `None` | 0.1000 | `cn-2026-07-06` | **静默 fail-open** |
| `None` | `None` | 0.1000 | `cn-2026-07-06` | 见下 |

通过真实 `Quote`：

```text
ts=datetime(2025-03-10)  trade_date=2025-03-10  limit_up_price=10.50  OK
ts="2025-03-10"          trade_date=None        limit_up_price=11.00  *** 错（10% 带宽）
ts=20250310              trade_date=None        limit_up_price=11.00  *** 错
ts=None                  trade_date=None        limit_up_price=11.00  *** 错
```

**「`None` 就用现行制度」这一条本身不能算 bug** —— 在实盘当日它恰好正确。
真正的缺陷是**设计上无法区分「已知是今天」与「日期未知」**：
`_as_date` 把「类型不认识」和「确实是 None」折叠成同一个 `None`，
于是**任何**日期丢失路径都自动落到「用今天的制度」这个**静默**的分支，不抛错、不告警、不计数。

**两条可达路径**（本轮均已实测）：
1. **replay 路径**：`replay.py:160` 干脆不传日期 → `when=None` → 现行制度（§2.2，端到端反例）。
2. **实盘/回填路径**：`ts` 缺失或不可解析时，真实解析器**确实会返回 `None`**
   —— `sources/eastmoney.py:143 _ts_of`（`_num` 为 None/≤0，或 `OSError/OverflowError/ValueError` 时返回 `None`）、
   `sources/sina.py:158 _ts_of`（`len(fields) <= 31`、日期/时间为空、不可解析时返回 `None`）、
   `sources/tencent.py:397 _parse_ts(...) if len(fields) > I_TIMESTAMP else None`；
   `server/web.py:291 coerce_ts` 在空/不可解析时返回 `None`，且 `:303-305` 对纯 `"%H:%M:%S"` 会**补上今天的日期**。
   必要时 `ts` 也可能是 `str`/`int`（JSON 源），一样落到 `None`。

**为什么现在不痛、什么时候会痛**：今天（2026-09-21）现行制度就是 `CURRENT`，
`None -> CURRENT` 与真值一致，**故实盘当日无影响**（此结论与 §2.4 一致，未夸大）。
它会在**两种情形**下变成真实故障：① 在**存档/回填**数据上跑（历史日）；
② **下一次制度变更之后**，所有 `ts` 缺失的老数据会被按新制度解读。

**这一点让 §2.2 的严重性升级**：replay 缺陷不需要「用户自定义 `ScriptedStock`」才可达 ——
只要有任何一条历史/回填行情没有可解析的 `ts`，就会走同一个 fail-open 分支。
（本轮**未统计**实盘有多少条行情会落到 `ts=None`：未接行情源、未统计 → 标「未计数」。）

**修复建议（并入 §2.6）**：`_as_date` 区分「未知类型」与「确实 None」，
并在 `st_limit_rate_on` 已有**制度变更点**的前提下，
当日期不可解析且调用方**声称**这是历史数据时**抛错或至少计数告警**，
而不是静默返回 `CURRENT`。若短期不便改契约，最低成本做法是
让 `market_rule_version(None)` 返回一个显式的 `"cn-unknown"` 哨兵并写入告警
（它已经是一个用于**事后追溯**的字段，`models.py:57`），这样「有多少告警是在日期未知的情况下生成的」
立刻变成一个可统计的数字。

### 2.6 修复建议

1. `ScriptedStock` 增加 `day`/`trade_date`（或 `limit_rate(when)` 接受回放日），
   `replay.py:160` 改为 `limit_rate_of(self.code, self.name, when)`。
2. 更稳的根治：`Quote.limit_up_price` 的**预置值优先级**需明确 —— 若 `limit_up > 0` 但 `trade_date`
   落在旧制度区间，应**不信任**预置值（或至少让 `replay` 不预置，交由 `Quote` 自算）。
   当前「预置即真理」的语义把制度变更的判定权交给了**最不了解交易日的那一层**。
3. 让 `default_universe` 真的包含一只主板 ST（见 §3.3），否则该路径在默认配置下永远不被测试覆盖。

### 2.7 验收测试

```python
def test_replay_st_limit_respects_the_replayed_day():
    """回放日 pre-2026-07-06 时，主板 ST 的限价必须是 5%，不是 10%。"""
    stk = ScriptedStock(code="600001", name="ST某", start_price=10.00,
                        kind=KIND_LIMIT_SEAL)
    q = generate_script([stk], day=datetime(2025, 3, 10, 9, 30),
                        minutes=5, seed=7)["600001"][0]
    assert q.limit_up_price == pytest.approx(10.50, abs=0.005)   # 当前会得到 11.00 -> 红
```

---

## 3. `IT-P2-LEGACY-STATE-SLOT-001` 与 `IT-P2-REPLAY-DOC-COVERAGE-001`

### 3.1 按端口分文件：**核心修复是真的**

`tools/run_daemon.py`：

```python
L51  def _state_file(port: int | None = None) -> Path:
L52      if port is None:
L53          return RUN_DIR / "arad-daemon.json"      # 兼容旧路径
L54      return RUN_DIR / f"arad-daemon-{int(port)}.json"

L133  tmp = path.with_suffix(".json.tmp")
L134  tmp.write_text(json.dumps(st, ...), encoding="utf-8")
L135  tmp.replace(path)                # 原子替换，避免读到写一半的文件
```

我实测：`_write_state(8899)` 与 `_write_state(8905)` 产出两个**互不干扰**的文件（`pid` 分别为 111/222）；
写入用 `tmp.replace()` **确实是原子的**。这条修复方向正确。

### 3.2 但旧单槽文件**仍被每次覆写**（**已确认错误**）

```python
L136  if port is not None:
L137      # 同时刷新单槽文件，作为 `--status`（不带端口）的"最近一个"兜底
L138      _write_legacy_slot(st)
```

实测（重定向 `RUN_DIR` 到临时目录）：

```text
写入 8899(pid=111) 后再写 8905(pid=222):
    arad-daemon-8899.json -> pid=111
    arad-daemon-8905.json -> pid=222
    arad-daemon.json      -> pid=222  port=8905     ← 8899 的记录在该槽中已被顶掉
```

**这仍是 `IT-P2-DAEMON-STATE-001` 的同一个症状，只是被移到了「不带端口的路径」上。**

**但必须如实降级限定**：我核对了全部引用点，`_read_state(None)`（读旧槽的那条分支 `:110-113`）
在当前 CLI 下**不可达** —— `--port` 有 `default=8899`（`:494`，`type=int`），
`main()` 的 `if args.status: return cmd_status(args.port)`（`:522-523`）**必然传入 int**；
`STATE_FILE`（`:57`）也**没有任何读取者**（仅定义处出现）。
且 `_read_state(port)` 访问旧槽时**有 port 匹配保护**（`:105 if int(st.get("port") or 0) == int(port)`），
我实测删掉 `arad-daemon-8899.json` 后 `_read_state(8899)` 返回 `{}`（`port=None/pid=None`），**没有串号**。

→ 因此本条**当前不可达**，我按手册规定标注为 **`待验证风险`（当前不可达 / 无实际影响）**，
而不是「已确认错误的实际故障」。它的价值在于：**任何未来把 `--port` 改成可选、
或让 `STATE_FILE` 被外部工具读取的改动，会立刻把这条变成真实故障**。
建议直接删除 `_write_legacy_slot` 调用与 `STATE_FILE` 常量，或让 `cmd_status` 在无端口时
**列出全部端口**（它已经有 `_list_state_files()` 这个更好的机制了）。

**验收测试**：

```python
def test_legacy_slot_is_not_written_when_a_port_is_known(tmp_path, monkeypatch):
    monkeypatch.setattr(rd, "RUN_DIR", tmp_path)
    rd._write_state(8899, pid=111)
    rd._write_state(8905, pid=222)
    legacy = tmp_path / "arad-daemon.json"
    assert not legacy.exists(), "已知端口时不应再维护单槽文件"
```

### 3.3 `default_universe` 文档与实现不符（**已确认错误**，P2）

`src/arad/replay.py:178` 的 docstring 原文：

```text
板块覆盖主板/创业板/科创板/北交所与 ST。
```

实测 `default_universe(30, seed=42)`：

```text
总数 = 30
名字含 ST 的 = 0
```

`catalog`（`:181-193`）10 只全是「测试急拉/测试急跌/…/测试震荡」，补足部分（`:208-223`）是
`600700`/`000701`/`300702`/`688703` 这类合成代码，名字是「合成NNN」。**没有任何一只 ST**。

**影响**：`ST_LIMIT_RATE_*` 这条新制度的端到端路径**在默认股票池下零覆盖**；
§2.2 的 replay 缺陷因此不会被默认回放暴露。这与 §2 的缺陷是同一处盲区的两面。

**验收测试**：

```python
def test_default_universe_covers_st():
    names = [s.name for s in default_universe(30)]
    assert any("ST" in n.upper() for n in names), "文档承诺覆盖 ST，实际没有"
```

---

## 4. 上游云端报告 `2026-09-21_08-04-12_JST.md` 的独立复核

该报告 3 项声明我逐条复核，**全部为真**：

| 声明 | 判定 | 我核到的位置 |
|---|---|---|
| `mark_published` 发生在 `AlertBus.accept` **之前**，故 `published` 实为 `rule_selected` | **真** | 规则侧 `volume_burst.py:343-344` 在 `:345 return [...]` 之前 `_mark(obs,"mark_published",...)`；引擎侧 `engine.py:1282 alerts = rule.evaluate(...)` → `:1292 if not self.bus.accept(a, now_ep): continue`，严格在后 |
| Engine `_dispatch_many()` **完全忽略** Notifier 返回的 bool | **真** | `engine.py:1326 def _dispatch_many(...) -> None:`；`:1341 digest(alerts)` / `:1344 n.send(a)` 均为**裸调用**，返回值从未被接收/比较/记录；只有异常兜底 |
| 内置 notifier 在 disabled / severity skip 时**也返回 True** | **真** | `console.py:276-277 if not self.enabled or severity_of(alert) < int(self.min_severity or 0): return True`；`file_jsonl.py:120`、`webhook.py:270`、`serverchan.py:119`、`dingtalk.py:163`、`feishu.py:111`、`windows_toast.py:218` 同构；`base.py:15,19` 协议把 `-> bool` 文档化为「是否成功」 |

**结论**：该云端报告在这一点上是可靠的，其 `IT-P1-NOTIFY-RESULT-001/002` 应被采纳。
我把它列为**已复核为真**，不计入我的新发现。

---

## 5. 未修项回归（子 agent B 独立执行，我在冻结树复核关键项）

| ID | 状态 | 证据（当前行号） |
|---|---|---|
| `IT-P1-CAPABILITY-002` | **STILL_OPEN** | `provides` 实际在 `rules/base.py:66-78`，`capabilities` 字段 `base.py:43` 每次由 `engine.py:1269 capabilities=caps` 填充一次（`caps = capabilities_for(serving_src)`，`engine.py:1171`）→ **round-global**，无 code 参数 |
| `IT-P1-CAPABILITY-004` | **FIXED** | L1 数量兜底存在：`spirit_order.py:952-953`、`:963-966`，兜底体 `:1080-1089`（`if l1 <= 0.0: return None`，不猜）；`sina.py:224-225` 供数量；逐 pattern 账本 `:349-351`/`:354-378`/`:709-726` |
| `IT-P1-SOURCE-EMPTY-001` | **STILL_OPEN** | `engine.py:563-582` 仍以「没抛异常」判成功；探针 Partial 返 1/2 时 `backup.calls=0`，`fails={'stocks':0}` |
| `IT-P1-WINDOW-001` | **STILL_OPEN** | 仅变化才 append：`engine.py:313-314`；`window()` `:165-182` 只读 `history`；探针平盘 0-60s→65s 跳 +3.0% 得 `price_change: None` |
| `IT-P2-OBS-STATUS-001` | **FIXED** | `store.py:152-161` 同一临界区写 ledger+seq+observed_at+poll_count；`status()` `:290-297` 单锁读，`:326-328` 导出；`tests/test_status_observation_identity.py` **12 passed** |
| `IT-P1-EVAL-PUBLISH-001` | **STILL_OPEN** | 见 §4 第 1 行 |
| `IT-P1-NOTIFY-RESULT-001/002` | **STILL_OPEN** | 见 §4 第 2、3 行 |
| `IT-P1-008/009/003` | **STILL_OPEN** | `store.py:204-218` 队列满 `get_nowait`+`put_nowait` 静默丢最旧，`except Exception: pass`，**无计数、无 gap 事件**；`maxsize=200`（`:224`）；`web.py:847-853` 只发 `event:`/`data:`，**无 `id:`**，`Last-Event-ID` 全仓 0 命中；`_series_codes` 有界（`:63` `maxlen=120`）但 `_series` 主 dict 无淘汰（`:119-132`） |
| `IT-P1-ALERT-IDENTITY-001` | **STILL_OPEN** | `models.py:462-483` 字段含 `cooldown_key`，但 `to_dict()` `:516-528` **不导出**它；`signal_id|event_id` 全仓 0 命中；`key` 含墙钟 bucket（`volume_burst.py:316`）故不稳定 |

**回归计数：STILL_OPEN 8 / FIXED 2 / NOT_REPRODUCED 0 / UNVERIFIABLE 0。**

**三处旧引用已漂移**（必须更正，否则后续轮次会照着错误行号审计）：
`capabilities.py:227-229` 现在是 `SignalEvalStats.evaluable_coverage`，**不再是 `provides`**；
`spirit_order.py:547-550` 现在是 `bucket = max_per_round` / `_ts` 赋值；
`sina.py:221-225` 现在是 bid1/bid_vol 区块。三条实质结论均已在**当前位置**重新取证。

---

## 6. 实际跑过的命令 / 退出码 / 真数

```text
1) git worktree add --detach D:\ccc\_sched\work\asr-h04 ce989ca        exit=0
   -> 冻结树干净（git status --porcelain 为空）；主树的 8 个他人 WIP 文件未触碰

2) 真实全量测试（工作目录 = 冻结树）
   python -m pytest -o addopts="" -p no:cacheprovider -q --ignore=reference
   => 1579 passed in 108.71s                                            exit=0
   （与归档声称的 1579 一致；这是我在冻结树上亲自跑出的真数）

3) 探针（全部在 D:\ccc\_sched\work\，未写入任何仓库）
   asr_h04_st_probe.py            exit=0   ST 边界 / 未知日期 / 名称子串 / 板块交互
   asr_h04_st_impact.py           exit=0   Quote 路径的限价影响
   asr_h04_replay_bypass.py       exit=0   ★ replay 绕过（决定性反例）
   asr_h04_impact2.py             exit=0   ★ 规则级后果（is_at_limit_up 应为 True 实为 False）
   asr_h04_state_probe.py         exit=0   按端口分文件 / 旧槽覆写 / 同端口竞态
   asr_h04_failopen_surface.py    exit=0   ★ 根因：_as_date 静默 fail-open 全表面
   （另有一次 st_impact.py 因 Quote 缺 5 个必填参数报 TypeError，补全后 exit=0）

4) 未跑项及原因：
   - 未启动任何 server / daemon / 浏览器 -> 无法测 SSE 端到端丢包率，
     IT-P1-008/009/003 仅为静态+控制流结论（已如实标注）
   - 未抓取真实行情、未做真实通知、未交易、未启动常驻任务
   - 未测 §2.4 的真实受影响 ST 股票数（无多日连续 ST 语料）-> 标「未计数」
```

---

## 7. 三项分离与状态三轴

| 三项分离 | 计数 | 说明 |
|---|---|---|
| **程序修复** | **0** | 本轮只写 `docs/audits/intraday/`，未改任何源码 |
| **任务定义变更** | **0** | 未提任务定义变更 |
| **真实模型增益** | **0** | 本轮**无新增实股结果**；两个产品提交均为工程正确性修复，与预测增益无关 |

| 轴 | 值 |
|---|---|
| `execution_status` | `FROZEN_WORKTREE_AUDIT_COMPLETED / 1579_TESTS_PASSED / NO_SOURCE_CHANGED` |
| `research_verdict` | `ST_DATE_FIX_CORRECT_ON_QUOTE_PATH_BUT_BYPASSED_ON_REPLAY / NOTIFY_RESULT_AND_EVAL_PUBLISH_CONFIRMED_OPEN / NO_NEW_REAL_MARKET_RESULT` |
| `evidence_status` | `REMOTE_SOURCE_VERIFIED / TESTS_REAL_1579 / DEFECTS_REPRODUCED_WITH_END_TO_END_COUNTEREXAMPLE / IMPACT_COUNT_NOT_MEASURED` |

**发现分类计数**：`已确认错误` **4**、`待验证风险` **1**（`IT-P2-LEGACY-STATE-SLOT-001`，当前不可达）、
`已经修复` **2**（`IT-P1-CAPABILITY-004`、`IT-P2-OBS-STATUS-001`）、`修复后回归` **0**。
其中 4 条「已确认错误」里，`IT-P1-ST-REPLAY-BYPASS-001` 与 `IT-P1-UNKNOWN-DATE-FAILOPEN-001`
是**同一根因的两个表现**（§2.5），可合并为一个待修项。

---

## 8. 有依据的研究/改进想法（依据均来自本轮证据）

1. **把「制度生效日」变成一等公民，并且让「日期未知」不再是合法状态。**
   依据：§2.5 —— 全仓**只有一处**依赖生效日的规则（`ST_LIMIT_RATE_CHANGED_ON`，全仓 grep 仅 `models.py`
   6 个命中），而**恰好这一处**出现了三种不一致的传参方式：
   `models.py:354/364` 传 `self.trade_date`（对）、`replay.py:160` 不传（错）、
   `Quote.ts` 为 `str`/`int` 时静默退化成「今天」（错）。而 `Quote.limit_up_price` 的
   「预置值优先」（`models.py:349-350`/`:359-360`，全仓**仅此两处**该模式）又让 replay 完全跳过判定。
   建议：① `_as_date` 区分「未知类型」与「确实 None」；
   ② 让唯一入口 `limit_rate_of` 在需要历史判定却拿到不可解析日期时**抛错或计数**，
   而不是静默返回 `CURRENT`（当前是**静默 fail-open**，无任何可观测信号）；
   ③ 预置 `limit_up` 不应越过制度判定 —— 预置值来自最不了解交易日的那一层。
2. **默认股票池应当覆盖它自己文档承诺的每一个制度分支。**
   依据：§3.3 —— docstring 承诺覆盖 ST，实测 0 只；而 §2 的缺陷恰好落在「ST + 历史回放」这个交集上。
   建议给 `default_universe` 加一只主板 ST 与一只 `*ST`，并把「池子覆盖了哪些制度分支」
   做成一条自检断言（这是**零成本**地让 §2 的缺陷在 CI 里变红的手段）。**这一条是本轮性价比最高的建议**：
   它不改变任何业务语义，却能把一个已确认的 P1 从「无人知晓」变成「CI 红灯」。
3. **Notifier 的 `bool` 语义需要升级为三态账本。**
   依据：§4 —— 内置 notifier 在 disabled/severity-skip 时返回 `True`，
   而 `_dispatch_many` 又完全丢弃返回值。「没发」与「发了」在账本上不可区分，
   使得任何「漏报率」都无法计算。建议返回值改为 `sent / skipped / failed` 三态
   并在 `SignalEvalStats` 里单列 `notified`，把「规则选出」与「真正投递」分开计数
   ——这同时会解除 `IT-P1-EVAL-PUBLISH-001` 的分母争议。

---

## 9. 边界声明

- **本轮未改任何模型源码、权重、配置、Actions、PR、根 `README.md`、`SCHEDULE.md`、其他实验归档。**
- **本轮未创建、修改或删除任何定时任务或排程**（未调用 `schedule_*`、`arm_self_wake`、
  `schtasks`、`Register-ScheduledTask`）。
- 本轮只碰命中线的那**一个**仓库（`D:\ccc\ashare-radar`）；
  未触碰 `D:\xm\60日预测\reference\eventnet_test_results.json`。
- 未 force push，未推分支，未推代码；本报告与 `LATEST.md` 为唯二上传物。
- 所有 SHA / 通过数 / 退出码均来自本轮实际命令回读，**未手写、未编造**；
  未运行项已在 §6 列明原因；未计数项已标「未计数」。
- **并发写者**：主工作树有 8 个他人未提交改动，我用 `git worktree` 隔离，
  全程未读未改未提交他人 WIP。

---

## 10. 给本地 Agent 的任务书（可复制）

> **优先修根因 `IT-P1-UNKNOWN-DATE-FAILOPEN-001`**（本轮唯一有真实告警影响的 P1，含两个表现）：
> 1. `models.py:64-72 _as_date` 区分「类型不认识」与「确实是 None」；不可解析时不要静默返回 `None`。
> 2. `st_limit_rate_on` / `market_rule_version`：日期不可解析而代码已知存在制度变更点时，
>    抛错或**至少计数告警**（最低成本：`market_rule_version(None)` 返回显式 `"cn-unknown"` 哨兵
>    并随告警写出，立刻使「日期未知下生成了多少告警」成为可统计数字）。
> 3. `replay.py:160` 的 `limit_rate_of(self.code, self.name)` 补上回放日；
>    `ScriptedStock.limit_rate()` 与 `replay.py:249-251` 一并接受 `when`。
> 4. 复查 `Quote.limit_up_price`（`:349-350`）/`limit_down_price`（`:359-360`）的**预置值优先级**：
>    制度变更判定不应由预置值越过。
> 5. 给 `default_universe` 加主板 ST + `*ST`，并加「池子覆盖 ST」自检断言。
> **验收**：§2.5 的表必须全部变成 OK（`str`/`int`/`None` 不再静默给出 10%）；
> §2.7 的 `test_replay_st_limit_respects_the_replayed_day` 与
> §3.3 的 `test_default_universe_covers_st` 必须从红转绿；
> `--replay` 在 2025-03-10 上对 ST 给出 10.50 而非 11.00。
>
> 其次清理 `IT-P2-LEGACY-STATE-SLOT-001`：删除 `_write_legacy_slot` 调用与 `STATE_FILE`，
> 或让无端口 `--status` 走 `_list_state_files()` 列出全部端口。
>
> **不要**在本轮去改 `_dispatch_many` 的投递账本（§8 第 3 条）——它需要先定
> `SignalEvalStats` 的分母口径，属任务定义变更，应先出方案再动代码。
