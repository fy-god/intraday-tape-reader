# 盘中预警审计 · 补遗二 — 2026-09-23 22:05:48（h=20）

- `publication_kind`: `CURRENT_INTRADAY_RESEARCH_AUDIT_ADDENDUM_2`
- `parent_report`: `docs/audits/intraday/2026-09-23_21-44-55_JST.md`
- `preceding_addendum`: `docs/audits/intraday/2026-09-23_21-56-45_JST.md`
- `reviewed_repo`: `https://github.com/fy-god/intraday-tape-reader.git`（branch `main`）
- `main_head_at_audit`: `9129ef9924dd7ac66d172c8f58c5606b6e48dc1c`
- `audit_time_jst`: 2026-09-23 22:05:48（本地 CST 2026-09-23 21:05:48）
- `evidence_type`: `SOFTWARE_CONTRACT_PROBE`（**无**实股结果）

> **本补遗二存在的理由**：补遗一（`21-56-45`）把新回归
> `IT-P2-TENCENT-NAME-BLIND-REQUEST-KEY-REGRESSION-002` 的**影响面写小了**
> —— 我只举了 `sh000009` 一例（写作 `1/5` 暴露面）。补遗一发布后，
> 我按子 agent B 给出的更宽符号集**逐条实测**，确认**6/6 全部回归**。
> 按手册"错误必须原样更正"，此处更正**影响面**，不改判定（判定本就是"已确认错误"）。

---

## 1. 更正：受影响的符号是 **6 个**，不是 1 个

我用自己的探针形状（`fetcher=` 正确注入 + PRE/POST 成对 + 每轮实测 fetcher 调用 = 1 次，
**全程未出网**）重测 11 个符号：

```text
symbol     name             inINDEX_CODES  399段  lli(名称)  lli("")  PRE    POST   REGRESSION
sh000903   中证100指数        否             否     True       False    True   False  ← 是
sh000904   中证200指数        否             否     True       False    True   False  ← 是
sh000906   中证800指数        否             否     True       False    True   False  ← 是
sh000009   上证380指数        否             否     True       False    True   False  ← 是
sh000015   上证红利指数        否             否     True       False    True   False  ← 是
sh000133   上证150指数        否             否     True       False    True   False  ← 是
sh000001   上证指数           是             否     True       True     True   True   否
sz399001   深证成指           否             是     True       True     True   True   否
sh000300   沪深300指数        是             否     True       True     True   True   否
sh600000   浦发银行           否             否     False      False    False  True   否
sz000001   平安银行           是             否     False      False    False  True   否
```

**回归判据**（每条都成立）：修前 `R=P={带前缀}`/`missing=unexpected=∅`/`admitted=1`；
修后 `R={裸码}`/`P=∅`/`missing={裸码}`/`unexpected={带前缀}`/`admitted=0`
—— **正是该条目本来要消灭的症状，只是方向反了**。

**受影响类的完整刻画**：`symbol` 前缀为 `sh`、**不在** `INDEX_CODES`
（= `{000001,000010,000016,000300,000688,000852,000905}`）、
**不以 `399` 开头**、且服务端名称含 **"指数"/"成指"** 的**全部**上证系列指数。
这是一个**开放集合**（中证/上证各系列指数），不是我原先举的单点。

## 2. 为什么出厂配置仍然安全（维持"当前暴露 0/5"）

`config/settings.yaml:27` 的 `index_codes` =
`["sh000001","sz399001","sz399006","sh000300","sh000688"]`。
我实测这 5 个在 `lli("")`（名称盲）下**全部仍为 True**（前 4 个命中白名单或 399 段；
`sz399006` 命中 399 段），故**出厂配置一个都不落在受影响类里**。
⇒ **当前线上爆炸半径仍为 0**，且 `call_detailed` 未接线。

**运维侧可达性**：`engine.py:882-887` 的合同要求"指数必须写成带前缀"，
运维按合同加入任一上证系列指数（如 `sh000903`）即**立即**触发；
**WP03 接线后**该回归随 `poll_once` 直接进线上账本。

## 3. 为什么这个轴只有合成测试覆盖（实测）

```text
真实 fixture fixtures/raw/tencent_bulk_sample.txt:
  解析行数            = 424
  出现的交易所前缀    = {sz}          （全部 sz）
  名称含"指数"/"成指" = 0 行
```
⇒ 真实 fixture **结构上无法**触发该轴（全 sz、无指数名）。
而 `4648b1c` 新增的测试用的是 `sh000001` —— **恰好在白名单内**，
名称盲判据对它也为 True，因此**绕开了本形态**。
这就是"修复对报告形态成立、对邻近形态反向回归"却**测试全绿**的原因。

## 4. 三轴状态（仅本条，修正后）

| 字段 | 值 |
|---|---|
| `execution_status` | `PROBE_EXECUTED`（11 符号 × PRE/POST，注入 fetcher，未出网） |
| `research_verdict` | **已确认错误（P1，新回归）** |
| `evidence_status` | 控制对照实测；**出厂配置暴露 0/5**；受影响类 = 开放集合（≥6 例实测） |

**根因一句话**：`tencent.py:479` 请求侧判据把名称固定传 `""`，
而 raw 侧 `tencent.py:448` 用的是**服务端真实行名**；
`models.py:241` 的"名称含 指数/成指"规则于是**请求侧死、raw 侧活** → 两轴分叉。

**最小修法**：让两侧对称 —— 请求侧不做名称判定，
**显式写了前缀即在 `idx_set` 内就保留前缀**（恰好恢复 `4648b1c` 之前的对称性），
或让两轴共用同一个 `_key` 函数。
**验收**：同轴不变量 `_req_key(sym,name) == _key(sym,name)`，
并对上表 6 个符号断言 `missing==[] and unexpected==[] and admitted==1`，
同时保留 `sh600000`/`sz000001` 取裸码的对照（RED 2 不得复发）。

## 5. 声明

- 本轮**未改任何源码**：工作树 `git status --porcelain --untracked-files=all` = **0 条**。
- PRE-FIX 对照在 scratch 副本构造，审计仓库不可变。
- 未触碰他人未跟踪目录 `docs/audits/intraday/evidence_2026-09-23_21-00-00_JST/`。
- **未创建或修改任何排程**；未启动训练/通知/交易。
- 未编造任何 SHA / 通过数 / 退出码。
- 本次更正的触发者是子 agent B 给出的更宽符号集；**结论由我本人独立重测得出**（11 符号）。
