# 本地 Agent 任务书 — 2026-09-24 12:04:56 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`7a4f549a15e78fe87db7de00796eff71b707ae9b`
- audit-start main：`ba8c3029cb15b96a34892da9972a943c699f1b08`
- 开工前重新 fetch；docs commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取与付费服务。

## WP01 — StateUpdateResult / update_detailed【第一优先】

目标：
`EngineState.update` 不再只返回 admitted dict，而是提供 route-local admission facts。

建议：
```text
StateUpdateResult
route
admitted
rejected_future
rejected_out_of_order
stale_diagnosed
```

如果不能破坏旧 API：
保留 `update()`，新增 `update_detailed()`；WP07 生产路径必须用 detailed。

### RED 1：route code collision

模拟 Sina identity：
```text
index route code = 000001
stock route code = 000001

index age = 900s
stock age = 0s
```

期望：
```text
index stale identity 仍存在
stock fresh
```

不能因为 stocks 后更新而覆盖 index age。

### RED 2：reject reason route attribution

Case A：
```text
index future=1
stock ooo=1
```

Case B：
```text
index ooo=1
stock future=1
```

aggregate 都 future=1/ooo=1，
但 per-route result 必须不同。

Rollback：
恢复旧 global-only 逻辑后行为 RED 必须回来。

---

## WP02 — route-key time diagnostics

真实存储改成：
```text
provider_ts_raw_by_route[(route,code)]
effective_event_time_by_route[(route,code)]
received_at_by_route[(route,code)]
time_age_seconds_by_route[(route,code)]
```

旧 stock-only 兼容 view 可保留，但要明确命名。

---

## WP03 — Per-route RoundObservation

输入：
```text
SnapshotFetchResult
+
StateUpdateResult
```

输出 route terminal buckets：
```text
admitted
missing
quality
future
ooo
stale diagnostic
unattributed
```

禁止从 global stats 反推 route reason。

---

## WP04 — Engine detailed integration

stocks/index 都走 `call_detailed`。

normal/empty 只保留一个 attribution helper。

`ctx.current_indices` 继续只用本轮 index admitted。

---

## WP05 — live_session

采：
```text
stock_source
index_source
stock/index raw_return_coverage
stock/index usable_coverage
stock/index future/ooo/stale
```

`index_requested>0` 时独立 index health。

---

## WP06 — Eastmoney no-total pagination

把已有 RED/GREEN 正式落产品：
raw/member empty stop + page ledger + rollback。

---

## WP07 — Membership

transport membership 与 usable quote 分离。

## WP08 — exact soft-empty

只允许 exact raw absence 触发。

## WP09 — Timezone / ObservationInterval

继续关闭历史 P1。

## WP10 — Research

真实 manifest 后：
labels/splits -> factor diagnostics -> tabular -> SSE -> TCN。

## 产品策略纪律

当前 `test_engine.py` 明确固定 SourceManager global promotion。
本轮不要顺手改成 route-specific。
如要改，另开独立策略变更。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

route_time_diag_red.log
route_time_diag_green.log
route_time_diag_rollback.log

route_reject_red.log
route_reject_green.log
route_reject_rollback.log

route_observation_red.log
route_observation_green.log
route_observation_rollback.log

engine_detailed_red.log
engine_detailed_green.log
engine_detailed_rollback.log

pagination_red.log
pagination_green.log
pagination_rollback.log

route_time_cases.json
route_reject_cases.json
route_observation_cases.json
pagination_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

所有未运行项必须 `not_run/blocked + 原因`。
