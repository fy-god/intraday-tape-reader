# 盘中预警审计 — 2026-09-23 05:35:00 JST

- `audit_time_jst`: `2026-09-23 05:35:00 JST`
- `reviewed_repo`: `fy-god/intraday-tape-reader`（工作树 `D:\ccc\ashare-radar`）
- `reviewed_source_sha`: `bf0b83bf212f169c720ca8a1c5108405c18a9712`（**实际命令读出**）
- `main_head_at_audit_start`: `eb70ad5bf2c6df009d8521d1a13522a162ab56ed`
- `previous_report`: `docs/audits/intraday/2026-09-23_04-05-42_JST.md`（peer，非本线）
- 开放 PR：0
- 类型：`INDEPENDENT_REAL_MARKET_REEVAL + SOFTWARE_TEST_EXECUTION + INDEPENDENT_VERIFICATION`

---

## 0. 本轮结论（先说最重的）

1. **peer 的 04:05 P1 主张被我"实测"确认，而不是被推翻。** peer 自己写明沙箱**无完整 checkout**，所以 full pytest、fixture 驱动**全部 `not_run`**，"370 个老成员被静默移除"是**读代码推出来的**。我有完整 checkout，**驱动真实调用链实测**：`len(eng._codes)` 刷新前 **5913** → `refresh_universe()` 返回 **5553** → 刷新后 **5553**，**净 -360**，**370 个原始成员消失**，10/10 新股进入。**peer 的 9 个数字全部逐一对上。**

2. **【本轮唯一真正的新证据 · 实股】我把 peer 只用合成夹具测过的"停牌率"，第一次放到真实 A 股面板上量化。** peer 的夹具是 `every=16 → 370/5913 = 6.26%`，并**明确声明**"这只是配置上限推导，不是本轮实测漏报率"。实测 2013-01-04..2025-12-31 全主板 8,728,357 行 / 3,392 只 / 3,157 个交易日：
   - 停牌股占当日成员的比例：**p50 = 0.81%，p95 = 11.61%，p99 = 17.58%，max = 50.15%**
   - **达到或超过 peer 夹具那个 6.26% 的交易日：956 / 3,157 = 30.3%**
   - 最坏日 **2015-07-09**：2,299 只成员中 **1,153 只停牌（50.15%）**
   - 独立交叉信号（`volume == 0`）与 `tradestatus == 0` 相关 **0.9992**
   ⇒ peer 的合成率**不是保守上界，而是中位数附近**；真实市场有 **30% 的交易日**比它更糟，2015 年股灾期间达到 **8 倍**。

3. **【本轮第二个新发现 · 我实测，peer 没有报】同一 5 行里还有一个更严重的后果：终止页被丢弃。** `eastmoney.py:434` 的 `break` **在** `:436` 的 `pages.append(page)` **之前**。arm T3 实测：page2 整页无可用报价时，`pages_requested = [1, 2]`、**page3 从未被请求**、且 **page2 的 100 个合法 code 也没进 raw 账本**（`raw_unique_codes = 100` 而非 200）。⇒ 不只是"后续页不请求"，**终止页自己的合法成员也被扔掉**，传输账本同时**少记**。peer §7 只说了前半句。

4. **无门禁**：`docs/audits/validate_latest.py` 在本仓**不存在**（`ls-tree` 0 命中；263 个跟踪文件）。这是我用**不会产生假"不存在"**的方法复核的——正因为我 h=23 在无 `.git` 的抽取里跑 `cat-file -e` 得出过假"不存在"。本仓**确实没有**，故本轮**不得声称任何门禁 PASS**。

---

## 1. 固定版本与执行边界

| 项 | 值（实际命令读出） |
|---|---|
| 远端 | `https://github.com/fy-god/intraday-tape-reader.git` |
| `origin/main` | `eb70ad5bf2c6df009d8521d1a13522a162ab56ed` |
| 本地 HEAD | `bf0b83bf212f169c720ca8a1c5108405c18a9712`（= `reviewed_source_sha`） |
| 领先提交 | `bf0b83b..origin/main` **10 个，全部 docs-only** |
| 工作树 | **0 行**（干净；无他人未提交改动） |
| 本仓门禁 | **不存在**（`ls-tree` 0 命中） |
| `git status` 前后 | 均 0 行 |

**产品源码在本轮窗口内零变化**：`git diff --stat bf0b83b..origin/main` 只含 `docs/audits/intraday/` 下 9 个文件（+2346/-93）。⇒ 本轮**不是**"审是否已修"，而是**审 peer 主张是否成立**，并把它的合成率放到**真实数据**上量化。

---

## 2. 【P1 · 已确认错误】股票池刷新在 transport 完整时无下限保护，370 个老成员被移出扫描集

**文件/函数/行**
- `src/arad/sources/eastmoney.py:191-200` `_quote_of()`：`f2`/`f18` 缺失、为 `"-"` 或非正 → `return None`（该行被丢弃）。
- `src/arad/sources/eastmoney.py:287-304`：`raw_rows += 1` 记**每一行**，`codes` 记**每个合法 code**，`quotes` 只记**能构造 Quote 的行** ⇒ **双账已经存在**。
- `src/arad/sources/eastmoney.py:446-478`：`shortfall` 由 **`raw_unique_codes`** 算，故**停牌不产生 shortfall**（370 行有唯一 code，只是价格无效）⇒ `transport_complete = True`；`usable_coverage = 0.9375` 仅作**诊断展示**。
- `src/arad/engine.py:1255` `if not meta.get("complete", True):` → 走进**完整分支**。
- `src/arad/engine.py:1268-1285`（关键 `:1273 self._codes_raw = codes`）：**直接用 5553 覆盖 5913，无任何尺寸比较**，记 `股票池已刷新: 5553 只` 并 `return 5553`（成功）。
- `src/arad/engine.py:1290-1302`：**"不得缩小扫描范围"保护只存在于 `partial is not None` 分支**（`if prev and n < prev: ... return 0`），在 `complete=True` 路径上**结构性不可达**。

**触发条件 / 反例（我实测，非推理）**

驱动仓库**自己的** `_suspend_every_fetcher(every=16, new_codes=NEW_CODES)`，经真实 `EastmoneySource.universe() → Engine.refresh_universe() → _record_universe() → _extract_codes()`：

| 量 | 实测值 |
|---|---|
| `len(eng._codes)` 刷新**前** | **5913** |
| `refresh_universe()` 返回值 | **5553** |
| `len(eng._codes)` 刷新**后** | **5553** |
| 净变化 | **-360** |
| 原始 5913 只中**消失** | **370** |
| 10 只新股进入 | **10 / 10** |
| 消失样本 | `600000, 600016, 600032, 600048, 600064, 600080 …` |

**同时整仓测试全绿**：`python -m pytest -q -o addopts= -p no:cacheprovider` → **1802 passed in 114.93s，exit code 0**；`tests/test_eastmoney_truncation.py` → **29 passed in 2.46s，exit code 0**。

**severity 曲线（我实测，peer 只测了一个点）**。`every` = 每 N 个老 code 插 1 行无效当前价；`after` = 刷新后 `_codes`：

| every | raw | usable | dropped_invalid | before | after | delta |
|---|---|---|---|---|---|---|
| **0（对照）** | 5923 | 5923 | 0 | 5913 | **5923** | **+10** |
| 2 | 5923 | 2966 | 2957 | 5913 | 2966 | **-2947** |
| 4 | 5923 | 4444 | 1479 | 5913 | 4444 | -1469 |
| 8 | 5923 | 5183 | 740 | 5913 | 5183 | -730 |
| **16（peer 的点）** | 5923 | 5553 | 370 | 5913 | 5553 | -360 |
| 32 | 5923 | 5738 | 185 | 5913 | 5738 | -175 |
| 64 | 5923 | 5830 | 93 | 5913 | 5830 | -83 |

**对照臂 every=0 给出 delta=+10**（只有 10 只新股，老成员零损失）⇒ 探针**能**返回"不缩小"的结果，**不是恒返回缩小的坏探针**。

**机制**：`transport_complete` 只对**传输层**负责（raw 行够 → True），但 Engine 收到"完整"信号后**直接用 usable 投影覆盖**扫描集。**保护代码写在了另一条分支上**——这正是 `engine.py:1293` 自己的注释所警示的场景（"否则 5563 只会被 3000 只静默替换，且记为「刷新成功」"），但该保护在完整路径上永不执行。

**影响**：
- 被移除的 code **不再出现在后续 snapshots 请求里**（`engine.py:1448` 用 `self._codes`），规则状态机、SourceManager 都**看不到它**。
- 恢复交易后，**最长要等一个 refresh interval**（默认 `poll.universe_refresh_seconds: 1800` = **1800 秒**）才重回扫描集。这是**配置上限推导**，**不等于**实测漏报次数。
- **不"静默"到完全不可见**（**peer 措辞需收紧**）：`Engine.universe_truth()` 事后确实报 `active_scan_codes=5553`、`coverage_active=0.937531656255276`、`expected_total=5923`。⇒ 准确说法是"**未被阻止、且从未作为失败上报**"，而不是"完全不可见"。我采纳 peer 的结论但收紧这个词。
- **实股规模（本轮新增）**：真实 6.26% 以上占 **30.3%** 交易日，p95 = **11.61%**、p99 = **17.58%**、max = **50.15%**（2015-07-09）。按我实测的曲线，一个 5,923 只的池在 p95 日会扫描约 **5,235** 只（丢 **~688**），在 2015-07-09 那种日子只剩约 **2,952** 只（丢 **~2,971**）。

**修复建议**：
1. 把 `engine.py:1290-1302` 的"不得缩小"保护**提升为对 complete 路径同样生效**，或至少**在缩小超过阈值时告警/降级**；
2. 让"扫描集"与"可用报价集"成为**两条独立口径**（peer 的 Membership Truth v1 方向正确）：`active_scan_codes` 应基于 **raw transport 成员**，`usable_quotes` 单独统计；
3. 停牌/无有效价的成员应**保留在扫描集**（其行情请求无害）或走**显式降级路径**并在 status 里标记。

**验收测试（可直接写红）**：
```python
# transport_complete=True, raw_unique_codes=5923, usable_quotes=5553
assert len(eng._codes) == 5923            # 当前会失败（实测 5553）
assert set(pool_5913) <= set(eng._codes)  # 每个原始 transport 成员仍在扫描集
```
**对照/阳性对照**：`every=0` 时必须 `len(eng._codes) == 5923` 且无老成员丢失（已实测 +10）；把 `complete` 置 False 且结果更小时 `refresh_universe()` 必须返回 0（保护分支，须实测其可达性）。

**分类**：`已确认错误`（错误值确实**上线**：`_codes_raw` 真的被覆盖成 5553，且 `refresh_universe()` 真的把 5553 作为**成功**返回）。**我独立复现**，不是转述 peer。

---

## 3. 【P2 · 已确认错误 · 本轮新发现】无 numeric total 时，`break` 先于 `append`，终止页被整页丢弃

**文件/函数/行**：`src/arad/sources/eastmoney.py:429-441`
```python
429|        else:
430|            # total 不可用（data/diff 为 null）：顺序探测，遇空页即停，避免空转 80 页。
431|            # 「空页」判据沿用**可用行情为空**（而不是 raw 行为空），与修复前一致。
432|            for pn in range(2, self.max_pages + 1):
433|                page = parse_clist_page(self._request_json(self._clist_url(pn)), seq)
434|                if not page.quotes:
435|                    break
436|                pages.append(page)          # <-- 在 break 之后
437|                pages_requested = pn
```
`:444-445` 的 `raw_rows = sum(p.raw_rows for p in pages)` 只累加**已 append 的页**。

**反例（我实测）**：
- 臂 T1（无 total，page1/page2 各 100 个**合法** code，page3 空）：`pages_requested=[1,2,3]`、`usable=200`、`raw_unique_codes=200` ⇒ 正常路径**不丢**（`quotes` 非空故 append 执行）。
- 臂 T2（对照，有 total=200）：`pages_requested=[1,2]`、`raw_unique_codes=200`、`transport_complete=true`。
- **臂 T3（无 total；page1 合法；page2 整页停牌 0 可用；page3 合法）**：
  - `pages_requested = [1, 2]` ⇒ **page3 从未请求**
  - `usable_quotes = 100`
  - `raw_rows = 100`、`raw_unique_codes = 100` ⇒ **page2 的 100 个合法 code 也没进账本**（若 append 先于 break 应为 200）
  - `requested_page3 = False`

**机制**：判据用 **usable quotes** 是否为空的**同一个值**既决定"停止翻页"，又（因为 append 在后）决定"这页算不算数"。⇒ 一个数据页要么全要，要么全丢。

**B 补充的对照臂（我采纳其为机制证据，因与我 T1/T3 一致）**：
| 臂 | `pages_requested` | page3? | `raw_unique_codes` / `usable_quotes` |
|---|---|---|---|
| TEST（page2 有合法 code、0 可用） | `[1, 2]` | **否** | 100 / 100 |
| CONTROL-A（page2 可用） | `[1, 2, 3, 4]` | 是 | 300 / 300 |
| CONTROL-B（page2 真为空 0 行） | `[1, 2]` | 否 | 100 / 100（此处 break 是**对的**） |
| **CONTROL-C（TEST 的行形状 + numeric total）** | `[1, 2, 3]` | **是** | 300 / 200 |

**CONTROL-C 是关键**：同样的行形状只要**有** numeric total 就会继续翻页 ⇒ **停止是由 `else:` 分支的规则造成的，不是行形状造成的**，探针有真实区分力。
**修复方向经 B 在 scratch 副本验证（未动真仓库）**：把 `if not page.quotes:` 改为 `if not page.raw_rows:` 后，TEST 变 `[1,2,3,4]`、`raw` 100→300、`usable` 100→200，而 CONTROL-B 仍为 `[1,2]` ⇒ peer 的方向**机制上足够**。

**与 peer 的关系**：peer §7 只报"后续页不再请求"。**"终止页自身的合法成员也被丢弃 + 传输账本少记"是我实测补充的**，而且它更严重：**它污染的是 raw transport 账本本身**，也就是 §2 那个 `transport_complete` 所依赖的同一个账本。

**影响**：无 total 的响应下（`data.total` 为 `null`）会**同时**低估成员数并停翻；`transport_complete=false` 会让 Engine 走 partial 分支（这里恰好被 `:1290-1302` 保护兜住），但**账本数字已经错了**，任何据此做的完整性判断都建立在一个偏低的分母上。

**可达性（不夸大）**：`fixtures/raw/eastmoney_clist_p1.txt` 含 `"total": 5913`；`eastmoney_ulist_5.txt` 含 `"total": 5`。⇒ 仓库内**没有任何夹具**含"缺 numeric total 的 clist 响应"，故本路径**无法经任何已入库产物触发**。**0 个测试**覆盖"未知 total + 一页全不可用"：`tests/test_sources_eastmoney.py:402-413` 是本仓唯一探测未知 total 的用例，但其各页都有可用行（`raw_rows==0` 与 `quotes==[]` 同时成立 ⇒ 对该缺陷**零区分力**）；`tests/test_eastmoney_truncation.py:401,611` 在 **page1** 用 `data:null` 故循环根本未进入；所有"全不可用行"用例（`test_eastmoney_truncation.py:434-467`、`test_sources_eastmoney.py:223-243`）都带 numeric total ⇒ 永不进入 `else` 分支。
入口本身是**真实且有文档**的：`DATA_CONTRACT.md:189`（"`data` 为 `null` 或 `diff` 为 `null`…"）与 `NOTES_eastmoney_sina.md:133-134` 正是这条路径；实测 `page1 data:null` 可进入（`[1,2,3,4]`）。默认源链 `config/settings.yaml:50` = `["eastmoney","sina"]`，且 `engine.py:1255/1265-1267` 把不完整结果只当 `partial` 并继续尝试下一源 ⇒ **后果被缓解**（除非 Sina 同时失败）。故我标 **P2**（不是 P1），并**不声称**它在生产上高频。peer 也自标 P2——**没有理由升级或降级**。

**修复建议**：`pages.append(page)` 移到 `if not page.quotes:` **之前**；并把终止判据改为基于 **raw 成员行**而非 usable quotes（与 peer §7 方向一致）。
**验收测试**：造一个无 `total` 的响应序列 `[100 可用, 100 可用但 0 可用, 100 可用]`，断言 `pages_requested` 含第 3 页**且** `raw_unique_codes == 200`。
**阳性对照**：全可用页时断言 `raw_unique_codes == 300`（证明探针能返回"不少记"）。

**分类**：`已确认错误`（终止页的合法 code 确实没进账本，可实测复现）。

---

## 4. 【P2 · 待验证风险】Eastmoney 无 numeric total 时分页终止由 Quote 可用性决定

即 peer 的 `IT-P2-EASTMONEY-UNKNOWN-TOTAL-USABLE-EMPTY-STOPS-PAGINATION-001`。**我确认该代码事实**（`:434 if not page.quotes`），但**不重复其 ID**，而是把它与 §3 合并成同一处 5 行的**两个**后果。peer 自标 P2 且承认未做 fixture —— 我用 T3 把它**做成了实测**。分类维持 `待验证风险`→实际已是 `已确认错误` 的代码事实，但**生产可达性未证实**（夹具都有 total），故整体标 **`待验证风险`**。

---

## 5. 我自己的探针错误（如实记录）

**第一次实股量化是无效的，我作废了它。** 我用"面板里 (code, date) 行缺失"当停牌信号，得到**每个交易日都 0.00%**——一个对所有变体返回同一值的探针。我没有把它当结论上报，而是**先调试**：直接按 code 统计"span 内应有 session 数 vs 实际行数"，结果 **3392/3392 只 code 缺失 = 0**。⇒ **该面板在每个 code 的存续区间内是逐日补齐的（供应商为停牌日也出行）**，"缺行"根本不能当停牌信号。**该探针无效，结论作废。**

改用面板自带的 baostock `tradestatus` 列重测，并**先验证该列非恒定**（`{0.0: 283,076, 1.0: 8,445,281}`，NaN=0，distinct={0,1}）才采用；再用独立的 `volume == 0` 交叉验证（与 `tradestatus==0` 相关 **0.9992**）。§2 的实股数字全部来自这个**修正后**的测量。

---

## 6. 三轴状态

- `execution_status`: `AUDIT_COMPLETED / READONLY / REAL_SUITE_EXECUTED(1802 passed exit 0)`
- `research_verdict`: `PEER_H04_MEMBERSHIP_CLAIM_CONFIRMED_BY_EXECUTION(5913->5553, -360, 370 lost, 10/10 new); NEW_SUBFINDING_TERMINATING_PAGE_DISCARDED(eastmoney.py:434 before :436; raw_unique_codes=100 not 200; page3 never requested); REAL_MARKET_SUSPENSION_RATE_MEASURED(p50 0.81%, p95 11.61%, p99 17.58%, max 50.15%; 30.3% of sessions exceed the peer's synthetic 6.26%); NO_GATE_IN_THIS_REPO`
- `evidence_status`: `REAL_SOURCE_READ + REAL_SUITE_EXECUTION + REAL_MARKET_PANEL_MEASUREMENT(8,728,357 rows) + SYNTHETIC_FIXTURE_RED + MUTATION/CONTROL_ARMS`
- `evidence_type`: 分开计数 —— `SOFTWARE_TEST_EXECUTION`（1802 passed）、`REAL_MARKET_NEW_FIT` **0**（本轮无新拟合）、`REAL_PACK_MEASUREMENT`（面板停牌率）、`PROVENANCE_AND_RECONCILIATION`（SHA/账本）

---

## 7. 三类分开

- **程序修复**：**0 项**（本轮未改任何产品源码）。
- **任务定义变更**：**0 项**。
- **真实模型增益**：**无**（`real_market_fit_count = 0`、模型训练 **0**）。

---

## 8. 研究/改进想法（各附依据与证伪方式）

1. **把"扫描集"与"可用报价集"做成两条独立不变量，并对缩小本身设红。**
   依据：§2 实测 5913→5553 且 `engine.py:1290-1302` 的保护在完整路径不可达。
   证伪：若把 raw 成员作为扫描集后，`active_scan_codes` 在 `every=16` 夹具下变为 5923 且**不**改变任何现有告警行为，则该改造无收益。
2. **用 `tradestatus` 给"停牌 vs 退市 vs 无数据"三分，替代用 coverage 比值反猜原因。**
   依据：§2 实股 p95=11.61%；panel 的 `tradestatus` 与 `volume==0` 一致度 0.9992；peer §8 亦指出 drop-cause 被压缩成一个数。
   证伪：若三分后没有任何消费方（status/研究 manifest）读取它，则只是新增死字段——**这正是本仓已有 3 处"只写不读"字段的前车之鉴**。
3. **把"终止页"作为独立断言对象，而不是只断言页数。**
   依据：§3 臂 T3 实测 `raw_unique_codes=100`（应为 200）而 `pages_requested` 只差一页；只断言页数的测试会漏掉账本少记。
   证伪：若断言 `raw_unique_codes` 后现有 29 个测试有任一失败，说明现有口径本就不同，需先对齐口径。

---

## 9. 未运行项与原因（不写 0 伪装）

| 项 | 状态 | 原因 |
|---|---|---|
| 真实行情网络抓取 | **未运行** | 手册禁止；且需避免对外部数据源发压 |
| notifier / 交易 | **未运行** | 手册禁止 |
| 模型训练 | **未运行** | 本轮为产品缺陷审计，非建模轮 |
| `dash` 渲染 / `check_*.py` | **未运行** | 与 §2/§3 的发现无关；预算投给可复现的 RED 与实股量化 |
| 本仓门禁 | **不存在** | `ls-tree` 0 命中；故**不声称任何 PASS** |
| 修 `engine.py` / `eastmoney.py` | **未做** | 改产品源码超出授权 |

---

## 10. 独立复核（3 个只读子 agent）

**纪律**：全部只读、只写各自 scratch；禁止改 `docs/audits/`、push、合并 PR、创建排程、启动远程训练、覆盖他人未提交改动。结论**必须经我独立复核**才采纳。

| 子 agent | 结论 | 我的复核 |
|---|---|---|
| **A 成员契约** | `CLAIM_CONFIRMED`：实测 `len(eng._codes)` 5913→5553；真实 pytest **1802 passed exit 0**；定位 `engine.py:1255/1268-1285/1290-1302`；测试盲点 `tests/test_eastmoney_truncation.py:643-650` 只断言"10 只新股进入"，**不断言 5913 老成员仍在** | **我独立复现全部**：5913→5553、370 丢失、10/10 进入、1802 passed；并**亲自重测保护分支不可达**（见 §2） |
| **B 分页 P2** | `CLAIM_CONFIRMED` + **新子发现**：`:434` 的 break 先于 `:436` 的 append，终止页被丢弃；4 臂（含 CONTROL-C 有 total 的对照）证明停止由 `else:` 规则造成；在 scratch 副本把判据改成 `raw_rows` 后 TEST 变 `[1,2,3,4]`、raw 100→300 ⇒ 修复方向机制上足够；`tests/test_eastmoney_truncation.py tests/test_sources_eastmoney.py` = **90 passed exit 0** | **我独立复现**：臂 T3 实测 `pages_requested=[1,2]`、`requested_page3=False`、`raw_unique_codes=100`；三臂对照见 §3；**修复方向我未复跑**（超出本轮授权，且 B 已在副本验证），故我**只记录不背书** |
| **C 未修项回归** | 逐项实测，全部带 `file:line` 与离线反例：`IT-P0-002`(tz) **CONFIRMED_STILL_OPEN**；`IT-P1-SOURCE-EMPTY-001` **CONFIRMED_STILL_OPEN**；`IT-P1-WINDOW-001` **CONFIRMED_STILL_OPEN**；`IT-P0-001`/`IT-P1-LIMIT-001`/`IT-P2-OBS-001`/`IT-P0-003` **NOW_FIXED**；并指出**手册 §4 基线已失效** | **我独立复核（见 §10.1）**：4 个 FIX commit 均 `cat-file -t`=commit 且 `merge-base --is-ancestor`=**True**；`session.py:72` 确不含 `CLOSE_AUCTION`；`limit_board.py:210-219` 确先判 `qualified` 再写 `sealed`；`git grep ObservationInterval -- src/` **rc=1（0 命中）**，全仓 52 文件**全在 `docs/`**；窗口内**产品文件 0 个**（9 个全 `docs/`） |

### 10.1 我对子 agent C 的独立复核（含一处我自己的探针 bug）

C 的**核心结论我逐条复核并确认**（命令与输出见 scratch `it_h04_verifyC.txt`）：

| 复核项 | 我的实测 |
|---|---|
| `020f2bb` / `100e06a` / `2406d81` / `3477820` 四个 FIX commit | 均 `type=commit`、`ancestor_of_pin=`**True**；提交信息分别明写 `(IT-P0-001)` / `(IT-P1-LIMIT-001)` / `fix(obs)` / `(IT-P0-003)` |
| `session.py:72` 是否排除 `CLOSE_AUCTION` | **是**：`CONTINUOUS = (MORNING, AFTERNOON)`，且 `:67` 注释明写"**故意不含** CLOSE_AUCTION" |
| `limit_board.py` 是否先判门槛再写 `sealed` | **是**：`:210 qualified = ...` → `:213 if not qualified: :217 _state = "at_limit_unqualified"; return None` → `:219 _state = "sealed"` |
| `ObservationInterval` 在 `src/` 的命中数 | **rc=1，0 命中**；全仓 52 个文件**全部在 `docs/`** |
| 窗口内产品源码变化 | **0**：9 个变更文件全在 `docs/`；10 个提交全部 `docs(audit): … [skip ci]` ⇒ **pin 的 SHA 本身就是产品 HEAD** |

**我的一处探针 bug（如实记录）**：我写了一个小检查想逐项确认"手册把哪些 ID 列为未修"，但它用 `"盘中预警" in l and "ashare-radar" in l` 选行，**先命中了路由表那一行（manual:19）而不是 §4 的正文行（manual:101）**，于是 6 项全部被误报为 `False`。**该子检查无效**（过滤器匹配到了错误的行）。我已**直接读取并引用 manual:101 的原文**，不依赖那个坏检查。

**手册 §4 原文（manual:101，逐字）**：
> `- **盘中预警** D:\ccc\ashare-radar：曾记录 1068 passed / 0 failed；IT-P0-003 已修；未修 IT-P0-002、IT-P0-001、IT-P1-SOURCE-EMPTY-001、IT-P1-WINDOW-001、IT-P1-LIMIT-001、IT-P2-OBS-001。`

⇒ 它列 6 项为"未修"，其中 **3 项（`IT-P0-001`、`IT-P1-LIMIT-001`、`IT-P2-OBS-001`）经我复核确已修复**；**3 项（`IT-P0-002`、`IT-P1-SOURCE-EMPTY-001`、`IT-P1-WINDOW-001`）确实仍未修**。**"1068 passed" 也已失效**——我在 pin 的树上实跑为 **1802 passed / exit 0**，**比该数字多 734**。

**这是本轮的第三个交付：把本线的回归基线从"上轮残留"改为"经实测核对"**。手册 §4 自己就要求"必须逐条用真实文件重新确认后才可上报告；对不上就如实修正"，我照做并在此**如实修正**。**我未修改手册文件本身**（越权）——只在本报告中给出更正后的基线，供人类操作者决定是否更新。

**A 的两处收紧我都接受并写入**：（a）"silently"应改为"未被阻止且从未作为失败上报"（`universe_truth()` 事后可见）；（b）`_suspend_every_fetcher` **无装饰器**（普通函数），我打印时也确认其为 plain `def`。
**B 的一处我未采纳**：B 称臂测 `raw_unique_codes=100` 出现于"page2 有 100 个合法 code"的场景——该数字只在其 **all-invalid 终止页**臂下成立；我的 T1（两页全合法）实测为 **200**。**已按我自己的三臂测量为准写入 §3。**

---

## 11. 本轮未改源码、未动排程（声明）

- **未修改任何模型源码、权重、配置、Actions、PR、根 `README.md`、`SCHEDULE.md`**。
- **未创建或修改任何定时任务或排程**（未调用 `schedule_*`、`arm_self_wake`、`schtasks`、`Register-ScheduledTask`）。
- **未 push 任何代码**；本轮只新增本报告并随即更新 `docs/audits/intraday/LATEST.md`。
- 所有执行都在**仓外 scratch clone**（`D:\ccc\_sched\work\it_h04_main\clone`）内完成，审计工作树 `D:\ccc\ashare-radar` 全程**只读**，`git status` 前后均 **0 行**。
- **未触碰** `D:\xm\60日预测\reference\eventnet_test_results.json`。
- 本轮 `real_market_fit_count = 0`、`model_training_runs = 0`。

---

## 12. 更正后的本线回归基线（供下一轮使用；我不改手册文件）

手册 `audit_rotation_v3.md:101` 的 §4 基线**已失效**。经本轮实测，正确基线应为：

| 项 | 手册 §4 记载 | 本轮实测 | 依据 |
|---|---|---|---|
| `IT-P0-001` | 未修 | **已修复** | `session.py:72/158-160/225-227`；commit `020f2bb`（祖先=True） |
| `IT-P1-LIMIT-001` | 未修 | **已修复** | `limit_board.py:210-219`；commit `100e06a`（祖先=True） |
| `IT-P2-OBS-001` | 未修 | **已修复** | `live_session.py:263-274`；`capabilities.py:476-544`；commit `2406d81`（祖先=True） |
| `IT-P0-003` | 已修 | **已修复**（确认） | `engine.py:1563/1579/1596-1597`；commit `3477820`（祖先=True） |
| `IT-P0-002`（TZ 半） | 未修 | **仍未修** | `session.py:100` `_now_fn` **声明后 0 处读取**；`:122/:142/:214/:232/:242/:252` 均为裸 `datetime.now()`；`app.timezone` 只写不读 |
| `IT-P1-SOURCE-EMPTY-001` | 未修 | **仍未修** | `engine.py:563-582`：**只有 `except Exception` 推进 failover**；主源返回 `[]` 即 `serving=primary`、`fails` 归零并 `return` |
| `IT-P1-WINDOW-001` | 未修 | **仍未修** | `engine.py:313-314` 仅变化时 append；`:179-182` 只读 `history`；`tick_surge.py:197-198` `_covered`；`ObservationInterval` 在 `src/` **0 命中** |
| 测试真数 | "1068 passed" | **1802 passed / exit 0** | 我在 pin 的树上实跑 |
| 本轮新增 | —— | **`IT-P1-UNIVERSE-MEMBERSHIP-QUALITY-001`（peer 报，我实测确认）+ 终止页丢弃（我新增）** | §2、§3 |

**下一轮应优先处理的仍未修项**：`IT-P0-002`（TZ，`session.py` 的 `_now_fn` 死字段）、`IT-P1-SOURCE-EMPTY-001`（空而不失败抑制 backup）、`IT-P1-WINDOW-001`（窗口区间表达缺失）。
