# 本地 Agent 执行任务书 — 盘中预警研发 v2 / 2026-09-19 20:10 JST

## 0. 基准与边界

- 仓库：`fy-god/intraday-tape-reader`
- 本轮云端观察 HEAD：`6d92f50e317de9b8ff4a34a84766347c3de3d041`
- 当前产品源码基准：`34778204dee41575697357f5d2e20a40aad3bc9d`
- 已修不可回退：`IT-P0-003` current-only stock Snapshot；`IT-P1-006` 主体完整性保护；`IT-P1-007` route-specific failover accounting。
- 本地研发只在隔离工作区进行。不要 push 产品代码、权重、原始行情；远端 GitHub 文档发布由审计流程单独处理。
- 禁止真实通知、交易、常驻抓取、自动租卡、自动改 Actions/PR。测试默认离线，使用 FakeSource/假时钟/RecordingNotifier。

## 1. 先做 WP01：把云端机制反例迁成真实仓库测试

### 1.1 IT-P0-002 时间质量

真实文件：`src/arad/engine.py::EngineState.window/update`。

新增测试建议放入现有 `tests/test_engine.py` 或**待新增** `tests/test_observation_quality.py`；不要引用不存在的 `tests/test_full_day_simulation.py` 作为既有文件。

必须先在未修代码看到这些红测：

- history 有 t=100 与 t=110，查询 `now=105,W=10` 时不能返回 t=110；旧代码应红。
- 新 Quote `q.ts` 早于上一已接纳 provider ts 时，不允许回滚 current state。
- `q.ts > now + allowed_skew` 时，应在 state/history 写入前 reject/quarantine。
- 同秒但内容变化必须定义修订策略；不要用“timestamp 相等就丢”掩盖。

实现建议先新增内部时间 admission helper，再改 `window` 上界；不要只补 `<= now` 就宣布全部时间问题完成。

### 1.2 IT-P1-WINDOW-001 横盘 anchor

固定 fixture：t=0,5,...60 每轮新鲜 `P=10,V=100`，t=65 `P=10.3,V=101`。旧逻辑 60s `price_change=None`；候选应约 +3%。

不要简单无限保存全部重复点。候选实现 `ObservationInterval(first,last,price,cum_volume,source_epoch)`；新鲜同值延长 last，断线不延长。额外负控制：只有 t=0 与 t=65 两次真实观测时，60s 窗口仍必须 unavailable。

### 1.3 IT-P1-LIMIT-001 封单资格状态

在 `tests/test_rule_limit_board.py` 加同一 Rule 实例两轮测试：价格始终涨停，封单金额 100万→300万，门槛 200万。期望 `[None, seal_once]`；跌停侧镜像。把状态拆成 `at_limit_unqualified` 和 `sealed_qualified`，不要在 `_make_seal` 资格检查前写最终 sealed。

### 1.4 IT-P1-006-R1 Eastmoney max_pages

在 Eastmoney source 测试中固定 `total=300,page_size=100,max_pages=2`，两页都成功。期望 `universe_info.complete=False`、`reason` 明确 truncated，Engine 不得用该结果缩小完整池。再加 required_pages<=max_pages 的正控制。

### 1.5 IT-P1-SOURCE-EMPTY-001 soft-partial

对 Tencent/Sina/Eastmoney 分别造“请求 A,B，但正常响应只包含 A”的 fixture。先钉住现状，再增加内部 `BatchResult`，区分 `explicit_unavailable` 与 `unknown_missing`。SourceManager 只对 unknown missing 有界补备用源；明确停牌不能无限重试。

完成标准：回传每项“修前红、修后绿、回退再红”的命令/退出码/日志；全量 pytest 只有真实跑过才报告数字。

## 2. WP02：真实数据 manifest 和 TaskSpec

扫描**用户已授权且本地实际存在**的数据目录与 repo-local `data/`，不猜路径，不持续下载。生成：

`research/intraday_quality/artifacts/dataset_manifest.json`

字段至少：文件/表 hash、symbol count、交易日 count、每 symbol-day 行数分布、timestamp 字段角色、采样间隔 p50/p95/max、OHLC/price/amount/volume/depth/inner/outer/source 字段存在率、单位、重复率、乱序率、缺失率、跨午休/跨日情况、source epoch、evidence_type。

若没有至少约 30 股×20 日可连续随访的真实快照，真实模型训练状态写 `blocked_real_data`，但继续完成 schema/label/split/feature 代码，不准用 synthetic 指标冒充。

冻结 `task_spec.json`：主任务先用 180s observed-snapshot persistence，60/300s 仅辅助；明确 direction、15bp/25bp 等阈值是否沿用本轮 prototype 或重新预登记。任何重新定义必须在看新测试成绩前完成并写 hash。

## 3. WP03：迁移 35 因子到真实字段

待新增 `research/intraday_quality/features/`。将云端 synthetic prototype 的 8 家族 35 因子作为目录起点，但**逐个重新映射真实 Quote/Observation 字段**，不要复制 synthetic 假字段。

每个因子登记：name/family/formula/unit/window/available_at/missing_policy/source_capability/source_epoch_requirement/mechanism/failure_modes。必须做 unit test 和 future-perturbation test。

优先顺序：A价格 → B量额 → C/D盘口/microprice → E持续压力 → F VWAP → G市场残差 → H质量。若真实 source 没五档或字段单位不稳定，C–E 对该 source 标 blocked/missing，不用 0 填充冒充。

输出 `factor_catalog.csv`、`factor_diagnostics.csv`、`factor_causality.json`。

## 4. WP04：真实因子诊断和粗筛

只在 train/development 时间块做：覆盖、分位数组、单因子 oriented AUC（只作诊断）、相关簇、跨日/跨股票稳定性、family incremental、失败案例。预登记最多 24 个唯一粗筛配置，把**所有** trial 记入 `trials.jsonl`，不删失败项。

质量因子单独 `quality_only` 对照。若 quality-only 很强，先检查是否在学习 source 故障/时间泄漏，不把它直接当 alpha。

## 5. WP05：Tabular 训练基线

待新增 `train_tabular.py`。同候选、同标签、同切分跑：原规则、Logistic、HistGradientBoosting、小 MLP。预处理只 fit train；超参只用 tune；calib 单独；final test 在冻结前不看。

每个模型必须：fit→checkpoint→reload→predict→evaluate；输出 checkpoint hash、逐样本 `predictions.csv`、训练日志、校准输入、推理延迟/峰值内存。

云端 synthetic smoke 的 HGB 比 MLP/TCN 更稳只是流水线先验，不是实盘结论；真实数据先把 HGB 做扎实。

## 6. WP06：因果 TCN，满足数据条件才执行

如果真实原始频率不是 5 秒，不得插值伪造 5 秒信息。用 native frequency 重新计算 TaskSpec/lookback，或明确 blocked。

起始架构：128 step、features+mask+delta_t，6 残差 causal conv block，kernel3，dilation1/2/4/8/16/32，channel32，dropout0.1，参数量上限20万。AdamW lr3e-4，wd1e-4，batch128，最多30epoch、patience5、clip1。

必须检查：小批过拟合、unknown mask、梯度、学习曲线、checkpoint reload一致性、推理延迟/RSS。若真实 fold 无稳定增量，不扩网，保留简单模型。

## 7. WP07：时间隔离、walk-forward、多种子、消融

按整交易日时间顺序 split；实际 lookback + label_end + known_at 做 purge/gap；重叠事件簇不能跨边界。额外做未见股票分组。

候选最多 3 个，seeds 17/29/43，数据允许时 3 个 expanding walk-forward。消融至少：A、A+B、A..F、no-quality、quality-only、full、去盘口、去市场、去KDJ（仅有严格对齐日线KDJ时）、静态 vs sequence。负控制：保持 event/time cluster 的 label permutation + noise factor。

最终报告同时列 base rate、n、股票数、交易日数、事件簇数、unknown 分母。只有人工/独立真值才报检测 Precision/Recall。

## 8. WP08：工程故障矩阵与真实 current coverage

扩展 `tools/live_session.py` round sample，不再只写累计 `quotes/universe`。增加：

```json
{
  "requested": 0,
  "returned": 0,
  "admitted": 0,
  "explicit_unavailable": 0,
  "unknown_missing": 0,
  "stale_rejected": 0,
  "out_of_order_rejected": 0,
  "source_mix": {}
}
```

固定离线故障：重复、乱序、future、soft-partial、source switch、累计量回退、午休、跨日、停牌/零价、SSE queue overflow/reconnect。输出 `event_reconcile.json` 和 `latency_memory.csv`。

## 9. WP09：shadow 接入

待新增 `shadow.py`。只在 replay/离线运行：保留原规则 candidate/event，附加 `quality_score/model_score/model_version/feature_hash/latency_ms/available`。模型超时、缺字段、版本不匹配时 `available=false`，原规则继续。不得把 shadow 结果接真实通知。

## 10. WP10：可恢复编排和交付

待新增统一入口，在实现前不要声称可运行：

```bash
python -m research.intraday_quality.run \
  --stage all --resume --offline \
  --manifest <REAL_MANIFEST> --out <OUT>
```

每阶段写 checkpoint/status，blocked 阶段不阻止独立可执行阶段。生成 `RUN_MANIFEST.json`，记录 baseline SHA、candidate diff hash、dataset/task/split/factor/config hash、seed/fold、机器、命令、开始结束、exit、artifact hash。

## 11. 必须回传给下一轮审计

回传以下真实文件或等价产物：

- git diff（仅本地候选）与 diff SHA256；
- 红/绿/回退验牙测试日志与完整 pytest 日志（若运行）；
- `dataset_manifest.json` / `task_spec.json` / `split_manifest.json`；
- `factor_catalog.csv` / `factor_diagnostics.csv` / causality test；
- `trials.jsonl` / `training_log.jsonl` / checkpoint hash；
- `predictions.csv` / `ablation.csv` / walk-forward / seed stability；
- `event_reconcile.json` / `latency_memory.csv`；
- `RUN_MANIFEST.json` / `NEXT_STEPS.md`。

失败时不要删日志。明确 `completed / failed / blocked / not_run`，并保留下一条可执行命令。不能用“思考很久”“跑了很多模型”代替这些产物。
