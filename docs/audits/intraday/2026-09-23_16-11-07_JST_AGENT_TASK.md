# 本地 Agent 任务书 — 2026-09-23 16:11:07 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`bf0b83bf212f169c720ca8a1c5108405c18a9712`
- 审计开始 docs HEAD：`37988fe8294a034340546dfc55838e2af2bf9885`
- 开工前重新 fetch；docs-only commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取、付费服务。
- 所有 probes 必须显式 FakeSource，禁止意外联网。

## WP01 — SnapshotFetchResult v4.1【P1，第一优先】

### 接口
至少：
```text
source
route
requested_keys
raw_presence_known
raw_returned_requested_keys
quotes
rejected_quality_keys
unexpected_raw_keys
duplicate_raw_keys
provenance
```

### 三源同时实现
- Tencent
- Sina
- Eastmoney

raw identity 必须在 price / prev_close 质量验证之前捕获。

原 `snapshots()` 可兼容：
```text
return detailed.quotes
```

### RED
同一 requested raw-present-unusable fixture：三源都必须得到：
```text
raw presence = present
quality = code
missing = no
```

legacy source：
```text
raw_presence_known=False
```
不得伪装 exact missing。

---

## WP02 — Round Attribution invariant【P2/P1 前置】

新增：
```text
unattributed_requested
```

终态桶：
```text
admitted
unknown_missing
rejected_quality
future
out_of_order
unattributed
```

机械恒等：
```text
Requested = disjoint union(all terminal buckets)
```

参数化场景：normal / true raw empty / raw-present all-quality / future / out-of-order / legacy raw-presence unknown / mixed。

**禁止**：
```text
quotes == [] => all requested are missing
```

---

## WP03 — Empty-round migration

把当前特殊 empty RoundObservationSet 分支改为调用统一 attribution helper。

期望：
- exact raw empty -> all requested missing；
- exact raw-present invalid -> quality；
- legacy unknown -> unattributed；
- 不再出现 requested=5913 / terminal rhs=0。

rollback 后 RED 必须重新失败，且不得靠 ImportError/collection error。

---

## WP04 — live-session

新增采集/汇总：
```text
unattributed_requested
unattributed_total
unattributed_rounds
```

保持：
```text
missing_total = exact provider absence
```

初始 health：unattributed 只 warn/diagnostic，不直接等价 source outage。

---

## WP05 — Eastmoney unknown-total pagination【已有真实 RED/GREEN】

固化 15:52 三臂进仓库测试。

当前：
```text
page2 raw nonempty
page2 quotes empty
→ break before page accounting
```

修：
- 已请求 raw page 先进入 pages/ledger；
- stop 根据 raw/member emptiness；
- pages_requested/pages_ok 与 fetcher 实际调用数一致。

独立小 diff，不夹 Snapshot/Membership 大改。

---

## WP06 — Deterministic correctness gate【P1】

当前 `test_performance_1000_stocks` 同时断言：
```text
len(alerts)==800
elapsed<0.2
```

修改：
- 默认 correctness test 保留语义断言；
- tight wall-clock 移到 explicit non-gating benchmark；
- 不要求新增第三方依赖；
- benchmark 记录 machine/Python/N/warmup/p50/p95/max。

验收：默认 full pytest 同 tree 连续 ≥3 次 exit code 一致。

---

## WP07 — Membership 5923/5553

仅在 WP01-04 绿后开始。transport member 决定 active membership，current Quote usability 独立。

---

## WP08 — SourceManager exact soft-empty

仅当：
```text
requested nonempty
raw_presence_known=True
raw_returned_requested_keys empty
```
才允许 exact soft failure/fallback。

---

## WP09 — Timezone / ObservationInterval / provenance

继续关闭：ZoneInfo + `_now_fn`、fresh-flat interval、per-code source/source_epoch/capability。

---

## WP10 — Research

真实 multi-day manifest 满足门槛后：labels/splits -> factor diagnostics -> rule/Logistic/HGB/MLP -> SSE durable -> TCN。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

snapshot_outcome_red.log
snapshot_outcome_green.log
snapshot_outcome_rollback.log

round_attribution_red.log
round_attribution_green.log
round_attribution_rollback.log

pagination_red.log
pagination_green.log
pagination_rollback.log

correctness_suite_3x.log
performance_benchmark.log

snapshot_outcome_cases.json
round_attribution_cases.json
pagination_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

所有未运行项必须 `not_run/blocked + 原因`。

## 验收失败

- empty list[Quote] 被无条件解释成 exact missing -> FAIL
- built-in source raw-present-invalid 仍因 source 不同落不同 bucket -> FAIL
- requested identity 没有且只有一个 terminal bucket -> FAIL
- default correctness suite 仍含 tight one-shot wall clock -> FAIL
- pagination 仍用 usable Quote empty 判断 raw end-of-pages -> FAIL
- rollback 后 RED 不恢复 -> 测试无探测力，不能算完成
