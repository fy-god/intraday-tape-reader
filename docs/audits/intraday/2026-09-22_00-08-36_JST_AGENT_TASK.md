# 本地 Agent 任务书 — 2026-09-22 00:08:36 JST

## 固定接续

- 仓库：`fy-god/intraday-tape-reader`
- 云端本轮开始 HEAD：`f6efd5fc2e32a0be2a00c85fbada9251f0ac5078`
- 固定产品证据 SHA：`fd51674508cb237c844d6a4924b7e6a2f084c4b6`
- 开放 PR：0
- 注意：`fd516745...` 之后当前只看到 docs-only 提交；开工前必须重新读取实时 main。
- 本轮云端没有 checkout：GitHub connector 可读，但容器 DNS 无法解析 github.com；不要把云端机制 fixture 当成真实 repo pytest。

## 第一目标

本轮不要扩模型。把下面两类已经明确的状态错误做成**真实 checkout 的红测/绿测/回退验牙**：

1. Delivery accounting 已证明的 verdict 接线遗漏；
2. Universe degraded recovery：watchlist fallback 永久 pin + smaller-partial 每轮重刷。

---

## WP01 — H20 Delivery verdict patch【ready，先单独落地】

### 证据
最新云端 addendum 已经实际证明：
- 当前坏账本仍 `healthy=True/exit=0`；
- 新 5 测在原产品 2 fail；
- scratch patch 后 5 pass；
- full suite 1681 pass。

### 修改
`tools/live_session.py::evaluate_health` 增加独立 `delivery_accounting` check：
- `accounting_status=="inconsistent"` 或 `accounting_errors>0` → fail；
- `ok` → ok；
- `not_measured`/missing → 兼容 skip，不判 fail。
打印层同时显示该状态。

### 验收
红 → 绿 → rollback 产品实现 → 红重新出现 → 还原。
不要和 Universe diff 混在同一个产品提交。

---

## WP02 — `IT-P1-UNIVERSE-WATCHLIST-PIN-001`【P1，ready】

### 当前触发链
normal 模式：
```text
全市场 source 全失败
→ _maybe_refresh_universe 退回 watchlist
→ self._codes = watchlist
→ setter 令 _codes_pinned=True
→ 后续自动 refresh 永久短路
```

### 红测
新增 `tests/test_universe_recovery.py`（建议）：

`test_normal_watchlist_fallback_is_recoverable`
- watchlist=10；
- 第一次 universe 全失败；
- 断言 active=watchlist，但**不是 explicit pin**；
- 假时钟 +60s；
- source 第二次给 full；
- 无 force/restart；
- 断言自动恢复 full。

对照：
`test_watch_only_still_explicitly_pins`
必须继续绿。

### 实现原则
不要删除 pin 机制；把：
```text
explicit_pinned
```
与：
```text
watchlist_fallback
```
拆开。

internal fallback 禁止通过通用 setter 设置 explicit pin。

---

## WP03 — `IT-P1-UNIVERSE-REFRESH-STORM-001`【P1，ready】

### 当前触发链
已有 full pool、TTL 过期：
```text
refresh
→ smaller partial candidate
→ 正确拒绝覆盖
→ return 0
→ _universe_refreshed_at 不变
→ 5s 下一轮仍 TTL 过期
→ 再 refresh
```

### 红测
- initial full active=100/5913 均可用 fixture；
- `universe_refresh_seconds=1800`；
- fake time 已让 full 超 TTL；
- source 每次只返回 smaller partial；
- 模拟 120 个 5 秒 poll；
- 修前调用数接近 120；
- 修后按 `universe_retry_seconds=60`，调用数约 10；
- active old full 必须保留。

### 实现
分三个时钟：
```text
last_attempt_at
last_applied_at
last_complete_at
```

smaller partial rejected：
- advance last_attempt；
- do not change active；
- do not advance last_applied/last_complete。

---

## WP04 — Universe Truth & Recovery Contract v2【依赖 WP02/03】

### 状态
```text
unknown
full
partial_degraded
retained_previous
watchlist_fallback
explicit_pinned
```

### schema
```text
refresh_id
observed_at
source
mode
expected_total
raw_unique_codes
usable_quotes
candidate_codes
active_scan_codes
transport_complete
transport_coverage
usable_coverage
active_coverage
candidate_applied
reason
last_attempt_at
last_applied_at
last_complete_at
```

### 不变量
- 只有 explicit_pinned 阻止 auto refresh；
- expected_total unknown → active_coverage=None；
- candidate 与 active 分开；
- round returned/requested 不能替代 active market coverage。

---

## WP05 — status/live-session + 真实测量

### Store/status
当前：
```text
status.universe = len(state.quotes)
```
它是 latest cache，不是 active universe。

新增：
```text
latest_quote_cache_size
active_scan_universe
universe_truth
```

旧 `universe` 若保留必须 deprecated。

### live-session
同时汇总：
```text
transport_coverage
usable_coverage
active_coverage
round_coverage
universe_mode
age_since_last_complete
```

### 真实覆盖率
只在授权本地环境跑。必须生成：
`universe_coverage_reconcile.json`

至少：
```text
timestamp
source
request/config
raw response hash 或完整可复算 manifest
expected_total
raw_unique_codes
usable_quotes
active_scan_codes
transport_complete
```

旧 `73.0%/83.1%` 已作废，禁止再引用。

---

## WP06 — per-code provenance/capability

建立：
```text
(route, code)
→ source
→ source_epoch
→ advertised capabilities
→ observed field availability
```

---

## WP07 — targeted soft-partial fallback

只能在 WP06 后做。

只补：
`unknown_missing`

不无限补：
`explicit_unavailable / suspension / quality reject`

---

## WP08 — ObservationInterval

建立：
```text
first_observed_at
last_observed_at
price
cum_volume
source_epoch
```

fresh-flat 延长区间，真实断流不延长。

---

## WP09 — SSE durable delivery / bounded store

完成：
```text
committed -> sent -> received -> applied
```
cursor/gap；慢客户端不能静默 drop-oldest。

---

## WP10 — 真实标签与模型

只有以下全部满足才恢复模型：
- UniverseTruth 可复核；
- committed event_id 稳定；
- 连续多日真实数据；
- future label known_at 合同；
- time-forward split。

先固定简单模型，再决定是否值得 TCN；不要预设神经网络胜出。

---

## 本轮必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

wp01_delivery_gate_red.log
wp01_delivery_gate_green.log
wp01_delivery_gate_rollback.log

universe_watchlist_recovery_red.log
universe_watchlist_recovery_green.log
universe_watchlist_recovery_rollback.log

universe_partial_backoff_red.log
universe_partial_backoff_green.log
universe_partial_backoff_rollback.log

universe_recovery_cases.json
universe_truth_cases.json
universe_coverage_reconcile.json（若真实测量完成）

full_pytest.log
check_*.py logs
dash_render_check.log

RUN_MANIFEST.json
NEXT_STEPS.md
```

## 完成标准

- normal watchlist fallback 能自动恢复 full；
- `--watch-only` 与 replay injected codes 仍 explicit pin；
- smaller partial rejected 后不再每 5 秒重刷；
- active old full 不被 smaller partial 覆盖；
- expected_total unknown 不伪造 coverage；
- rollback 后相应 RED 必须重新出现；
- 真实测量若没跑，明确 `not_run`，不能填 0；
- 不启动真实通知、交易、自动 push、Actions 或付费服务。
