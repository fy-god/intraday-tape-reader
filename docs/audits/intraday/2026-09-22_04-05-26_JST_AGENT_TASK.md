# 本地 Agent 任务书 — 2026-09-22 04:05:26 JST

## 基线与范围

- 仓库：`fy-god/intraday-tape-reader`
- 当前云端固定产品证据：`e57bb3b87baacefd4e8abd908a5bc17877179ebf`
- 开工前必须重新读取实时 `main`；只把真正改 `src/tests/tools/config` 的提交视为产品升级。
- 云端当前开放 PR：0。
- 不修改 Actions，不真实通知、不交易、不自动 push、不持续抓取。

## WP01 — zero-return 必须成为真实 Round Observation【P1，第一优先】

### RED
真实 `Engine` + FakeSource + 假时钟：
1. 先跑一轮正常行情，让 `state.quotes` 有缓存；
2. 下一轮股票 request 确实 dispatch；
3. provider 返回 `[]`，不抛异常；
4. 断言当前产品：
   - `observation_seq` 不增长；
   - 新账本不存在/仍是上一轮；
   - `returned=0 / coverage=0` 无法被本轮读取。

### GREEN
修改 `Engine.poll_once()`：
- zero-return early-return 之前构造并提交本轮 `RoundObservationSet`；
- `requested>0 / returned=0 / coverage=0`；
- 推进 observation identity；
- 不把旧 state.quotes 填进当前轮。

### 负控制
- closed/idle 未 dispatch：不能算 outage；
- provider exception：仍走 error path；
- 正常返回：既有账本不回归。

### rollback tooth
回退产品实现后上述 RED 必须重新失败；无 ImportError/收集期错误。

---

## WP02 — live-session 不得再把 latest cache 当本轮数据【P1】

当前 `_soak_loop()`：
`quotes=len(state.quotes)`。

修成：
- observation present → `quotes = returned`（或明确命名 `round_returned`）；
- `latest_quote_cache_size` 另存诊断；
- `no_data_rounds` 只由 current-round truth 驱动。

### 端到端验收
连续 6 轮 silent `[]`：
- `no_data_rounds == 6`
- `coverage_p05/p50/min == 0`
- verdict `healthy=False`
- `fail` 至少含 `fetch/data/coverage`

正常 5908/5913：
- 仍健康。

---

## WP03 — R-21 Universe health gate【已有 scratch 证据，直接落地】

不要重新设计。
最新云端已证明：
- setup 已有 `universe_size/fell_back`;
- evaluate_health 不读；
- scratch patch 34 tests 零回归。

要求：
- 加 `universe` 判决项；
- 用会话 observed max 做当前相对基线，不发明绝对 A 股总数；
- `universe_size=0` / 无 setup → not_measured；
- 脏 setup 不崩；
- 后续 UniverseTruth 再替换这个过渡 gate。

---

## WP04 — partial universe retry/backoff【P1】

RED：
- full pool 已过 1800s TTL；
- 后续一直 smaller partial；
- 模拟 120 × 5s poll；
- 当前 refresh 约 120 次。

实现：
- `last_attempt_at`
- `last_applied_at`
- `last_complete_at`
- `poll.universe_retry_seconds`（起点 60s，配置化）

smaller partial rejected：
- 更新 last_attempt；
- active pool 不变；
- last_complete 不伪造。

---

## WP05 — ACK 有序裁剪【P2】

当前 `set(list(set)[-2000:])` 不表示最新 2000。

改用有序 bounded 结构。
RED/GREEN：
- 写入 5001 keys；
- 最新 2000 必须保留；
- 最新 key 必须 acked=True；
- 最旧 key 被淘汰；
- 多 hash seed 不影响语义。

---

## WP06 — Delivery verdict 脏值硬化【P2】

- truthy scalar `inconsistent_rounds` 不能让 `evaluate_health` 崩；
- 保持 shipped list producer 正常；
- 可增加防御式 `named<=total` 复算，但不要把 `not_measured` 误判成 fail。

---

## WP07 — UniverseTruth / status

正式拆：
- `latest_quote_cache_size`
- `active_scan_universe`
- `universe_truth`

以及：
- expected/raw/usable/active
- full/partial/retained/watchlist/pinned
- last_attempt/applied/complete

---

## WP08 — per-code provenance + targeted fallback

先做 `(route, code) -> source/source_epoch/capability/observed-fields`。
之后才只对 `unknown_missing` 做 targeted fallback。

---

## WP09 — ObservationInterval + SSE durable delivery

- fresh-flat vs true gap；
- committed→sent→received→applied；
- cursor/gap；
- 慢客户端不可静默 drop-oldest。

---

## WP10 — 真实标签 / 模型

前置全部满足后才恢复：
rule / Logistic / HGB / MLP / TCN。

必须使用真实多日 manifest、known_at、time-forward split；TCN 无稳定增量就保留简单模型。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

round_truth_zero_return_red.log
round_truth_zero_return_green.log
round_truth_zero_return_rollback.log

silent_empty_soak_red.log
silent_empty_soak_green.log
silent_empty_soak_rollback.log

universe_health_gate_red.log
universe_health_gate_green.log
universe_partial_backoff_red.log
universe_partial_backoff_green.log
ack_recency_red.log
ack_recency_green.log

round_truth_cases.json
universe_health_gate_cases.json
universe_partial_backoff_cases.json
ack_recency_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log
RUN_MANIFEST.json
NEXT_STEPS.md
```

任何未运行项必须标 `not_run/blocked + 原因`，禁止填 0 或挪用旧结果。
