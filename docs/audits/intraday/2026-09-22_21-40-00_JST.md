
# 盘中实盘预警系统 · 轮审报告

- `audit_time_jst`: `2026-09-22 21:40 JST`（本机 CST 20:40）
- `reviewed_repo`: `fy-god/intraday-tape-reader`
- `reviewed_source_sha`: `bf0b83bf212f169c720ca8a1c5108405c18a9712`（产品源码，**本轮新提交**）
- `main_head_at_audit_start`: `bf0b83bf212f169c720ca8a1c5108405c18a9712`
- `previous_report`: `docs/audits/intraday/2026-09-22_21-00-00_JST.md`（`ba52e10`）
- `report_created_commit_sha`: 见本报告提交后回读
- `evidence_type`: **实股/实盘工程**（无软件样本结果）；`execution_status=COMPLETED`；`research_verdict=NO_NEW_RESEARCH_RESULT`（纯工程审计）

---

## §0 本轮一句话

**`bf0b83b` 的三项"假绿"修复是真修**——我用**同一批输入跑修前/修后两棵树**做了对照，三项**全部可判别**，且**没有造出新的假红**。
**但修复在"肯定句"那一支留下了同类的残余假绿**：分母用了**只含带证据轮次的子集**，于是 **30 轮里只有 1 轮有扫描范围证据时，仍输出「全程全市场扫描，1/1 轮」**。

---

## §1 已确认错误（本轮新发现，全部我亲自复现）

### 1.1 `IT-P2-UNIVERSE-SCOPE-POSITIVE-USES-EVIDENCED-SUBSET-AS-DENOMINATOR-001`（**P1**）〔**已确认错误 · 未修**〕

- **文件/函数/行号**：
  - 生产者 `tools/live_session.py:552-565` `_universe_truth_now`（`except Exception: return None`）
  - 采样守卫 `tools/live_session.py:438` `if isinstance(universe_truth, dict) and universe_truth:`
  - 聚合 `tools/live_session.py:609-612`（老轮样本无该键 -> **不计入**）、`:663` `scope_measured = sum(scope_counts.values())`
  - 判决 `tools/live_session.py:1737` `elif _scope_measured > 0 and _full_rounds == _scope_measured:`
  - 消费 `tools/live_session.py:1698-1701`（**只读四个 scope 字段，不读总数**）
- **触发条件**：一次 soak 中**部分轮次**没有 `universe_scope_state` 键，而**所有带键的轮次**都是 `full_market`。
  产生"没有键"的现实路径是 `_universe_truth_now` 吞掉 `engine.universe_truth()` 的异常（该函数注释自称"**绝不抛**"，说明抛是被预期的情况）。
  **该异常本轮实测可达**：`shortfall` 为非数字时 `engine.universe_truth()` 抛
  `ValueError: invalid literal for int() with base 10: 'zz'`（`engine.py:997` `int(meta.get("shortfall") or 0)`）。
- **机制**：`scope_measured_rounds` 是**带证据轮次的数量**，不是会话轮次总数；`:609` 注释明确"老轮样本没有这个键 -> **不计入**"。
  肯定分支把 `full_market_rounds` 与 `scope_measured_rounds` 相比 —— 这是**子集与自身相比**，恒等成立。
  会话轮次总数**就在同一份聚合里**（`:687` `"rounds": len(rows)`），但该分支**从不读它**：
  全树 `_sess.get("rounds")` 出现 **0** 次。
- **影响（实测，同一批输入双树对照）**：

  | 输入（30 轮） | 修前 `universe_scope` | 修后 `universe_scope` |
  |---|---|---|
  | 30/30 有证据、全 `full_market` | ok /「未降级…（**全程全市场扫描**）」 | ok /「…30/30 轮有明确扫描范围证据」 |
  | **1/30** 有证据（其余无键）、该 1 轮 `full_market` | ok /「全程全市场扫描」 | **ok /「全程全市场扫描，1/1 轮有明确扫描范围证据」** |
  | **5/30** 有证据 | ok /「全程全市场扫描」 | **ok /「…5/5 轮…」** |
  | **15/30** 有证据 | ok /「全程全市场扫描」 | **ok /「…15/15 轮…」** |
  | 0/30 有证据 | ok /「全程全市场扫描」 | ok /「证据**不足**（测到 0 轮）…**不能**据此说…」 |

  即：修复确实堵住了 **0/30**（这是它声称修的那个洞），但 **1/30、5/30、15/30 仍在说"全程"**。
  聚合里 `measured=True`、`rounds=30`，而文案写 `1/1`，**同一份事实自相矛盾**。
- **与作者自述合同冲突**：作者报告 §1.1（`2026-09-22_21-00-00_JST.md:97`）写
  「**只有**「测到了、且**每一轮**都有明确扫描范围证据」才允许肯定句」。
  实测代码的"每一轮"= **每一轮有证据的轮次**，不是**每一轮**。是**实现低于它自己的合同**。
- **修复建议**：肯定分支的分母改用**会话轮次总数**，并把未测量轮次显式排除：
  `elif _total_rounds > 0 and _scope_measured == _total_rounds and _full_rounds == _scope_measured:`，
  其中 `_total_rounds = _safe_int(_sess.get("rounds"))`（字段已存在，`live_session.py:687`）；
  文案改为「全程全市场扫描（**{_full_rounds}/{_total_rounds}** 轮有明确扫描范围证据）」。
- **验收测试**（两条，缺一不可）：
  1. **正向**：30 轮里 **1** 轮带 `universe_scope_state=full_market`、其余 29 轮**无该键**，
     聚合后 `evaluate_health` 的 `universe_scope` **不得**再出现肯定句，`detail` 必须说明证据覆盖不足；
  2. **负向对照**：30/30 带证据且全 `full_market` 时**仍必须**输出肯定句（防止把正例一起打死）。
  仅加 1 号会把一个"有证据也不肯说"的假红引进来。

### 1.2 三处**只写不读**的新字段〔**已确认错误 · 未修**（P3）〕

| 字段 | 写入点 | 读者 |
|---|---|---|
| `coverage_active_denominator_kinds` | `live_session.py:679` | **0** |
| `coverage_active_p05` | `live_session.py:676` | **0** |
| `coverage_active_last` | `live_session.py:678` | **0** |

另有 `scope_counts`（`:665` 写入）**无 `.get` 读者**，仅被 `:667-669` 立即消费派生量。
全树 grep（`D:\it_h20_tip`，排除 `fixtures/`）确认读者数为 0。
**影响**：`coverage_active_min` 被判决读取（`:1852`），但 `p05`/`last` 与**分母种类**只进 JSON 不进判决 ——
与本仓库反复出现的"算了没人读"同类（作者自己在 `:1846-1847` 把它当第 6 次来数）。
**修复建议**：要么接进判决（`p05` 做稳健下界、`denominator_kinds` 做"是否可跨轮比较"的门），要么从产物里删掉，别留假的可观测性。
**验收测试**：对三个字段各写一条"改变它 -> verdict 或产物必须变化"的断言；否则删除字段。

---

## §2 对 `bf0b83b` 三项修复的**独立确认**（写清楚哪些是真修的）

**方法**：`bf0b83b^` = `7b0404a8e8d1a3ea56b1498ad8650b2bfa75acae`（修前）与 `bf0b83b`（修后）分别 `git archive` 成独立树，
**同一批输入**各跑一遍 `evaluate_health`。**修前/修后必须给出不同结论**才算验证（否则测试无判别力）。

| # | 作者声称修的问题 | 修前（实测） | 修后（实测） | 判定 |
|---|---|---|---|---|
| 1 | 零证据被肯定成"全程全市场扫描" | `finalize_metrics([])` -> ok /「全程全市场扫描」 | ok /「证据**不足**（测到 0 轮）…**不能**据此说…」 | **真修** |
| 2 | 空扫描与全市场不可区分 | 30/30 `active=0` -> **ok** /「全程全市场扫描」 | **fail** /「30 轮**活跃股票池为空**…」 | **真修** |
| 3 | 会话内覆盖率下降无消费者 | t0 98.33% + 会话 75% -> **ok**（只报 t0） | **fail** /「**会话内最差** 75.00% < t0 98.33%…」 | **真修** |
| 4 | t0 `transport_complete=False` 被会话"全完整"盖掉 | `measured=1` 就翻 **ok** | 恒 **fail**（`measured=0/1/30` 全 fail） | **真修** |

第 4 项我复现了作者自述的"**一个单位就翻绿**"：修前 `rounds_transport_measured` 从 0 改到 1，
`universe_transport` 即由 fail 变 ok —— 作者的负控制确实**绕过了被测分支**，其自我批评**成立**。

---

## §3 被我自己否证/降级的假设（不得作为发现上报）

1. **"修复造出新的假红" —— 未复现（降级为潜在风险）**。
   我怀疑空扫描硬 fail 会在**休市/冷启动**误伤。实测反证：
   - 7 份真实归档 `data/live_session_*.json` 中，**休市的 5 份** `metrics.universe.min` 分别为
     **5563 / 5563 / 5563 / 5563 / 5563**，**没有一份是 0**；交易中的 1 份为 4800。
   - `engine.py:1203` 注释与 `:1320` 行为：**所有来源都失败时保留原股票池，"绝不清空"**。
   ⇒ 空池**不会**由休市或取数失败产生，硬 fail 打中的是真实故障。**假设不成立，撤回。**
2. **"会话覆盖聚合忽略分母种类" —— 未复现（机制存在但不可达）**。
   确有其事：`coverage_active_denominator_kinds` 被记录（`:679`）却**无读者**（见 §1.2），
   而 min 聚合（`:615-618`）**不看**分母种类，与 t0 分支"分母未知 -> 判未测量"（`:1875-1880`）**口径不一致**。
   但**可达性为 0**：穷举驱动真实生产者 `engine.universe_truth()` 的 45 组 meta 组合，
   **18 组产生数值 coverage，其中分母种类非 `provider_declared_total` 的为 0 组**
   （`engine.py:960-1001` 构造上保证：分母为 0 时 `_ratio` 返回 `None`）。
   ⇒ 只作为 §1.2 的死字段上报，**不作为独立缺陷**。
3. **我自己的探针错了一次**：最初用朴素子串 `"全程全市场扫描" in detail` 判断"是否肯定"，
   在修后树上得到 `True`，看似修复失败。**实为假阳性** —— 诚实文案把该短语写在**否定句**里
   （「**不能**据此说'全程全市场扫描'」）。仓库自己的 `_asserts_full_market`
   （`tests/test_health_consumer_contract.py:395-405`，排除 `不能`/`不足`）已经处理了这一点。
   改用该谓词后结论反转。**教训：匹配错了的过滤器什么也证明不了。**

---

## §4 实际跑过的命令 / 退出码 / 真数

| 命令 | 退出码 | 真数 |
|---|---|---|
| `pytest -o addopts= -ra -p no:cacheprovider`（`bf0b83b` 纯净树 `D:\it_h20_tip`） | **0** | **1802 passed in 120.05s** |
| 同上（独立子 agent 在 `%TEMP%` 副本重跑） | **0** | **1802 passed in 121.29s** |
| 修前/修后双树 `evaluate_health` 对照（4 场景） | 0 | 见 §2 表（3 项判别 + 1 项恒 fail） |
| 残余假绿探针（1/5/15/30 有证据 of 30 轮） | 0 | 见 §1.1 表 |
| 真实归档 `data/live_session_*.json` 普查 | 0 | **7** 份；`universe_session` 键 **0/7**；`universe.min` = 5563×5、4800、5564 |
| `engine.universe_truth()` meta 穷举（45 组） | 0 | 数值 coverage **18** 组；非 provider 分母 **0** 组 |
| `engine.universe_truth()` 异常可达性 | 0 | `shortfall='zz'` -> **`ValueError`**（故 `_universe_truth_now` 的吞异常路径可达） |
| 死字段读者普查（15 字段） | 0 | 3 个写后零读者 |

**未跑及原因**：
- **未跑真实 soak**（需行情服务与交易时段）—— 不伪造通过数；
- **未跑浏览器/看板渲染** —— 与本轮结论无关；
- **本仓库没有 `docs/audits/validate_latest.py`**（`Test-Path=False`）⇒ 手册步骤 5 的校验器**不适用**，**不声称任何 gate PASS**；
- **未实施任何修复**（见 §6 边界）：本审计线只读产品代码、只上传审计文档与索引。

---

## §5 三轴状态 + 分开写三件事

- `execution_status`：**COMPLETED**（双树对照 + 全套测试实跑）。
- `research_verdict`：**NO_NEW_RESEARCH_RESULT**（纯工程审计，无实股收益结论）。
- `evidence_status`：**VERIFIED**（真实命令、真实退出码、真实计数）。
- `evidence_type`：**实盘工程**；**无软件样本结论**，两者不合并计数。

- **程序修复**：§1.1 的分母修复 + §1.2 三字段取舍（**均未实施**，超出本线边界）。
- **任务定义变更**：无。
- **真实模型增益**：无 —— 本轮不含任何收益主张。

---

## §6 有依据的下一步（1–3 条）

1. **先修 §1.1 的分母**（依据 §1.1 实测表）：它使"全程"二字在最需要它的**间歇性故障**场景下失真，
   而间歇性正是本线存在的理由；修完必须同时加**正例对照**防止变成假红。
2. **给 `_universe_truth_now` 的吞异常留痕**（依据 §1.1 触发链）：当前异常与"引擎压根没这个属性"**不可区分**，
   是残余假绿的入口；建议计数并进产物，判决层据此区分"未测"与"取数失败"。
3. **把三个死字段接进判决或删掉**（依据 §1.2）：本仓库已第 6 次出现"算了没人读"，继续累积会让可观测性变成噪声。

## §7 边界与合规

只读产品代码与 GitHub 审计文档；**未改任何产品源码**、未合并 PR、未建/改 Actions、
**未创建或修改任何定时任务**、未启动行情服务；探针脚本写在 `$env:TEMP` 与本地暂存区，
**未落进仓库**；GitHub 侧只新增本报告并串行更新 `LATEST.md`；未 force push、未推送整条本地分支。
`data/live_session_*.json` 为**只读**，未改动。
