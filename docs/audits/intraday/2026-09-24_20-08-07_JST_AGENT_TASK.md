# 本地 Agent 任务书 — 2026-09-24 20:08:07 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`9670dd97169a4d9c4e5021d5fbfc4f1a4804cfdb`
- audit-start main：`89eec3e89305037874afebbdabdeb96cb2543ce6`
- 开工前重新 fetch；docs-only commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取/付费服务。

## WP01 — Sina index Quote canonical identity【P1】

目标：
- `src/arad/sources/sina.py`
- `tests/test_sina_index_identity_no_wrong_instrument.py`
- 可新增 `tests/test_sina_index_quote_identity.py`

RED：
```text
响应 var hq_str_sh000001=...
snapshots(["sh000001"])
```

必须：
```text
Quote.code == "sh000001"
Quote.board == INDEX
```

股票控制：
```text
sz000001 -> Quote.code="000001"
sh600000 -> Quote.code="600000"
```

实现必须让 parser 知道 caller-owned route/request role，不能单靠 name 猜 identity。

Rollback：
恢复 `code = symbol[2:]` 后 index test 必须行为 RED。

---

## WP02 — Sina detailed R/P/Q identity【P1 前置】

当前 `test_detailed_identity_axis_keeps_index_prefix` 只测数量，不够。

新增精确断言：
```text
requested_keys == {"sh000001"}
raw_returned_requested_keys == {"sh000001"}
admitted_keys == {"sh000001"}
quotes[0].code == "sh000001"
```

wrong symbol `sz000001` 响应：
- admitted=0
- sh000001 必须可观测为 missing
- 不得 exact-but-wrong。

Mutation：
把 R/P/Q 再改成 bare `000001`，测试必须 RED。

---

## WP03 — Cross-source canonical identity

对同一 index fixture：
- Tencent
- Sina
- Tencent fail -> Sina backup

必须：
```text
Quote.code
requested key
raw returned key
admitted key
```
全部一致。

至少覆盖默认 5 个 index_codes。

---

## WP04 — EngineState core identity regression

预载股票：
```text
history["000001"]
quotes["000001"]
day_open["000001"]
first_seen["000001"]
```

再准入指数：
```text
sh000001
```

必须：
- 股票各 map 不变；
- 指数独立存在于 `sh000001`；
- 不再共享 price/history/day_open/first_seen。

完成 Source identity 后再判断是否还需要把 core maps 改 `(route,code)`；
不要先大改 State。

---

## WP05 — SpiritIndex false-alert negative control

预载 5 分钟平安银行历史。
第一条上证指数样本进入。

必须：
- `ctx.state.window("sh000001")` 不够 5 分钟；
- 不得产生 synthetic index_pull/index_press。

阳性对照：
真正积累足够 index history 后规则仍可正常触发。

---

## WP06 — RoundObservation identity consistency

请求：
```text
sh000001
```

若：
```text
index returned=1
index admitted=1
```

则：
```text
unknown_missing
```
不得包含 `sh000001`。

加入机械 identity invariant。

---

## WP07 — Production detailed wiring

只有 WP01-WP06 绿后：
- stocks route -> call_detailed
- index route -> call_detailed
- online Round ledger 消费 exact raw presence
- legacy fallback 明确 raw_presence_known=False
- 不使用 deprecated generic coverage。

---

## WP08 — Per-route Health / Provenance

保留上一轮：
- index 独立 raw/usable coverage
- index source
- mixed source provenance
- index 0/5 不得被 stock-heavy aggregate 覆盖。

---

## WP09 — Membership / exact soft-empty / current live soak

先 Membership 与 soft-empty。
之后在真实交易时段仅 dry-run/read-only 做当前版本 soak：
- 不真实通知；
- 不交易；
- 不持续后台跑；
- 记录 current source/route/E2E latency。

---

## WP10 — Research

事实链稳定 + 真实多日数据后：
manifest -> labels/splits -> factor diagnostics -> Rule/Logistic/HGB/MLP -> SSE -> TCN。

## 已修项只做回归

不要重新设计：
- StateUpdateResult route-local future/ooo；
- route-key time diagnostics；
- Eastmoney no-total pagination；
- Sina wire-symbol strict filter。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

sina_quote_identity_red.log
sina_quote_identity_green.log
sina_quote_identity_rollback.log

sina_detailed_prefix_red.log
sina_detailed_prefix_green.log
sina_detailed_prefix_rollback.log

cross_source_identity_red.log
cross_source_identity_green.log
cross_source_identity_rollback.log

engine_state_identity_red.log
engine_state_identity_green.log
engine_state_identity_rollback.log

spirit_index_collision_red.log
spirit_index_collision_green.log
spirit_index_collision_rollback.log

round_identity_red.log
round_identity_green.log
round_identity_rollback.log

identity_cases.json
cross_source_cases.json
round_identity_cases.json

full_pytest.log
check_*.py logs
dash_render_check.log
selftest.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须 `not_run/blocked + 原因`。
