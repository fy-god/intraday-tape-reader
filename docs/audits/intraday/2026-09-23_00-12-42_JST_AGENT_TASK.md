# 本地 Agent 任务书 — 2026-09-23 00:12:42 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- reviewed product SHA：`bf0b83bf212f169c720ca8a1c5108405c18a9712`
- 当前 docs HEAD 在它之后只有审计文档；开工前必须重新核对 main。
- 不真实通知、不交易、不自动 push、不改 Actions、不持续抓取、不租卡。
- 本轮最高目标是 Evidence Completeness；不要再重复修 bf0 已关闭的 t0-hard-negative / mid-session numeric coverage 98→75。

## WP01 — Universe Evidence Completeness v2【第一优先】

### RED A：明确 0 != missing
```text
setup.universe_size=None -> not_measured
setup.universe_size=0    -> FAIL
```
denominator unknown 时也必须 FAIL。

### RED B：session absolute shrink
```text
t0 universe_size=5000
setup denominator unknown
30 轮中 active min=2000
```
当前 absolute gate 只读 t0；目标必须 FAIL。

### RED C：session coverage without t0 truth
```text
setup.universe_truth=None
session coverage_active_min=0.75
coverage_active_measured_rounds=30
```
目标 `universe_coverage=FAIL`。

### RED D：freshness time denominator
```text
total rounds=30
freshness evidence=1 round
state fresh
```
不得输出“会话期间股票池新鲜”这种全称肯定。
30/30 fresh 才允许。

### RED E：scope magnitude
```text
30/30 active_scan_codes=3000
expected_total=None
```
不得输出 `full_market` / “全程全市场扫描”。
分母未知只能 `broad_scan_unquantified` 或等价弱状态。

### GREEN 设计
- `rounds_total`
- `freshness_measured_rounds`
- `scope_measured_rounds`
- `coverage_active_measured_rounds`
- `universe_min_active_codes`
- `SCOPE_BROAD_UNQUANTIFIED`
- absolute / coverage session facts 必须独立于 t0 truth block 消费

### rollback tooth
逐项回退被改产品实现，RED 必须重新变红；无 collection/import error。

---

## WP02 — Universe membership / Quote usability【P1，独立 diff】

禁止和 WP01 混 patch。

provider replay：
```text
transport codes=100
usable Quote=94
transport_complete=true
```
目标：
```text
active membership=100
usable coverage=94%
```

6 个不可用 code 在 snapshot 阶段判 suspension/zero/unavailable/missing。

---

## WP03 — SourceManager soft-empty snapshots failover【P1】

```text
requested codes 非空
primary.snapshots -> []
backup.snapshots -> valid
```
backup 必须被尝试。

负控制：
- caller no-dispatch 不进入 SourceManager；
- primary raises 既有 fallback 不回归；
- universe empty 不套 snapshots policy。

---

## WP04 — TradingCalendar timezone【P1】

- `app.timezone` -> `ZoneInfo`
- `_now_fn` 真正成为测试/clock injection 入口
- same epoch host UTC/CST -> same phase/is_open
- replay naive/aware contract 明确

---

## WP05 — ObservationInterval【P1】

- fresh-flat 延长 interval
- true gap 不延长
- source epoch change 切 interval
- cutoff 落 interval 内可取合法 anchor
- bounded memory

---

## WP06 — per-code provenance/capability

完成 `(route,code)->source/source_epoch/capability/observed fields`。

---

## WP07 — Research software chain

`research/intraday_quality/`：
dataset / labels / splits / factor registry / factor_eval。

先做因果性、missing、capability、train-only scaling 单测。
无真实数据就 blocked，禁止制造成绩。

---

## WP08 — Factor diagnostics【数据满足后】

单因子、相关簇、逐家族增量、quality-only、失败案例、完整 trial ledger。

---

## WP09 — Tabular models【真实数据满足后】

rule / Logistic / HGB / small MLP。
全部 fit→checkpoint→reload→predict→evaluate。

---

## WP10 — SSE durable + TCN / shadow

先完成 committed→sent→received→applied、cursor/gap 与 bounded state。
再跑 TCN；time-forward 无稳定增量则淘汰。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

evidence_zero_red.log
evidence_zero_green.log
session_abs_red.log
session_abs_green.log
coverage_without_t0_red.log
coverage_without_t0_green.log
freshness_denominator_red.log
freshness_denominator_green.log
scope_magnitude_red.log
scope_magnitude_green.log
wp01_rollback.log

universe_evidence_cases.json
membership_cases.json
source_empty_cases.json
timezone_cases.json
observation_interval_cases.json
per_code_provenance.json

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md

full_pytest.log
check_*.py logs
dash_render_check.log
```

未适用项必须 `not_run/blocked + 原因`，禁止伪造 0 成绩。
