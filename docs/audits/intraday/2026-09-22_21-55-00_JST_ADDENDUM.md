
# 盘中实盘预警系统 · 轮审报告（**追加**：回归核对表）

- `audit_time_jst`: `2026-09-22 21:55 JST`（本机 CST 20:55）
- `publication_kind`: `REGRESSION_LEDGER_ADDENDUM`
- `reviewed_repo`: `fy-god/intraday-tape-reader`
- `reviewed_source_sha`: `bf0b83bf212f169c720ca8a1c5108405c18a9712`
- `main_head_at_append_start`: `99934d619cfb30e0ac4aa90f59393c39ab62b4f6`
- `parent_report`: `docs/audits/intraday/2026-09-22_21-40-00_JST.md`（`99934d61`）
- 三轴：`execution_status=COMPLETED`；`research_verdict=NO_NEW_RESEARCH_RESULT`；`evidence_status=VERIFIED`

---

## §0 为什么追加

`2026-09-22_21-40-00_JST.md` 只审了 `bf0b83b` 的**修复本身**。轮转手册 §4 的**基线表仍把 6 条记为"未修"**，
本轮逐条重取真值后发现 **3 条已经修好** —— 基线**已过期**，必须更正，否则后续轮次会去"回归"根本不存在的问题。

**8 条结论全部经我独立复核后才写入**（不采信子 agent 单一来源）：阅读同一份纯净树 `bf0b83b` 的实际代码行，
逐条重跑 grep / 运行探针。**无一条与子 agent 结论相左**。

---

## §1 已修（**基线仍记为"未修"，本轮更正**）

| ID | 当前证据（`bf0b83b`，逐行复核） | 实测 |
|---|---|---|
| `IT-P0-001`（收盘集合竞价） | `src/arad/session.py:46` 新增 `CLOSE_AUCTION`；`:87` `_T_CLOSE_AUCTION_START = time(14,57)`；`:156/:159` 14:57–15:00 返回 `CLOSE_AUCTION`；`:67` **注释明确 CONTINUOUS 故意不含它**；`:75` `CALL_AUCTIONS` 与 `:80` `OBSERVABLE` 均含它；`engine.py:1437` 按 `OBSERVABLE` 放行 | **14:56:59 → afternoon / is_open=True**；**14:57:00、14:58:00、14:59:59 → close_auction / is_open=False / observable=True**；**15:00:00 → post**；`elapsed_trading_seconds(14:58)=14220.0` |
| `IT-P1-LIMIT-001`（贴限价 vs 封单达标混同） | `src/arad/rules/limit_board.py:210` `qualified = self._seal_qualified(q, rising)` 移到 `:211` 的 `if prev=="sealed" and qualified` **之前**；`:217` 不达标写 `at_limit_unqualified`；`:274` 门槛唯一来源 `_seal_qualified`；`:320` 兜底二次校验 | 同一规则实例、门槛 200 万：**第 1 轮 100 万 → `[]` / 状态 `at_limit_unqualified`**；**第 2 轮 300 万 → 1 条 `limit_up` / 状态 `sealed`** |
| `IT-P2-OBS-001`（累计缓存冒充本轮覆盖） | `tools/live_session.py:247` 新增 `_round_quote_count(obs, state)`，`:2883` 用它替换 `len(state.quotes)`；`src/arad/capabilities.py:476` + `store.py:155-164` `set_poll_stats(observation=)` 携带 `observation_seq` | 累计缓存 5000 vs 本轮 450：`_round_quote_count(soft-partial 500/450)=450`；`make_round_sample` 得 `quotes=450 / returned=450 / coverage=0.9 / missing=50` 与 9 个 observation 字段。**仅在 obs 缺失时才退回 `len(state.quotes)`**（代码注明是"退化"，不是正确值） |

**这三条的"已修"判定我要强调一点**：它们**不是**我这轮动手改的 —— 是**别人（产品线）在他自己的提交里修的**，
本线只做**核对与更正**。按手册"程序修复 / 任务定义变更 / 真实模型增益"三分，这三条属**别人已完成的程序修复**，
本线**没有**贡献代码。

---

## §2 仍然未修（**基线仍有效**，附本轮重取的行号）

| ID | 当前状态 |
|---|---|
| `IT-P0-002`（时区）**P1** | `session.py:100` `_now_fn: object = None` 是全文件**唯一**一次出现（`self._now_fn` = **0** 次）⇒ **死参数**；裸 `datetime.now()` **6** 处（`:122,142,214,232,242,252`）；全树 `ZoneInfo\|zoneinfo\|pytz\|utcnow` = **0** 命中 |
| `IT-P1-SOURCE-EMPTY-001` | `engine.py:547-582` 的成功判据仍是"**有没有抛异常**"（`:571` `self._serving[route]=i`、`:572-573` 重置失败计数、`:582` 返回），**从不检查返回条数**。实测：soft-partial（请求 2 得 1，且备用源已注册）→ **备用源调用 0 次**；全空（请求 3 得 0）→ **备用源调用 0 次**、失败计数 0 |
| `IT-P1-WINDOW-001` | `engine.py:313` change-only 追加条件仍在；`tick_surge.py:197-198` 的 `_covered` 仍只看 `hist[0][0]`。全树 `ObservationInterval` **85** 命中，`src/`=`tests/`=`tools/`=**0**（纯粹是文档在提它） |
| `active_snapshot["epoch"]` 占位 | `engine.py:1049` 是全树**唯一**代码出现，且是**读者** `int(getattr(self, "_universe_epoch", 0) or 0)`；**写者 = 0**。对照：隔壁 `_universe_refresh_id` **有**写者（`engine.py:758`）⇒ 不是"这类字段都没有写者"，而是 **epoch 单独缺写者** |

**⚠ 关于 `epoch` 我要按纪律降级标注**：该字段**并非本线新发现** ——
生产者自己的报告与计划里**已经写明**（`2026-09-22_13-00-00_JST.md:204,297`、`17-00-00_JST.md:398`、
`21-00-00_JST.md:398`、`NEXT_STEPS.md:363`）。按"作者已披露"口径，它属**已披露的已知占位**，
本线只做**独立确认**（写者 0），**不主张为首报**。

---

## §3 不存在项（**不得再当"回归"跑**）

`tests/test_full_day_simulation.py` —— **两个树都不存在**，`git ls-files` / `git log --all` 均无记录，
`*full_day*` 路径命中为 **0**。它在文档里出现过，属**待新增**，**不是**回归项。
后续轮次若把它列进"未修项回归"就是**在追一个从未存在的文件**。

---

## §4 对轮转手册 §4 基线的更正建议

`audit_rotation_v3.md:101` 现文：

> 未修 `IT-P0-002`、`IT-P0-001`、`IT-P1-SOURCE-EMPTY-001`、`IT-P1-WINDOW-001`、`IT-P1-LIMIT-001`、`IT-P2-OBS-001`。

**应改为（依据本节实测）**：

- **仍开放（4）**：`IT-P0-002`（P1，时区/死参）、`IT-P1-SOURCE-EMPTY-001`、`IT-P1-WINDOW-001`、`active_snapshot["epoch"]` 占位；
- **已修（3，勿再回归）**：`IT-P0-001`、`IT-P1-LIMIT-001`、`IT-P2-OBS-001`；
- **不存在**：`tests/test_full_day_simulation.py`（待新增）。

> 注：手册我**不能改**（硬约束：绝不创建或修改任何定时任务与排程；手册不在本线可写范围内）。
> 本节只把自己的**更正结论**写进审计文档，供人手动同步。

---

## §5 实际跑过的命令 / 退出码 / 真数

| 命令 | 退出码 | 真数 |
|---|---|---|
| 全树 grep `_universe_epoch`（`D:\it_h20_tip`，排除 `__pycache__`） | 0 | **1** 处，在 `engine.py:1049`，**写者 0** |
| 对照 grep `_universe_refresh_id` | 0 | 5 处，**写者 1**（`engine.py:758`）⇒ 对照成立 |
| `session.py` 逐行复核（CLOSE_AUCTION / datetime.now / `_now_fn`） | 0 | CLOSE_AUCTION 命中 11 行；`datetime.now()` **6** 处；`_now_fn` **1** 处 |
| 全树 grep `ZoneInfo\|zoneinfo\|pytz\|utcnow` | 0 | **0** 命中 |
| `limit_board.py` 逐行复核 | 0 | `:210` 判定前移、`:217` 新状态、`:274` 唯一定义、`:320` 兜底 |
| `live_session.py` grep `_round_quote_count` | 0 | 定义 `:247`、使用 `:2883` |
| `tests/test_full_day_simulation.py` 存在性 | 0 | **False**（两树） |

**未跑及原因**：未跑真实 soak（需行情服务与交易时段）；**本仓库无 `docs/audits/validate_latest.py`**，
校验器步骤不适用，**不声称任何 gate PASS**；未实施任何代码修复（见 §6）。

**子 agent 使用说明**：本轮派了 4 个只读子 agent（新提交审查 / 未修项回归 / 机制与影响 / 独立证伪）。
其中 2 个（机制、独立证伪）**因 PowerShell 不支持 heredoc 而中途失败**，已改正提示词后重派；
本条回归表的原始出处是"未修项回归"子 agent，其 8 条结论**已由我逐条独立复核**（含写者计数、
行号、以及"作者已披露"这一降级判断），**未直接采信**。

## §6 边界与合规

只读产品代码与审计文档；**未改任何产品源码**、未合并 PR、未建/改 Actions、
**未创建或修改任何定时任务与排程**、未启动行情服务；`data/live_session_*.json` 只读未动；
GitHub 侧只新增本追加与串行更新 `LATEST.md`；未 force push、未推送整条本地分支。
