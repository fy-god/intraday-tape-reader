# 本地 Agent 任务书 — 2026-09-21 12:02:53 JST

## 基准与目标

仓库：`fy-god/intraday-tape-reader`

云端本轮固定审计产品 SHA：`4e73ded6b1fe12e3d4eaa15fb8971098124bebe9`。
开始执行前必须重新读取实时 `main`，区分产品提交与 docs-only 提交。

本轮主目标不是加模型，而是关闭：
- `IT-P1-ST-REPLAY-BYPASS-001`
- `IT-P1-LIMIT-PROVENANCE-001`（新）
- 日期输入 fail-open 的边界问题
并把市场规则身份接到未来 committed-event 标签链。

## 关键更正

不要把 `server/web.coerce_ts()` 当成 `Quote.ts` 的 ST 市场规则可达证据；该函数当前重建的是 `Alert.ts`。
`str/int -> _as_date(None) -> current rule` 是真实函数缺陷，但内置 Quote producer 当前已证实的输出是 `datetime|None`。优先修**可达根因**：replay day 缺失 + local-derived limit 冒充 explicit limit。

## WP01 — replay 日期 + limit provenance【第一优先】

先红测：
- 2025-03-10 主板 ST, prev=10 -> 10.50
- 2026-07-05 -> 10.50
- 2026-07-06 -> 11.00
- post-change -> 11.00

实现：
- `ScriptedStock.limit_rate(when)`
- `build_script()` 传 replay day
- 局部 script clamp 用 date-aware limit
- replay Quote 不要把 local-derived limit 当 provider explicit；优先方案是 `limit_up/down=0`，让 Quote 根据 `ts` fallback。若必须保留则加 provenance sidecar。
- `default_universe` 加 ST / *ST。

同时回归：
- Tencent 明确 limit_up/down 仍优先
- Sina fallback 仍按 decision date
- N/C no-limit 不变
- GEM/STAR/BJ 不变

做 rollback tooth：只回退产品实现，红测必须重新红。

## WP02 — 日期输入边界

`_as_date` 不要默默把 unsupported `str/int` 与真正 `None` 合并。

建议：
- model 层严格 `date|datetime|None`
- ingestion 层解析 text/int
- unsupported type 明确异常/计数
- `None` 作为 missing date evidence

产物 `market_rule_date_cases.json`，记录每个真实 Quote producer 能产生的类型与证据行号。

## WP03 — MarketRuleEvidence

在 committed event identity 中加入：
- decision_trade_date
- market_rule_version
- limit_source
- source/source_epoch

`market_rule_version` 若继续存在，必须有真实消费者；否则删除 helper 和“可追溯”能力声明。

## WP04 — Alert Truth Contract

实现：
`hit_candidate -> rule_selected -> bus_accepted -> committed(event_id)`。

兼容 `published` 可以保留字段名，但语义必须明确 deprecated/alias，不能继续叫“真的发出去”。

红测：连续 24 次规则命中、cooldown 只允许 1 次：
- rule_selected=24
- bus_accepted=1
- committed=1

## WP05 — NotificationResult

typed 状态：
`sent / skipped_disabled / skipped_severity / skipped_dry_run / failed`。
Engine 必须消费结果；False 或 typed failed 进 delivery ledger。

## WP06 — Future labels

仅从 committed `event_id` 建 T+5/T+30。
保存 known_at。
matched control 选择不得读取 future。
事件簇内重复不当 IID 行；按 day/symbol/event cluster 做 bootstrap。

## WP07/08 — per-code provenance 后再 soft-partial

先实现 `(route,code)->source/source_epoch/capability`。
再只对 `unknown_missing` 做 targeted fallback。
禁止 round-global capability 下直接 mixed-source。

## WP09 — ObservationInterval + SSE durable

解决平价新鲜观测覆盖和 committed→sent→received→applied cursor/gap。

## WP10 — 真实数据认证

无真实多日连续 corpus 就保持 blocked。
不要重启 TCN/Transformer 搜索。
先用 committed-event 标签验证简单基线。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

replay_market_rule_red.log
replay_market_rule_green.log
replay_market_rule_rollback.log

market_rule_date_cases.json
market_rule_provenance.json
alert_stage_reconcile.json
notification_delivery_reconcile.json
provider_coverage.csv（有真实多日数据才生成）

full pytest log
gate logs
RUN_MANIFEST.json
NEXT_STEPS.md
```

未运行项必须写 `not_run/blocked + 原因`，不要用 0 占位。
