# 本地 Agent 任务书 — 2026-09-20 04:05 JST

## 基准

- repo: `fy-god/intraday-tape-reader`
- baseline SHA: `a3cfd567fb07e414c1d553e5fc478e50892abea8`
- highest priority: `IT-P0-002-R1`
- next research experiment when real corpus is available: `EXP-IT-CAP-003-real-mask-routing`

先确认工作区干净程度与未提交修改；不得覆盖用户已有改动。所有产品修改先留在本地隔离工作区，回传 diff 与日志；审计自动化没有授权把产品补丁直接推远端。

## WP01 — 关闭 IT-P0-002-R1

当前 `EngineState.update()` 会拒绝远未来点，但 `poll_once()` 随后又从原始 `quotes` 构造 `returned/eligible/Snapshot`；out-of-order 分支也在拒绝 history append 前已覆盖 `state.quotes`，且最后覆盖 `last_price`。

实现单一 admitted-current 合同。时间/质量拒绝必须发生在 latest cache、last_price、history、rule Snapshot、RoundObservationSet.admitted 之前。股票和指数共用同一 admission 结果。

必须新增真实 repo 红测：
1. far-future provider quote 不进入 CaptureRule Snapshot；
2. far-future 不计入 `RoundObservationSet.admitted`；
3. out-of-order quote 不覆盖 `state.quotes` / `last_price`；
4. out-of-order quote 不进入 rule Snapshot；
5. index path 同样遵守 admission；
6. 正常/轻微 future clamp 路径不退化。

先让旧代码红，再修，最后把修复局部回退做验牙。回传 `red.log / green.log / rollback-tooth.log / git.diff / diff.sha256`。

## WP02 — 让 RoundObservationSet 真正可观测

当前 sidecar 只活在 `poll_once()` 局部。把 bounded summary 接到 Store/status/live_session；不要持久化无界逐股明细。

每轮最少输出：`requested, returned, admitted, explicit_unavailable, unknown_missing, future_rejected, out_of_order_rejected, source, source_epoch, capability set`。

每个启用规则输出：`evaluable, unavailable_capability, rejected_quality, evaluated_no_hit, hit`。测试 Tencent→Sina：REST/SSE 服务仍可健康，但 `volume_burst` 必须明确显示缺 turnover，而不是只有 0 alerts。

## WP03 — soft-partial 与定向 fallback

引入请求级 batch contract。主源 `[A,B]` 只回 A 且不抛错时，B 必须成为 `unknown_missing`；健康备用源可只补 B。明确停牌/合法 unavailable 不无限重试。记录 provenance/source epoch，跨 epoch 不串累计量。

## WP04 — 收盘集合竞价

当前 `session.py` 14:57–15:00 仍属于 AFTERNOON/CONTINUOUS。按 SSE/SZSE 股票时段建立 close-auction 语义或兼容 helper。回归边界：14:56:59 / 14:57:00 / 14:59:59 / 15:00:00。同步检查 `elapsed_trading_seconds` 与 `only_continuous` 规则。

## WP05 — ObservationInterval

修 change-only history 丢失新鲜横盘 anchor。新鲜同值观测只延长 `last_observed_at`，真实缺口不前填。测试 5 秒观测的 60 秒横盘后 +3% 能算出；中间实际无观测则 unavailable。保持内存上界。

## WP06 — limit qualified-state

把 `at_limit_unqualified` 与 `sealed_qualified` 分开。门槛 200 万、封单 100→300 万：第二轮应首次发 seal；持续 300 不重复；离开后再封可重新武装。涨停/跌停镜像。

## WP07 — Eastmoney universe truncation

`required_pages=ceil(total/page_size)`；`required_pages > max_pages` 必须 `complete=false` 并记录 `truncated_by_max_pages`。300/100/max2 红测；max3 正常；分页失败既有保护不退化。

## WP08 — 真实因子 capability mask

建议研究路径 `research/intraday_quality/`，重依赖与生产启动链隔离。35 因子值和 `cap_A..cap_H` mask 分离；blocked 微结构字段不得填业务 0。保留 future perturbation causality tests，新增 provider capability fixtures。回传 `factor_catalog.csv / factor_diagnostics.csv / provider_coverage.csv`。

## WP09 — 真实数据与固定 HGB 对照

先生成 `dataset_manifest.json`；不足 30 股×20 日也照实报告。冻结 TaskSpec/splits 后，如果连续真实数据足够，只跑预登记三模型：HGB base、capability-dropout、explicit capability-mask。不再重新大搜超参，不打开 TCN。按 provider、时间折、未见股票、事件簇分层；同时报 coverage。无独立真实标签不报实盘 Precision/Recall。

synthetic v4 仅作机制先验：provider-shift PR AUC base 0.6716、capdrop 0.6755、capmask 0.6778；Eastmoney capmask 没有增益，所以必须保留 provider 分层与 abstention 对照，不能只报总体。

## WP10 — Shadow / SSE / 交付

Shadow 只输出 `raw_score, model_version, feature_hash, capability_status, fallback_reason, latency_ms`，不改变原规则通知。模型不可用时原规则继续。推进 `committed→sent→received→applied` 对账，慢客户端需要 gap/补账语义。

最终必须回传：

```text
git diff + SHA256
red.log / green.log / rollback-tooth.log
source_baseline.json
dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
provider_coverage.csv
trials.jsonl
training_log.jsonl
checkpoint hashes
predictions.csv
ablation.csv
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

`RUN_MANIFEST` 是测试数、数据 hash、配置 hash、seed/fold、机器、命令、退出码和产物 hash 的事实源。报告/LATEST/commit message 的机器数字必须与它一致。

禁止为了通过验收修改负例、抬阈值、减少股票或在最终锁箱挑方向。任何 `blocked` 项保留原因并继续独立可执行工作。
