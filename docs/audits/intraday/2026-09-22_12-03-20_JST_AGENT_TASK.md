# 本地 Agent 任务书 — 2026-09-22 12:03:20 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- 云端固定产品 SHA：`369b13f0d6f20fc22198abf51469ee14e49c84dc`
- 开工前重新读取实时 `main`；如果只是 `docs/audits/intraday/` 变化，不算产品升级。
- 当前开放 PR：0。
- 不真实通知、不交易、不改 Actions、不自动 push、不持续抓取、不租卡。

## WP01 — Universe Refresh Truth v4【P1，第一优先】

### RED 1：unknown denominator + explicit truncation
输入：
```text
expected_total=0
coverage_active=None
transport_complete=false
```
当前：`universe_coverage=ok/not_measured`。
目标：`universe_transport=fail` 或等价硬失败。

### RED 2：active snapshot 与 latest attempt 分离
1. full refresh 成功；
2. 下一次所有 universe source 抛异常；
3. active pool 允许保留；
4. `latest_attempt.status` 必须为 `all_failed`；
5. active age / last complete age 必须前进；
6. 不允许 realtime status 继续无条件显示“刚验证的 100%”。

### RED 3：retained previous freshness
- age 600s + refresh failed/rejected -> WARN；
- age > `universe_refresh_seconds` -> FAIL。

### 实现
建议新对象：
```text
active_snapshot
latest_attempt
freshness
```
不要“失败就清 meta”，也不要“失败就完全不动 meta”。

---

## WP02 — live-session session-level Universe Truth【P1】

当前 setup 只在 soak 前取一次 `universe_truth`。

RED：
- fake clock；
- t0 full；
- soak 中跨 1800s；
- universe refresh fail；
- final metrics 必须含最新/最坏 universe refresh state；
- final verdict 不能继续使用 t0 快照。

可以每轮采样，也可以至少 poll 后同步 latest truth；必须有 session aggregation。

---

## WP03 — `IT-P0-002-TZ-R1`【P1】

目标：同一真实 instant 在 UTC/CST 宿主上 phase 一致。

实现：
- `app.timezone` 使用 `zoneinfo.ZoneInfo`；
- TradingCalendar 的 wall-clock fallback 显式使用 market timezone；
- injected replay/test clock 合同写清 naive/aware 处理；
- 不让 provider ts 决定 session phase。

RED：
same epoch, host UTC vs Asia/Shanghai -> phase must equal.

---

## WP04 — `IT-P1-SOURCE-EMPTY-001`【P1】

只改 snapshots 的 soft-empty policy，不粗暴影响 universe。

RED：
```text
requested codes 非空
primary.snapshots -> []
backup.snapshots -> quotes
```
目标：
```text
backup.calls >= 1
serving route = backup
```

负控制：
- caller 未 dispatch -> 不进 SourceManager；
- primary exception -> 既有 fallback 继续工作；
- universe() 空值继续走 Universe 自己的 completeness/source-chain 语义。

---

## WP05 — `IT-P1-WINDOW-001` ObservationInterval【P1】

不能用“每5秒重复 append”简单放大 history。

建议：
```text
ObservationInterval(
  first_observed_at,
  last_observed_at,
  price,
  cum_volume,
  source_epoch
)
```

fresh-flat：延长 `last_observed_at`。
true gap：不延长。
source epoch change：切 interval。

RED：
13 个 fresh-flat observation + t65 +3%：
- `_covered(60s)` 与 `price_change(60s)` 必须一致；
- price anchor 能从覆盖 cutoff 的 interval 得到。
另做真 60s gap 负控制。

---

## WP06 — partial universe retry/backoff

先真实 Engine import 证明当前 call count，只有行为 RED 成立才升级严重度。

随后：
```text
last_attempt_at
last_applied_at
last_complete_at
universe_retry_seconds
```

---

## WP07 — per-code provenance/capability

为 targeted fallback、source-switch evaluation、质量因子建立真实 per-code source/capability。

---

## WP08 — research data/labels/32-factor registry

待新增：
```text
research/intraday_quality/
  dataset.py
  labels.py
  splits.py
  factor_catalog.py
  features/
  factor_eval.py
```

先做：factor formula unit tests、causal tests、missing masks、source capability gating、train-only scaling、same-time-of-day baseline only from prior days。
扫描授权本地目录；有真实数据则 manifest，无则 blocked，不买/抓新数据。

---

## WP09 — tabular + attribution【数据满足后】

固定：TaskSpec H=180s 主标签；60/300s 辅助；known_at / label_end / unknown；time-forward split + purge。
模型：rule / Logistic / HGB / small MLP。
全部 fit→checkpoint→reload→predict→evaluate。

---

## WP10 — SSE durable / TCN / shadow

先关闭：committed→sent→received→applied、cursor/gap、slow client silent drop-oldest、bounded state。
之后才跑 TCN；TCN time-forward 无稳定增量直接淘汰。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

universe_transport_red.log
universe_transport_green.log
universe_refresh_freshness_red.log
universe_refresh_freshness_green.log
universe_session_snapshot_red.log
universe_session_snapshot_green.log

timezone_red.log
timezone_green.log
source_empty_red.log
source_empty_green.log
observation_interval_red.log
observation_interval_green.log
partial_retry_red.log
partial_retry_green.log

universe_refresh_truth_cases.json
timezone_cases.json
source_empty_cases.json
observation_interval_cases.json
per_code_provenance.json

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若实际训练）
predictions.csv（若实际训练）
ablation.csv（若实际训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md

full_pytest.log
check_*.py logs
dash_render_check.log
```

未适用项必须 `not_run/blocked + 原因`，不得填 0。
