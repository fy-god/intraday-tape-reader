# 盘中预警 审计追加-2 — 2026-09-22 16:20:44 JST（更正我自己的一条过强表述）

- `publication_kind`: `CURRENT_INTRADAY_AUDIT_ADDENDUM_2`
- 更正对象：`docs/audits/intraday/2026-09-22_16-20-44_JST_ADDENDUM.md`（`8e95e4d4b19eab1df57a8fd7d1feab63529b44c4`）
- `reviewed_source_sha`（被测产品提交）：`6c8f11e363b3efcd21e410a5af7a718e08e8564d`
- `execution_status`: `COMPLETED`
- `research_verdict`: `SELF_CORRECTION_GUARD_NOT_DEAD / SESSION_OVERRIDES_T0_HARD_FACT_CONFIRMED / MEMBERSHIP_DROP_IS_CONSUMED`
- `evidence_status`: `VERIFIED`
- `evidence_type`: `SOFTWARE_SAMPLE`

三个**只读**子 agent 各自独立复核本轮发现。我按规则 (vvv) **逐条自己重算**。结果：
**一条推翻了我上一份追加里的表述（我改口）**，一条**确认**了一个新缺陷并指出作者自己的负控制**测错了**，
一条**推翻**了回归子 agent 对第 8 项的定性。全部在提交版 `6c8f11e` 上复算。

---

## 1. 【更正我自己 · 撤回过强表述】`:1576` 的守卫**不是死代码**

我在上一份追加里写：因为 `ever_watchlist_only = bool(...)` **恒不为 `None`**，
所以「无法判定」的那一支「**永不成立**」。

**这句话对「由 `_universe_session` 造出来的 dict」成立，但作为一般断言是错的，我撤回。**

我实测**真实仓库里归档的报告**（只读，未改动）：

```text
data/live_session_*.json  共 7 个文件（均未被 git 跟踪）
  live_session_20260917_010307.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_010351.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_010510.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_010646.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_080126.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_101539.json   universe_session_key=False   _w_ever=None  -> 1576 触发
  live_session_20260917_102925.json   universe_session_key=False   _w_ever=None  -> 1576 触发
```

**七个归档报告全都没有 `universe_session` 键** ⇒ `_w_ever is None` ⇒ **走 `:1576`，输出诚实的
「无会话级扫描范围记录…无法判定」**。所以：

- **该分支是活代码，且是唯一正确的旧报告处理**；
- 若按我原来的暗示去「删掉/合并」它，会把**唯一的诚实旧报告路径**换成 `:1590` 的肯定句 ——
  **那是引入回归**。这一条我采纳子 agent 的更正。
- **真正该修的仍然是我原来指出的那一处**：`_universe_session` 造出的零证据 dict
  （`measured=False`、`ratio=None`、`ever=False`）会落到 `:1590` 发出肯定句。
  **修法必须是给 `:1580` 加证据门控，而不是动 `:1576`。**
- 这是「**修一个缺陷时不要把旁边正确的分支一起改坏**」——与我本轮主题同源。

## 2. 【确认 · 新缺陷】会话的「全完整」会**盖掉 t0 的硬事实**

我实测（真实 `evaluate_health`，提交版）：

```text
t0 transport_complete=False  + session 30 轮全测、0 轮不完整
  -> universe_transport = ok     detail「会话期间 30 轮 transport 均为完整（无截断）」
t0 transport_complete=True   -> ok（同上文案）
t0 transport_complete=None   -> ok（同上文案）   <-- 连 None 都被同一句话盖掉
```

**t0 的 `transport_complete=False` 是「provider 明确声明股票池被截断」，
这是不需要分母就能确定的硬事实**（文件自己的注释 `:1645` 就这么叫它），
**却被会话级「全完整」覆盖，判决输出 ok，退出码不变。**
反向对照正常：会话 12/30 不完整时**确实**判 `fail`（说明分支本身是活的）。

**⚠ 作者自己的负控制测错了（我复算确认）**：
`tests/test_health_consumer_contract.py:180-192` `test_explicit_incomplete_still_fails`
设了 `rounds_transport_measured=0`，使 `:1819` 的肯定分支条件为假，于是**落到 t0 分支**才判 `fail`。
实测：

```text
t0=False + rounds_transport_measured=0  -> fail   <-- 该测试走的路径
t0=False + rounds_transport_measured=1  -> ok     <-- 一个单位的改变就翻绿
t0=False + rounds_transport_measured=30 -> ok
```

⇒ **该测试「以错误的理由通过」**：它从未在 `rounds_transport_measured>0` 时用 t0=False 试过 `:1819`。
**这正是我本轮一直在用的「正控制+负控制配对」纪律被抓到的实例**——
作者加了负控制，但负控制的**输入把被测分支绕过去了**。

## 3. 【更正 · 我自己的探针无效】第 8 项不是「零消费者」，是**语义缺口**

我第一版矩阵把 `universe_truth` 放在顶层，而真实消费者读的是 `setup["universe_truth"]`
（`:1642`），于是每一项都返回「无 universe_truth」——
**一个什么都没匹配上的过滤器证明不了任何事**（规则 mmm2）。改用正确的键后：

```text
active=    4/6000? no -- 4/6   cov=66.67% -> universe_coverage = fail  「全市场覆盖过低…扫描范围缺 2 只」
active= 5900/6000              cov=98.33% -> universe_coverage = ok
active= 3000/6000              cov=50.00% -> universe_coverage = fail
active=  600/6000              cov=10.00% -> universe_coverage = fail
```

**⇒ 解析层丢掉的 2 只（停牌）确实被消费了**：`coverage_active` 驱动了真实的判红。
所以第 8 项**不是**「生产者有出口、消费者零读者」。真正的缺口是**语义**：

| 情形 | 都塌缩成同一个数 |
|---|---|
| (a) 解析器丢掉 2 行停牌股（**预期，良性**） | 66.67% |
| (b) provider 真的少给 2 行（**传输缺口**） | 66.67% |
| (c) 数据源切换/只取到部分页（**真实缺陷**） | 66.67% |

反向证据：`eastmoney.py:248-274` 的 `ClistPage` **刻意做了双账**
（`raw_rows` / `dropped_invalid`，注释明说是为了不把「市场里有停牌股」误判成「服务端少给数据」），
且 `universe_info()` 输出 `dropped_invalid`、`raw_code_rows`；
但 `_universe_meta_of`（`engine.py:1157-1181`）**不搬运这几个键**，判定层只看比率。
我把这条记为 **`IT-P2-UNIVERSE-DROP-CAUSE-COLLAPSED-001`（P2，待验证风险）**，不给真实发生率。

## 4. 【新增 · 旁证】`_now_fn` 是**死参数**（注入点无法注入）

复核第 6 项时我改了探针口径（我第一版把同一个 naive 字符串喂给三个主机 —— **那是恒真式，
测不出主机依赖**，规则 mmm2）。用**同一个真实瞬间**重测：

```text
真实瞬间 = 2026-09-22T10:00:00+08:00   (epoch 1790042400)
  host=America/New_York  now() -> 2026-09-21 22:00:00  phase=POST     is_open=False
  host=UTC               now() -> 2026-09-22 02:00:00  phase=CLOSED   is_open=False
  host=Asia/Shanghai     now() -> 2026-09-22 10:00:00  phase=MORNING  is_open=True
  distinct phases = ['CLOSED','MORNING','POST']   distinct is_open = [False, True]
```

**同一真实瞬间在三个主机上得到三种时段判定。** 并且：
`TradingCalendar.__init__` 有 `_now_fn` 参数（`session.py:100`），
但它在**整个文件里只出现这一次**（`[100]`）—— **声明了却从不读取 ⇒ 死参数**。
即「本来留给测试注入时钟的口子」从未接线，而 `:142/214/232/242/252` 六处裸 `datetime.now()`
全链无时区设施（`src/` 内 `ZoneInfo`/`astimezone`/`app.timezone` 命中数 = **0**）。

## 5. 状态与合规

- 本追加只新增本文档并更新 `LATEST.md`；**未改任何源码/权重/配置/Actions/PR**。
- **未创建、修改或删除任何定时任务与排程**；**未 force push**；未推整个分支。
- 排查 `data/` 下的 7 个归档 JSON 为**只读**；未改动、未提交、未删除它们。
- 作者工作区只读；其实测 blob `b990d410…` == HEAD == origin/main（我未改动）。
- 全部复算在 `D:\it_h16_c6`（`6c8f11e` 纯净导出）内执行，**未在真实工作树内跑测试**。
- **未跑**真实 soak；**未做**训练/评估；`程序修复` 0 / `任务定义变更` 0 / `真实模型增益` 0。
- 归档交叉核对仍不可得 ⇒ **不给任何真实发生率**。
