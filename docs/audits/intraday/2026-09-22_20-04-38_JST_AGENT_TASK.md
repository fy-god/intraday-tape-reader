# 本地 Agent 任务书 — 2026-09-22 20:04:38 JST

## 基线

- repo：`fy-god/intraday-tape-reader`
- 产品基准 SHA：`6c8f11e363b3efcd21e410a5af7a718e08e8564d`
- 审计开始 docs HEAD：`0816294d6b38ff1a788acd262c3734ecace6ae71`
- 开工前重新读 main；6c8 之后若只有 docs(audit)，产品基准保持 6c8。
- 不自动 push；不真实通知/交易；不改 Actions；不付费抓取；不启动常驻服务。
- Fake/Replay 显式钉 source/universe，禁止意外真实联网。

## WP01 — Session Universe Evidence Contract v1

建议新增 `tests/test_session_universe_evidence.py`。

### RED A zero evidence
`finalize_metrics([])` 或等价新 schema zero measured：
- `universe_scope` = not_measured；
- detail 不得含“全程全市场扫描”。

旧报告无 `universe_session` 的 `_w_ever is None` guard 必须保留。

### RED B t0 hard negative
```text
setup.universe_truth.transport_complete=False
session.rounds_transport_measured=30
session.rounds_transport_incomplete=0
```
=> `universe_transport=FAIL`。

当前 `test_explicit_incomplete_still_fails` measured=0，不算本 RED。

### RED C session active coverage
```text
t0 expected=6000 active=5900 coverage=.9833
session active first=5900 min/last=4500
round coverage=1.0
transport all True
freshness fresh
scope full_market
```
=> `coverage_active_min=.75`
=> `universe_coverage=FAIL`。

### GREEN schema
每轮：
```text
scope_state
active_scan_codes
expected_total
denominator_kind
coverage_active
transport_complete
fresh_state
attempt_status
refresh_id
```
session：
```text
scope_measured_rounds
full_market_rounds
watchlist_only_rounds
empty_scan_rounds
unknown_scope_rounds
coverage_active_measured_rounds
coverage_active_min
coverage_active_p05
coverage_active_last
```

Merge：
- intentional watch-only exempt；
- any watchlist/empty -> full-market soak fail；
- zero measured -> not_measured；
- t0 False OR any session False -> transport fail；
- coverage 用 t0+session numeric worst；
- round coverage 不替代 active coverage。

Rollback：回退后 A/B/C 必须重红，旧报告兼容仍绿。

命令：
```text
pytest -q tests/test_session_universe_evidence.py
pytest -q tests/test_health_consumer_contract.py
```

## WP02 — Universe membership / Quote usability
独立 diff。provider replay：raw codes=100 / usable Quote=94 / transport complete=true；active membership 必须=100，usable_coverage=94%。

## WP03 — Source snapshots soft-empty
nonempty requested codes；primary `[]`；backup valid quotes；backup 必须调用。no-dispatch 和 universe 空值为负控制。

## WP04 — Timezone / clock
`app.timezone -> ZoneInfo`；`_now_fn` 真使用；同 epoch 跨 UTC/Shanghai host phase 一致；naive/aware replay 合同固定。

## WP05 — ObservationInterval
fresh-flat 延长，true gap 不延长，source epoch 切段；cutoff 可从 overlapping interval 取 anchor。

## WP06 — per-code provenance/capability
`(route,code)->source/source_epoch/capability/observed fields`。

## WP07 — dataset / labels / splits / factor registry
软件链先完成；真实数据存在才落真实 manifest。

## WP08 — factor diagnostics/search
单因子、相关簇、逐家族增量、失败 trials。

## WP09 — tabular / attribution
数据门槛满足后：rule/Logistic/HGB/small MLP，fit->checkpoint->reload->predict->evaluate，time-forward+purge。

## WP10 — SSE durable + TCN/shadow
先 delivery cursor/gap/bounded state，再 TCN；无稳定增量即淘汰。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

session_scope_zero_red.log
session_scope_zero_green.log
transport_merge_red.log
transport_merge_green.log
session_active_coverage_red.log
session_active_coverage_green.log
old_report_compat.log
wp01_rollback.log

session_universe_evidence_cases.json
membership_replay_cases.json
source_empty_cases.json
timezone_cases.json
observation_interval_cases.json
per_code_provenance.json

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若训练）
predictions.csv（若训练）
ablation.csv（若训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md

full_pytest.log
check_*.py logs
dash_render_check.log
```

未运行项写 `not_run/blocked + 原因`，禁止填 0 或挪用旧成绩。
