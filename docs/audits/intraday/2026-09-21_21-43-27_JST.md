# 盘中预警 云端审计：假绿被**转移**而不是消除 —— 把账本打成 `inconsistent` 后 `evaluate_health` 的判决**逐字节相同**

- `publication_kind`: `INTRADAY_CLOUD_AUDIT`
- `audit_time_jst`: `2026-09-21T21:43:27+09:00`
- `audit_time_cst`: `2026-09-21 20:43`
- `reviewed_repo`: `D:\ccc\ashare-radar`
- `reviewed_source_sha`: `6997487f0ec5d92f81299edbb8c68845e9e056a8`（上一轮已审产品提交，未变）
- `main_head_at_audit_start`: `b858760c2ef9f676198f2f504829dd8989694457`
- **`main_head_at_audit_end` / `report_created_commit_sha` 基准`: `fd51674508cb237c844d6a4924b7e6a2f084c4b6`（审计途中被并行作者提交并推送；`origin/main` = `fd51674508cb237c844d6a4924b7e6a2f084c4b6`，`ls-remote` = `fd51674508cb237c844d6a4924b7e6a2f084c4b6`，三者相等）
- `audited_new_commit`: `fd51674508cb237c844d6a4924b7e6a2f084c4b6` — `fix(alerts): 交付账本假绿/ring-buffer/soak 盲区等 7 条缺陷 + 3 处自我更正`（作者 `fy-god`，2026-09-21 20:28:58 +0800）
- `previous_report`: `docs/audits/intraday/2026-09-21_21-00-00_JST.md`（并行作者所写，本报告不改它）

`execution_status`: `COMPLETED_READ_ONLY_AUDIT`
`research_verdict`: `FALSE_GREEN_RELOCATED_NOT_ELIMINATED / VERDICT_LAYER_BLIND_TO_THE_NEW_LEDGER / INDEX_TEST_COUNT_CONTRADICTS_ITS_OWN_REPORT_BODY / RING_BUFFER_FIX_ONLY_PARTIAL / NO_VALIDATOR_FOR_THIS_LINE`
`evidence_status`: `REAL_CODE_MEASUREMENT / SOFTWARE_SAMPLE_ONLY / ZERO_REAL_MARKET_CLAIMS / INDEPENDENT_SUBAGENT_CROSSCHECK`

> **`evidence_type`: 全部为软件样本（SOFTWARE_SAMPLE）。本报告不含任何实股/真实行情结论，不含任何模型增益结论。** 本轮是代码审计，不是实验。

---

## 0. 本轮定位（重要）

审计途中 `main` 被**并行作者**推进：我 20:2x 开始时工作树里还有 5 个**未提交**源码改动 + 1 个**未跟踪**测试文件；等我读完，作者已把全部内容提交并推送为 `fd51674508cb237c844d6a4924b7e6a2f084c4b6`（`git status` 现已干净）。

因此：

- **我审的是 `fd51674508cb237c844d6a4924b7e6a2f084c4b6` 这个提交**，不是"未提交工作树"。所有行号都锚在这个修订。
- 我**没有**提交、推送、暂存或还原任何东西；`git status --porcelain` 在我读完之后为空，我保持它为空。
- 该提交**只碰 6 个源码/测试文件 + 5 个文档**（我核过 `--numstat`）：
  ```text
  107   11  src/arad/capabilities.py
   41    1  src/arad/engine.py
    7    0  src/arad/server/web.py
  319    0  tests/test_delivery_false_green.py      （新文件，16 个测试）
   17    5  tests/test_replay_capability_ledger.py
  171    0  tools/live_session.py
  ```
  `src/arad/store.py`、`src/arad/models.py`、`src/arad/rules/base.py`、`src/arad/server/dashboard.html`、`notifiers/*`、`sources/*`、`tools/run_daemon.py` **全部未改**。所以这个提交关掉的是**交付账本那一族**，**不是** R-12..R-17 / IO 系列。**不要把两者混算成"大进步"。**

---

## 1. 【最重要】假绿被转移，不是消除（`待验证风险`，我已用真实代码证明）

### 1.1 修复确实做对了它声称的事

作者把交付门从"`max(total - named, 0)` 夹成 0"改成一个显式状态机（`src/arad/capabilities.py`）：

```text
:74-76    ACCOUNTING_OK = "ok" / ACCOUNTING_NOT_MEASURED = "not_measured"
          / ACCOUNTING_INCONSISTENT = "inconsistent"
:772      "accounting_status": self.delivery_accounting_status(),
:792-816  def delivery_accounting_status(self) -> str
:809-810      if self.accounting_errors > 0:  return ACCOUNTING_INCONSISTENT
:815          return ACCOUNTING_INCONSISTENT        # named > total
:818-830  def check_delivery_accounting(self) -> list[str]
src/arad/engine.py
:1341     observation.accounting_errors += 1         # 原来的 except: pass
:1358     observation.accounting_errors += len(bad)   # 不变量违反
:1363     observation.accounting_errors += 1
```

我又给**新测试做了变异实验**（把 `delivery_accounting_status` 改回旧的"只有 `accounting_errors>0` 才算坏、`named>total` 不报"的语义），在临时副本里跑新测试文件：

```text
基线（未变异）   : 16 passed, exit 0
变异后           : 2 failed, 14 passed, exit 1
  FAILED tests/test_delivery_false_green.py::test_gate_alone_was_a_false_green_when_denominator_was_swallowed
        AssertionError: 分母被吞时必须报 inconsistent，否则就是假绿   (:58)
  FAILED tests/test_delivery_false_green.py::test_ratio_above_one_is_flagged_not_clamped
        assert 'ok' == 'inconsistent'                                  (:69)
```

**⇒ 这两个测试对"门夹成 0"这个缺陷有真实探测力**，不是空断言。（这是我对本线铁律「任何『守卫有探测力』主张都必须做变异实验」的执行。）

### 1.2 但**没有任何判决层在读它** —— 这是本轮的核心发现

`accounting_status` 被算出来、被塞进 `/api/status` 的 observation，**然后呢**？我用两条独立路径查：

**（a）静态：全仓谁引用这些新键**

```text
非测试、非文档的引用者只有 3 个，且**全都是生产者**：
  tools/live_session.py: make_round_sample() 3 处 / summarize_rounds() 14 处 / empty_metrics() 7 处
判决类函数引用数：
  evaluate_health()           hits=0   <== 盲
  finalize_metrics()          hits=0   <== 盲
  _print_summary()            hits=0   <== 盲
  _fmt_observation_summary()  hits=0   <== 盲
```

`evaluate_health` 的整个函数体（`tools/live_session.py:985-1310`，**325 行**）里 `delivery|accounting|signal_delivery` 词元数 = **0**。

**（b）动态（决定性）：把账本打成重度损坏，看判决是否变化**

我构造一个 `delivery_accounting_session`：`accounting_status="inconsistent"`、`accounting_errors=7`、`inconsistent_rounds=[1,2,3]`、`committed_alerts_total=0` 而 `committed_with_signal_id=66`、`signed_ratio=None`，再加一个 `signal_delivery` 里 `committed=0 / rule_selected=100 / dropped_by_bus=95`。然后与未损坏的同一 `metrics` 分别喂给 `evaluate_health`：

```text
VERDICT_WITHOUT_BAD_LEDGER  healthy=False exit=2
VERDICT_WITH_BAD_LEDGER     healthy=False exit=2
IDENTICAL_VERDICT = True
n_checks = 10
verdict text mentions accounting   : False
verdict text mentions delivery     : False
verdict text mentions ledger       : False
verdict text mentions inconsistent : False
```

**判决逐字节相同。** 10 个 check 是 `rounds / fetch / data / coverage / capability / api / sse / memory / browser / alerts` —— 账本损坏对它们**完全不可见**。

**⇒ 结论：`IT-P1-DELIVERY-FALSE-GREEN-001` 在它被定义的那一层（gate/`accounting_status`）确实修好了；但假绿**没有消失，只是从"门"搬到了"判决"**。只要 `evaluate_health`（也就是 soak/健康判定的唯一权威）不看这个字段，一个记账坏掉的会话仍然可以报健康。**这是 `待验证风险` 而不是 `已确认错误`**：字段是真的、状态机是真的、`/api/status` 真的导出了，所以"假绿"在**契约层**被消除；但"坏账本能否让会话被判健康"这个**可操作问题**答案是"能"。

作者自己在 `R-18` 里承认了一半（"`accounting_status` 只有 soak 与测试在读……看板前端是否显示我未核实"）——**但他说"soak 在读"，我实测 soak 的判决函数一个字都没读**。这是对他的自我评估的**收紧**。

### 1.3 前端也不读

```text
src/arad/server/dashboard.html = 49,247 B / 1146 行
其中 accounting|delivery|signed_ratio|false.?green|inconsistent 命中 = 0
```

（`src/arad/server/static`、`templates/`、`web/`、`frontend/`、`dashboard/` 全部**不存在**；全仓只有 2 个前端资源：`dashboard.html` 与 `tools/dash_render_check.js`。）

### 1.4 修复建议 + 验收测试

**修**：让 `evaluate_health` 消费账本，且**用严重度而不是存在性**。

1. `tools/live_session.py` 的 `evaluate_health` 增加一个 check（例如 `name="delivery_accounting"`），当 `delivery_accounting_session.accounting_status == "inconsistent"` 或 `accounting_errors > 0` 时 `ok=False`、`exit_code=EXIT_UNHEALTHY`。
2. 看板 `dashboard.html` 增加 `accounting_status` 显示（作者自己列为未核实项）。
3. `_fmt_observation_summary` / `_print_summary` 打印 `delivery_committed_total`（见 §4）。

**验收测试（必须是行为测试，不是字符串测试）**：构造一个 metrics，其 `delivery_accounting_session.accounting_status="inconsistent"`，断言 `evaluate_health(m)["healthy"] is False` 且 `exit_code == EXIT_UNHEALTHY`；再断言把它改回 `"ok"` 时判决变回健康。**这个测试在今天会失败**——这正是它该有的样子。

---

## 2. `LATEST.md` 的测试计数与**它自己那份报告正文**互相矛盾（`已确认错误`）

同一个提交 `fd51674508cb237c844d6a4924b7e6a2f084c4b6` 里，两处对同一次测试给出不同数字：

```text
docs/audits/intraday/LATEST.md:17
  **1672 passed in 114.64s**（基线 1660 → +12）
docs/audits/intraday/2026-09-21_21-00-00_JST.md:337
  | 全量 pytest | **1676 passed**（基线 1660 → **+16**）| in 102.25s |
docs/audits/intraday/NEXT_STEPS.md:21
  > **1676 passed**（+16）
```

**我实测的权威值**：在临时副本（不动仓库）里对 `fd51674508cb237c844d6a4924b7e6a2f084c4b6` 的测试目录做收集与全量运行：

```text
python -m pytest tests --collect-only -q   -> 55 个文件，逐文件计数求和 = 1676
python -m pytest tests -q -o addopts=      -> 1676 passed in 139.43s, exit 0
tests/test_delivery_false_green.py 单文件   -> 16 个测试函数，收集 16
```

`1676 - 1660 = 16` = 新文件的**全部** 16 个测试。⇒ **报告正文的 `1676/+16` 与我实测一致；`LATEST.md` 的 `1672/+12` 是陈旧值（少 4 个、慢 12.39 s）**，而且 `LATEST.md:104` 另一处还写着基线 `1660 passed in 120.52s`。

**为什么必须报**：这个索引是跨 agent 的**唯一共享指针**（本线是双 agent 并行写同一个 `LATEST.md`）。索引里的通过数是下一轮判断"有没有回归"的基准；差 4 个测试会让下一轮误判新增/丢失。

**修**：把 `LATEST.md:17` 改成 `1676 passed`（+16）；把耗时统一为**同一环境**的数（我这里 139.43 s，作者 102.25 s，差异是机器/并发，应标注环境而不是并列两个数）。
**验收**：`LATEST.md` 的 passed 数 == 报告正文 == `pytest --collect-only` 逐文件求和。

---

## 3. ring-buffer 修复只做对了一半（`待验证风险`，我复现了）

作者把**聚合**断言从 ring buffer 换成单调计数器，这是对的：

```text
src/arad/store.py:47    max_alerts: int = 300
src/arad/store.py:55    self._alerts: deque[dict] = deque(maxlen=max(int(max_alerts), 10))
src/arad/store.py:113   self._total += 1              # 每次 add_alert 都加，先加后裁
src/arad/store.py:332-347  recent_alerts(): n = max(1, min(int(limit), 1000)); items = list(self._alerts)
src/arad/store.py:349-351  def alerts_total(self) -> int: return self._total
tests/test_replay_capability_ledger.py:140   total_committed = store.alerts_total()
tests/test_replay_capability_ledger.py:141   assert sum(tagged.values()) == delivery_total == total_committed
```

但**同一个测试里还有两条断言仍然只扫 ring**：

```text
tests/test_replay_capability_ledger.py:148   assert untagged == 0, (...)
tests/test_replay_capability_ledger.py:150   assert all(r.get("signal_id") is not None for r in rows)
   （rows 来自 :128 之前的 rows = store.recent_alerts(...)，最多 300 条）
```

我用真实 `AlertStore` 复现了这个盲区（临时副本，只读运行）：

```text
先发 10 条 signal_id="" 的告警，再发 305 条带 signal_id 的告警
alerts_total()            = 315
len(recent_alerts(1000))  = 300
buf maxlen                = 300
untagged in rows          = 0
ASSERT untagged==0 PASSES   = True
ASSERT all signal_id PASSES = True
=> 10 条无 signal_id 的告警确实存在、alerts_total()=315，
   而这两条门禁断言全部通过：**在 300 以上是盲的**。
```

**⇒ `IT-P1-ACCEPTANCE-GATE-RINGBUFFER-001` 判 `已经修复`（聚合断言确实改对了），但其"signal_id 覆盖率"子断言仍是 `待验证风险`。**

**修**：`untagged` 不能只从 ring 统计。要么在 `Store` 上维护 `_untagged_total` 计数器（与 `_total` 同样在 `add_alert` 里递增），要么断言改成 `untagged == 0` 时同时断言 `alerts_total() <= buf.maxlen`（即"未溢出"前提），否则显式 skip。
**验收**：构造"前 10 条无 signal_id + 后 305 条有"的场景，断言 `untagged == 10` 或该测试明确 skip；今天它错误地通过。

---

## 4. `_scope_note` 指向一个**不存在的键**（`已确认错误`）

`src/arad/capabilities.py:765` 的 `_scope_note` 告诉读者「会话累计请读 `delivery_session`，由 live_session/…」。全仓 grep：

```text
'delivery_session'            6 hits，其中 **源码只有 1 hit** = 就是这条注释自己
                              （其余 5 都在 docs 里，且多是 `delivery_session_reconcile.json` 这个待交付文件名）
'delivery_accounting_session' 8 hits，真正的键（tools/live_session.py:795/:937）
```

⇒ 指引不可执行。**这是 `已确认错误`**（一个指向不存在符号的指针，且有确定的 file:line 与计数）。

**另附一条同族风险**：`committed_alerts_total` 这个键名在**两层用同一个名字**——per-round 的 observation 与 session 汇总都叫它（`capabilities.py:767` 与 `live_session.py:796`）。所以读者若按名字抓 `delivery_accounting.committed_alerts_total`，拿到的仍是**最后一轮的 0**。`_scope: "last_round"` 缓解了这一点，但键名未区分，`R-18` 提到的"可被误读成会话 0"在**按名取值**的路径上依然成立。

**修**：把注释改成真实键名 `delivery_accounting_session`；并考虑把 per-round 的键改名（例如 `committed_alerts_this_round`）。
**验收**：加一条测试，断言 `_scope_note` 里提到的每个键名都能在 `summarize_rounds` 的返回字典里找到。

---

## 5. 新测试文件：16 个测试，探测力分层（`待验证风险`）

`tests/test_delivery_false_green.py`（319 行，16 个测试）**不是**假绿文件——我确认没有任何一个测试能在**完全未修**的实现上整体通过（新方法不存在 ⇒ `KeyError`）。但探测力**分层很陡**：

| 类型 | 测试 | 说明 |
|---|---|---|
| **行为断言（真探测）** | :44, :63, :73, :88, :105, :214, :227, :274 | 直接断言 `accounting_status`/`_scope` 的值 |
| **变异实验证实有探测力** | :44, :63 | 我改回旧语义后这 2 个**真的失败** |
| **源码子串检查**（注释即可满足） | :112, :167, :181, :198, :240, :256 | 6 个。`inspect.getsource(...)` / 读文件文本后 `assert "key" in src`。**只能锁住"文本还在"，锁不住"行为对"** |
| **默认值同义反复** | :105, :227 | 断言一个**新建** `RoundObservationSet` 的默认 `not_measured`。任何暴露该方法的实现都满足（`total==0 and named==0 ⇒ not_measured`）。只因方法在旧版不存在才"探测"到 |
| **零探测力** | :131, :298 | :131 测的是**本次提交没改** 的 `store.py`（`alerts_total` 本就存在）；:298 在本地**重新实现**了一遍 web 的 `signal_id` 传递，而没有调用 `feed_item_of` |

**关键**：`:298 test_web_rebuild_roundtrip_keeps_signal_id` 在**修复前后都会通过**——它自己 `Alert(...)` 构造时显式传了 `signal_id`，且 `Alert.to_dict()` 在旧版**就已导出** `signal_id`。它测不出它声称要测的东西（`src/arad/server/web.py:272-291` 的重建路径）。

**修**：把 6 个子串测试改成行为测试；删掉或重写 `:298` 让它真正调用 `feed_item_of`；`:105/:227` 之外再补一条"改变输入 ⇒ 状态改变"的断言。
**验收**：对每个新测试做一次变异（把对应修复回退），要求**至少一个**测试失败。今天 :131/:298 做不到。

---

## 6. 回归核对表（21 项，锚在 `fd51674508cb237c844d6a4924b7e6a2f084c4b6`）

**本轮提交关掉的 6 项（`已经修复`）** —— 全部在 `fd51674508cb237c844d6a4924b7e6a2f084c4b6` 的 diff 内：

| ID | 证据 |
|---|---|
| `IT-P1-DELIVERY-FALSE-GREEN-001` | §1.1 状态机 + 我的变异实验（2 个测试失败）。**但见 §1.2 判决层盲区** |
| `IT-P1-ACCEPTANCE-GATE-RINGBUFFER-001` | `store.py:349-351` 单调计数器；测试改用 `alerts_total()`。**但见 §3 残留盲区** |
| `IT-P1-SOAK-LEDGER-BLIND-001` | `live_session.py:141/:154` 两个白名单加入 `signal_delivery`/`delivery_accounting`；`:301-323` slim；`:463-481` 累计；`:768-812` session 层；`:929-946` `empty_metrics` 同构 |
| `IT-P1-DELIVERY-GATE-PEROUND-001` | `_scope="last_round"` + `_scope_note`（`capabilities.py:763-766`）。**但见 §4 dead key** |
| `IT-P2-ALERT-DICT-ROUNDTRIP-001` | `server/web.py:290` `signal_id=str(raw.get("signal_id") or "")` **在** `feed_item_of` 的重建里；我读源码确认 `web.py:272 Alert(` 与 `:290 signal_id=` 同段 |
| `IT-P1-SOAK-REPORTS-PREFIX-NUMBER-001` | `live_session.py:787-789` 新增 `delivery_committed_total = sum(dl_committed.values())`，与旧 `evaluability_committed_total`（`:755`）**并存**，不再只有一个前缀错配的数 |

**仍然开放、且本轮提交**没有**碰的 15 项**（`已确认错误` / 仍然开放）：

`R-12`（= `IT-P1-UNIVERSE-COVERAGE-001`）、`R-13`、`R-14`、`R-15`、`R-17`、`IT-P1-CAPABILITY-002`、`IT-P1-SOURCE-EMPTY-001`、`IT-P1-WINDOW-001`、`IT-P1-008`、`IT-P1-009`、`IT-P1-003`、`IT-P1-NOTIFY-RESULT-001`、`IT-P1-NOTIFY-RESULT-002`、`IT-P1-ALERT-IDENTITY-001`、`IT-P2-DAEMON-RACE-001`。

我亲自复核了其中三条最硬的：

```text
R-12 : _universe_meta 全仓 5 处定义/赋值全在 src/arad/engine.py（:744/:877/:939/:961/:980）
       store.py / web.py / live_session.py / dashboard.html / replay / cli 命中 = 0
       src/arad/store.py:304   "universe": len(quotes)      <-- 仍是 latest-cache 大小，不是 active 集合
       => 未导出。且 evaluate_health 对 universe 不敏感（与 §1.2 同一根因）
R-15 : src/arad/models.py:590  "ts": self.ts.strftime("%Y-%m-%d %H:%M:%S")
       Alert(ts=None).to_dict() -> AttributeError（子 agent 实测复现）
R-17 : 9 处 signal_id= 赋值（limit_board/tick_surge/spirit_index/spirit_order/spirit_price/unusual/volume_burst）
       三者一致性门禁 grep = 0；tools/check_*.py 8 个里覆盖 signal_id 的 = 0/8
```

**`未复现`（必须降级，不得当结论）**：
- 子 agent B 报告 R-12 的旧头条数字 `73.0% / 83.1%` 全盘不可复现：其重算为 `4090/5917=69.1229%`、`4576/5917=77.3365%`，且 `5917` 在任何地方都**没有真实出处**（代码基线是 `5913`）。⇒ **这两个百分比标 `未复现`**，R-12 的严重性**不依赖**它们（真正的确定性事实是"零导出"与"判决不敏感"，我已复核）。
- `IT-P2-DAEMON-RACE-001` 的**并发窗口本身未复现**（未做真实双进程竞态）；但 `tools/run_daemon.py:138,141-150` 的单槽写 + `:391-399` 的 read-then-write TOCTOU 是确定的。

---

## 7. 我实际跑过的命令、退出码、真数

全部在**临时副本**（`%TEMP%`）里跑，**不在仓库内**，以避开并行作者：

```text
python -m pytest tests --collect-only -q      exit 0   55 文件，逐文件求和 = 1676
python -m pytest tests -q -o addopts=         -p no:cacheprovider                    exit 0   1676 passed in 139.43s
python -m pytest tests/test_delivery_false_green.py -q  exit 0   16 passed in 0.27s（未变异基线）
  ↑ 变异 delivery_accounting_status 后            exit 1   2 failed, 14 passed
  ↑ 变异 alerts_total 为 len(deque) 后            exit 1   1 failed, 19 passed
  ↑ 复原后                                      exit 0   restored = True
tools/check_*.py  8 个逐个跑                     8/8 exit 0
        check_audit_shots.py / check_bom.py / check_colors.py / check_config_consumed.py
        / check_config_wiring.py / check_orphan_config.py / check_readme_tools.py / check_spirit_mapping.py
tools/check_bom.py                              exit 0
node tools/dash_render_check.js                 exit 0
evaluate_health 判决对比（坏账本 vs 正常）       IDENTICAL_VERDICT = True
AlertStore 315 条（10 无 signal_id + 305 有）    两条 ring 断言均 PASS（盲）
```

**没跑的、以及为什么**：
- **没在仓库工作树里跑 pytest** —— 会让并行作者的 `git status` 长出 `__pycache__`/产物，且本线铁律要求不干扰他人 WIP。全部改在临时副本。
- **没跑真实行情/soak 长跑** —— 本线是代码审计；且 `max_wall` 与预算不属于本轮。
- **没做 GitHub API 查询**（PR 状态）—— 本线不需要，且沙箱历史上对 `api.github.com` 有过阻断。

**本轮未改动**：任何源码、权重、配置、Actions、PR、`SCHEDULE.md`、根 `README.md`。**未创建、修改、删除任何定时任务或排程。** 未触碰 `D:\xm\60日预测\reference\eventnet_test_results.json`。**未 commit / 未 push 任何代码** —— 本报告只写文档。

---

## 8. 本线的一个结构性缺口：**没有校验器**（`待验证风险`）

`audit_rotation_v3.md` §2 步骤 3.5 要求：「若该仓库存在 `docs/audits/validate_latest.py` → 运行它，必须 PASS 才提交。」

```text
docs/audits/validate_latest.py 在 fd51674508cb237c844d6a4924b7e6a2f084c4b6 是否存在：False
docs/audits/ 下 validate* 条目 = NONE（共 71 个条目）
磁盘是否存在：False
```

⇒ 本线的**强制校验门**在这条线上**根本无法运行**。原模型线有（`D:\xm\60日预测\docs\audits\validate_latest.py`，PASS），盘中预警线**没有**。这不是我造成的，也不是本轮的错，但它是这条线的一个真实治理缺口：**索引与报告的一致性没有任何自动检查**——§2 那个 `1672` vs `1676` 的矛盾，正是没有校验器时会长出来的东西。

**修**：为本线补一个 `docs/audits/validate_latest.py`，至少检查
(a) `LATEST.md` 引用的每个 `*_JST.md` 都存在（今天 43 个报告）；
(b) `LATEST.md` 里的 `passed` 数与最新报告正文一致；
(c) 每个报告的必填字段（`audit_time_jst`/`reviewed_repo`/`reviewed_source_sha`/`main_head_at_audit_start`）存在且 `reviewed_source_sha` 是 40 位十六进制。
**验收**：故意把 `LATEST.md` 的 `passed` 数改错，校验器必须非零退出。

---

## 9. 1–3 条有依据的研究/改进想法（依据来自本轮哪条证据）

1. **"假绿"应当作为一类可度量的量来跟踪，而不是逐个 ID 修**（依据：§1.2 的 `IDENTICAL_VERDICT = True`）。本轮证明了一个模式：**把判据算出来 ≠ 让判据起作用**。可以给 `evaluate_health` 加一条元规则并在 CI 里断言："`checks` 里出现 `ok=False` 的次数必须随 `delivery_accounting_session.accounting_status` 从 `ok` 变 `inconsistent` 而增加" —— 把"新字段是否有消费者"变成**可执行的不变量**，而不是靠人工 grep。（这是**程序修复**方向，不是模型增益。）

2. **索引/报告一致性应自动校验**（依据：§2 的 `1672` vs `1676`，以及 §8 的校验器缺失）。双 agent 并行写同一个 `LATEST.md` 是本线的固有结构，单靠人眼对齐数字必然漂移。

3. **变异测试应当成为新测试的门槛**（依据：§5 的探测力分层 —— 6/16 是子串检查、2/16 零探测力，而变异实验能立刻把它们分出来）。做法很小：对这 6 个缺陷各回退一次，要求至少一个测试失败。

> **三者都是 `程序修复` 或审计流程改进，`任务定义变更 = 无`，`真实模型增益 = 无`（本轮不含任何模型实验，不得把软件测试计入模型增益）。**

---

## 10. 声明

- 本轮 `execution_status = COMPLETED_READ_ONLY_AUDIT`；`research_verdict` 见文首；`evidence_status = REAL_CODE_MEASUREMENT`。
- **`evidence_type` 全部为软件样本**；**本轮没有新增实股结果**，没有训练，没有拟合，没有模型对比。
- 本报告 `docs-only`：只新增本文件 + 追加式更新 `LATEST.md`。**未改任何源码/权重/配置/Actions/PR。**
- 我**没有**触碰并行作者的 `2026-09-21_21-00-00_JST.md`、`NEXT_STEPS.md`、`2026-09-21_20-10-37_JST*`；也没有还原/暂存任何未提交改动。
- 审计途中 `main` 从 `b858760c2ef9f676198f2f504829dd8989694457` 前进到 `fd51674508cb237c844d6a4924b7e6a2f084c4b6`（作者提交并推送）。我如实记录，并把全部结论重新锚定到 `fd51674508cb237c844d6a4924b7e6a2f084c4b6`；`git status` 在我读完后为空。
- 未复现项已逐条标 `未复现` 并降级（§6 的 R-12 百分比、DAEMON-RACE 竞态）。
- **未创建、修改或删除任何定时任务或排程**（未调用 `schedule_*`、`arm_self_wake`、`schtasks`、`Register-ScheduledTask`）。

---

## 固定来源

```text
仓库            D:\ccc\ashare-radar
HEAD(审计中)     fd51674508cb237c844d6a4924b7e6a2f084c4b6
origin/main     fd51674508cb237c844d6a4924b7e6a2f084c4b6
ls-remote       fd51674508cb237c844d6a4924b7e6a2f084c4b6     （三者相等 = True）
上一轮已审产品   6997487f0ec5d92f81299edbb8c68845e9e056a8
commit 起点      b858760c2ef9f676198f2f504829dd8989694457
被判提交         fd51674508cb237c844d6a4924b7e6a2f084c4b6  fix(alerts): 交付账本假绿/ring-buffer/soak 盲区等 7 条缺陷 + 3 处自我更正
被改文件         src/arad/capabilities.py / src/arad/engine.py / src/arad/server/web.py /
                tools/live_session.py / tests/test_replay_capability_ledger.py /
                tests/test_delivery_false_green.py（新）+ 5 个 docs
未改（重要）      src/arad/store.py / models.py / rules/base.py / server/dashboard.html /
                notifiers/* / sources/* / tools/run_daemon.py
关键行           capabilities.py:74-76,763-766,772,792-816,818-830
                engine.py:1341,1358,1363
                store.py:47,55,113,332-347,349-351
                live_session.py:141,154,755,787-789,795,985-1310
                models.py:590   web.py:272,290
                测试 test_delivery_false_green.py:44,63,131,298
                test_replay_capability_ledger.py:140,141,148,150
本轮脚本         D:\ccc\_sched\work\it_h20_*.py（含 .log / .txt）
```

**无编造。** 所有 SHA 由 `git rev-parse` / `ls-remote` 实测；所有行号由 `git show` 读出；所有通过数/退出码为真实运行结果；未复现项已标注并降级。
