# 本地 Agent 任务书 — 2026-09-24 16:02:09 JST

## 固定基线

- repo：`fy-god/intraday-tape-reader`
- fixed product SHA：`7a4f549a15e78fe87db7de00796eff71b707ae9b`
- audit-start main：`5d9c946ef8eb9bf9a855cb7d3cdcf5c2f2af5483`
- 开工前重新 fetch；docs commit 不算产品升级。
- 禁止真实通知、交易、自动 push、改 Actions、持续抓取/付费服务。

## WP01 — Sina Index Wire Identity【P1，第一优先】

目标文件：
- `src/arad/sources/sina.py`
- `tests/test_sources_sina.py`

RED：
```text
src.snapshots(["sh000001"])
```

FakeFetcher 实际 URL 必须：
```text
https://hq.sinajs.cn/list=sh000001
```

当前旧逻辑：
```text
_norm_code -> 000001
guess_prefix -> sz
=> sz000001
```
必须行为 RED。

默认 5 index table 全覆盖：
```text
sh000001
sz399001
sz399006
sh000300
sh000688
```

## WP02 — Legacy Quote identity

指数响应：
```text
var hq_str_sh000001="上证指数,...";
```

必须：
```text
Quote.code = sh000001
Board.INDEX
```

普通股票控制：
```text
sh600000 -> Quote.code=600000
sz000001 -> Quote.code=000001
```

不能因为修指数把 stock identity API 改坏。

## WP03 — Detailed index identity

```text
snapshots_detailed(["sh000001"], route="index")
```

必须：
```text
R = P = Q = {"sh000001"}
raw_presence_known=True
missing=empty
unexpected=empty
```

禁止 wrong wire security 仍被 exact ledger 认证。

## WP04 — Mutation / rollback

把代码注入回：
```text
strip explicit prefix
+ guess_prefix(bare)
```

上述 index tests 必须行为红。

## WP05 — Route-State recheck

Source 修完后再重跑：
- route/time same-code collision；
- state history/latest/day_open identity；
- current_indices 与 stock 000001。

若正确 index identity 全程保持 prefixed，
则下调/关闭之前依赖 bare-index 的碰撞假设。
不要未经复验直接做全量 route-key State 重构。

## WP06 — StateUpdateResult

完成 WP01–05 后继续上一轮：
route-local future/ooo/stale identity。

## WP07 — Per-route RoundObservation

SnapshotFetchResult + StateUpdateResult。

## WP08 — Eastmoney Pagination

独立落已有 RED/GREEN。

## WP09 — Membership / exact soft-empty / timezone/window

继续 downstream。

## WP10 — Research

真实数据事实链稳定后再开始训练。

## 必须回传

```text
git rev-parse HEAD
git status --porcelain
git diff
diff SHA256

sina_index_wire_red.log
sina_index_wire_green.log
sina_index_wire_rollback.log

sina_index_quote_red.log
sina_index_quote_green.log
sina_index_quote_rollback.log

sina_index_detailed_red.log
sina_index_detailed_green.log
sina_index_detailed_rollback.log

default_index_matrix.json
sina_index_cases.json
route_state_recheck.json

full_pytest.log
check_*.py logs
dash_render_check.log

dataset_manifest.json
task_spec.json
split_manifest.json
factor_catalog.csv
factor_diagnostics.csv
trials.jsonl
training_log.jsonl
checkpoint/hash（若训练）
predictions.csv（若训练）
ablation.csv（若训练）
event_reconcile.json
latency_memory.csv
RUN_MANIFEST.json
NEXT_STEPS.md
```

所有未运行项必须 `not_run/blocked + 原因`。

## FAIL 条件

- 任一显式 index prefix 在 HTTP 前被剥掉再猜；
- `sh000001` 实际请求 `sz000001`；
- detailed R/P/Q 对 wrong instrument 仍 `identity_holds`；
- 修 index 后显式普通股 API 被改坏；
- rollback 后 RED 不恢复；
- 在 Source identity 未闭合前直接继续 WP07 / 模型训练。
