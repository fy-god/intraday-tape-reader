# 盘中预警审计 — 2026-09-22 00:43:31 JST

## §0 本轮身份与证据边界

| 项 | 值 |
|---|---|
| 仓库 | `D:\ccc\ashare-radar`（`fy-god/intraday-tape-reader`） |
| 默认分支 | `main` |
| `main_head_at_audit_start` | `e57bb3b87baacefd4e8abd908a5bc17877179ebf` |
| `reviewed_source_sha` | `e57bb3b87baacefd4e8abd908a5bc17877179ebf`（= HEAD，实际 `git rev-parse` 读出） |
| 父提交 | `eca980ec6f1c8c01d6857842f9b6f084ffb12e7d`（实际 `git rev-parse HEAD^` 读出） |
| 上一份云端独立审计 | `2026-09-22_00-08-36_JST.md`（`reviewed_source_sha = fd516745...`） |
| 待审范围 | `fd516745..origin/main` = 4 个提交，其中**唯一含产品源码的是 `e57bb3b`** |
| 工作区 | `git status --porcelain` = **0 行**（无他人 WIP） |
| 本轮 `report_created_commit_sha` | `99c2cb2ce555ed8313dd7fff84e7b97d225e62d2`（`git ls-remote` 回读 = 本地 HEAD，EQUAL） |
| 开放 PR | 未查询（本轮不依赖） |

### 本轮实际执行的命令（真实退出码）

| 命令 | 退出码 | 真实输出摘要 |
|---|---|---|
| `python -m pytest tests/ -o addopts= -ra` | **0** | `1694 passed in 109.69s (0:01:49)` |
| `python -m pytest tests/test_verdict_layer_and_pin.py -v` | **0** | `collected 18 items ... 18 passed in 0.23s` |
| 新测试文件跑在**父提交未打补丁源码**上 | **1** | `7 failed, 11 passed in 0.56s`（全是 `AssertionError`，**0 结构性 ERROR，无 `ImportError`**） |
| `tools/check_*.py` × 8 | **0** ×8 | 8/8 全绿 |
| `node tools/dash_render_check.js` | **0** | 通过 |
| `python -m arad.cli selftest`（`PYTHONPATH=src`） | **0** | `68 条 / 6 种类型 / 8-8 剧本命中` |
| `python docs/audits/validate_latest.py` | — | **不存在**（见 §7） |

**证据纪律说明**：本轮所有数字均来自本轮真实命令。`1694` 与产品轮自报一致；`18 passed`、`7 failed/11 passed`、`8/8`、`selftest 68/6`、`node exit 0` 均为本轮独立复现。

---

## §1 一句话结论

**产品轮 `e57bb3b` 的三条修复我全部独立复现成立，其中判决层那条连"修复前逐字节不变"都复现出来了 —— 但同一"有界容器被当成完整总量"的 bug 类仍有至少 3 个存活实例，且产品轮自己列的最严重未修项 `R-21`（universe 无判决项）我把根因从"缺判决项"精确到了"数据早已传入、只差一个读者"，并给出了通过全部 18 条新测试的可落地补丁。**

---

## §2 待审提交 `e57bb3b` 的独立复核

### 2.1 `IT-H20-DELIVERY-ACCOUNTING-GATE-MISSING`（判决层假绿）→ **已经修复**（判据成立，非空转）

**文件/函数/行**：`tools/live_session.py:1294-1343`（新块）内 `:1318 _dl = m.get("delivery_accounting_session")`、`:1326` 判据、`:1327/:1334/:1338/:1342` 四个 `add("delivery_accounting", ...)`。

**触发条件/反例**：注入 `accounting_status="inconsistent" / accounting_errors=7 / committed_alerts_total=0 / committed_with_signal_id=66`。

**A/B 实测**（`it_h00_ab2.py`，父提交 `eca980ec` 全树 `git archive` 到 scratch 后子进程运行，**仓库零写入**）：

| 版本 | 健康基线 | 注入坏账本 | 判决项 | checks 逐字节相同？ |
|---|---|---|---|---|
| PARENT `eca980ec` | `healthy=True exit=0`，10 项 | **`healthy=True exit=0`**，10 项 | 10 | **是** |
| HEAD `e57bb3b` | `healthy=True exit=0`，11 项 | **`healthy=False exit=1`**，`fail=['delivery_accounting']` | 11 | 否 |

→ **假绿在父提交真实存在，在 HEAD 真实消除。**

**空转证明（硬规则 gg/hh）**：新测试文件跑在**未打补丁的父提交源码**上 = `7 failed, 11 passed`，全部是 `AssertionError`（例：`assert 'delivery_accounting' in [...10 项]`）；跑在 HEAD 上 = `18 passed`。**先红后绿，检测力成立。** 7 条行为级 RED / 0 结构性 ERROR / 无 `ImportError` 与产品轮自报**逐字一致**。

**机制**：`evaluate_health` 的 10 项 `rounds/fetch/data/coverage/capability/api/sse/memory/browser/alerts` 无一项读 `delivery_accounting_session`；`:2168 finalize_metrics(...)` → `:2181 evaluate_health(metrics)` 在生产路径 `run()` 内被调用，**修复在生产可达**。

**`not_measured` 判 ok 的决定**：成立。仓库四个"应当判健康"的 helper（含 `tests/test_verdict_layer_and_pin.py:34-73 _healthy_metrics()`）产出的都是 `not_measured`；判红会让全部绿色用例转红。

**判据不使用被夹的 `unsigned_total`**：成立。`capabilities.py:770` 写 `max(total-named,0)`；`live_session.py:1326` 用的是 `accounting_status`/`accounting_errors`，未读该夹值。

**修复建议**：无（正确）。**验收测试**：`tests/test_verdict_layer_and_pin.py` 已覆盖。

### 2.2 `IT-P1-UNIVERSE-WATCHLIST-PIN-001`（临时降级变永久）→ **已经修复**

**文件/行**：修复点 `src/arad/engine.py:1056 self._codes_raw = list(self.watchlist)` + `:1059 self._codes_pinned = False`。setter 契约 `:761-765`；短路 `:1033 if self._codes_pinned and not force: return`。

**A/B 实测**（真实 `Engine` + 真实 `AlertStore` + 注入 universe 源，父/子两棵树分别子进程运行）：

| 版本 | 降级后 `_codes` | 降级后 `_codes_pinned` | 源恢复 + TTL 过期后 `refresh_universe` 调用 | 最终 `_codes` |
|---|---|---:|---:|---|
| OLD `e57bb3b^` | `['000001','000002']` | **`True`** | **0** | 2 |
| NEW `e57bb3b` | `['000001','000002']` | `False` | **1** | **3000** |

**正当 pin 未被破坏**（真实执行，非仅测试）：`--watch-only` 在 `engine.py:1551`、replay 在 `replay.py:583` 均得 `_codes_pinned=True`，其后真实非 force 调用 **0** 次刷新，新旧树逐字节相同。

**穷举 `self._codes`/`_codes_pinned` 全部写入点**（`src/` 全量）：

| 位置 | 性质 |
|---|---|
| `engine.py:1551`（`--watch-only`） | 有意 pin（非空，`:1547-1550 if not self.watchlist: return` 守卫） |
| `replay.py:583`、`cli.py:227` | 有意 pin |
| `engine.py:765`（setter 自身） | 契约 `bool(codes)` |
| `engine.py:958/959`、`977/978` | 有意 unpin（`_codes_raw` 直赋） |
| `engine.py:1056/1059` | 本轮修复点，unpin |
| `tools/probe_live_ready.py:67` | pin 无害（随后 `poll_once(force=True)` 绕过 pin） |

→ **无残留误 pin 路径。** 一处已核查非缺陷：修复在 `pin=True 且 _codes=[]` 这一个此前不自洽的状态上改变行为；该状态无生产路径可构造（所有 pin 写入都写非空表，`refresh_universe` 从不清空 `_codes`），属潜在 API 改善，非回归。

**一处文档漂移（非代码缺陷）**：提交信息写 `--watch-only (~1536)`，HEAD 实际 setter 在 **`engine.py:1551`**（1536 是 `def run_forever`）。

### 2.3 `IT-P1-ALERT-TOTAL-KIND-TRUNCATION-001`（按 kind 的 total 被 ring buffer 截断）→ **核心已经修复**，但退化分支引入**更窄的新截断**（修复后回归，P3）

**文件/行**：`src/arad/server/web.py:595 def _alerts_total`，新逻辑 `:598-618`，退化路径 `:622-623`，外层 `except` `:624-626 return len(items)`。`ALERT_TOTAL_PROBE = 1000`（`:109`）。所有者类 `DashboardHandler`（`:448`）。

**截断真实存在**（真实 `AlertStore(max_alerts=300)` + 450 条 `limit_up`）：

| 口径 | 值 |
|---|---:|
| 灌入 `limit_up` | 450 |
| `alerts_total()` | **450** |
| `status()['by_kind']['limit_up']` | **450** |
| `len(recent_alerts(1000,'limit_up'))`（旧口径） | **300** |

**真实端到端 HTTP**：`GET /api/alerts?kind=limit_up` → `200 {"total":450,"count":300}`；`?limit=5&kind=limit_up` → `total=450`。修复前同一调用 `= 300`。

**`by_kind` 无重置点**：`src/arad/store.py` 仅两处变更 —— `:57` 初始化、`:115` 每条 +1（`_total` 同样只有 `:57`/`:113`）。无日切/会话重置 ⇒ 进程生命周期内单调不减，可超过 ring buffer（这正是目的），**不会向下重置**。非缺陷。

#### ⚠ 新发现：退化分支的截断（`待验证风险` P3 / `修复后回归`）

**文件/行**：`src/arad/server/web.py:598`（新代码在 kind 路径上**无条件**调用 `self.store.status()`）+ `:624-626`（异常兜底 `return len(items)`）。

**触发条件/反例**：真实 store 的 `status()` 抛异常、而 `recent_alerts()` 正常。此时新代码在**够到** `by_kind` 之前就抛进外层 `except`，退化成 `len(items)` = 本页 `limit`。

**真实计数**：真实 450 条 `AlertStore` + `status()` 抛异常的包装 → **OLD = 300，NEW = 100**（路由传 `limit=100`）。即**修复让这条降级路径更差**。

**影响面**：仅当真实 `DashboardHandler` 的 store `status()` 抛异常时可达。仓库自带的 `NullStore`（`:368`）**不抛**，但其 `status()` 无 `by_kind` 键 → 走有记录的截断兜底；实测它不含告警（`total=0`），无害。除此之外无别的 shipped store 缺 `by_kind`。

**修复建议**：把 `status()` 调用放进**独立**的 `try`，其失败**不得**吞掉后面的 `recent_alerts` 探测；或先算 `items`/`recent_alerts` 兜底、再尝试 `status()` 覆盖。

**验收测试**：`status()` 抛异常 + `recent_alerts` 正常 ⇒ `total` 必须 ≥ 旧口径 300，不得等于 `len(items)`。

### 2.4 新增判决块自身的两处残留缺陷

**判定层修好了，但新加的这段代码自己带了两处问题。**

#### D2（`修复后回归`，P2）—— 脏 `inconsistent_rounds` 让 `evaluate_health` **崩溃**

**文件/行**：`tools/live_session.py:1323 _dl_bad = _dl.get("inconsistent_rounds") or []` → `:1329 f"...{list(_dl_bad)[:5]}..."`。

**触发条件/反例**：走到失败分支（`status=="inconsistent"` 或 `errors>0`）时，若 `inconsistent_rounds` 是**真值但不可迭代**的标量（`int`/`float`/`bool`），`list()` 抛 `TypeError`。

**A/B 实测**（父提交 `eca980ec` 的 `evaluate_health` 从其 blob `d469ec7b939832beba43e3d9f9cd6d0a9917b5b3` 载入对照，`it_h00_d2b.py`，exit 0）：

| 输入（在失败分支内） | HEAD | PARENT |
|---|---|---|
| `status=inconsistent, rounds=5`（int） | **`RAISED TypeError: 'int' object is not iterable`** | `healthy=True exit=0` |
| `status=inconsistent, rounds=1.5` | **`RAISED TypeError: 'float' ...`** | `healthy=True exit=0` |
| `status=inconsistent, rounds=True` | **`RAISED TypeError: 'bool' ...`** | `healthy=True exit=0` |
| `errors=7, rounds=5` | **`RAISED TypeError: 'int' ...`** | `healthy=True exit=0` |
| `status=inconsistent, rounds=[1,2]` | `healthy=False exit=1` | `healthy=True exit=0`（= §2.1 的修复本体） |
| `status=ok, rounds=5`（不进分支） | `healthy=True exit=0` | `healthy=True exit=0` |

⇒ **4 个用例 HEAD 崩溃而父提交正常 —— 这是对父提交的回归**，且**违反该块自己写下的契约**（`:1310-1311` 注释第 2 条："键缺失 / 非 dict **不崩**"，并点名 `tests/test_live_session_observation.py` 就是钉这条的）。

**测试为何仍是绿的**：仓库自带的脏输入用例（`tests/test_verdict_layer_and_pin.py:166-171`）只覆盖 `{}`、`accounting_status=None`、`inconsistent_rounds="notalist"`、`None` —— **独独没有标量 `int/float/bool`**，所以 18 条全绿而契约已破。

**可达性（诚实边界）**：shipped producer 在 `:805` 写 `acct_status_bad_rounds`（由 `.append` 构造的 `list`）、`:944` 写 `[]`，**永远是 list** ⇒ 该崩溃只在**手写/外部** metrics 对象上触发；本轮**未**在真实 soak 中复现。定级 `修复后回归`（父提交不崩、HEAD 崩）但可达性有限。

**修复建议**：`_dl_bad = _dl.get("inconsistent_rounds")` 后做类型收敛，例如 `list(_dl_bad) if isinstance(_dl_bad, (list, tuple, set)) else ([_dl_bad] if _dl_bad not in (None, "") else [])`。
**验收测试**：把 `int`/`float`/`bool` 加进已有的 parametrize 列表；断言**不抛异常**且 `healthy is False`。该用例在 HEAD 上失败、修复后通过（非空转）。

#### D1（`待验证风险`，P2，纵深防御）—— 判决层**信任生产者自报的 label**，不独立复核 `named <= total`

**文件/行**：`tools/live_session.py:1324 _dl_total` / `:1325 _dl_named` **只出现在** `:1330` 的 detail f-string 里；真正的判据 `:1326` 是 `_dl_status == "inconsistent" or _dl_errs > 0` —— **从不重新检查 `named <= total`**。

**本轮自己的实测**（`it_h00_identity.py`）：构造 `committed_alerts_total=66 / committed_with_signal_id=1000`（分子 > 分母，逻辑不可能）但 `status="ok" / errors=0` ⇒ `healthy=True`、该项 `ok=True`、detail 却写着 `committed=1000/66`。`total=0 / named=66` 同样 `healthy=True`。**判决层对"账本内部自相矛盾"是盲的**，它只转述生产者的结论。

**为何现在不是"已确认错误"**：生产者 `src/arad/capabilities.py:792-816 delivery_accounting_status()` **确实**显式判 `named > total → INCONSISTENT`（`:813-815`），且 `:770` 的门禁用 `max(total-named,0)` 夹值。所以 shipped 生产链今天**不会**产出 `status=ok` 且 `named>total` 的组合 ⇒ 该路径当前**不可达**。

**为何仍要报**：这正是本轮 §2.1 修复的**同一个认识**（"门禁为 0 不能证明账本健康"）没有贯彻到底 —— 消费者应当**独立复算** `named <= total`，而不是转述 label。一旦生产者端的 label 逻辑再退化（本项目历史上已发生过多次"数据层修好、消费层不读"的搬家），判决层会**同时**失效。**纵深防御缺口**，非当前可利用缺陷。

**修复建议**：`:1326` 的判据加上 `or _dl_named > _dl_total > 0`。
**验收测试**：注入 `status="ok" / errors=0 / total=66 / named=1000` ⇒ 必须 `healthy=False`。该断言在 HEAD 上失败（当前 `ok=True`）、修复后通过。

---

## §3 `R-21` 复核：universe 没有判决项 —— **已确认错误**，且根因比报告说的更精确

### 3.1 确认部分

**文件/行**：`tools/live_session.py:984-1343`（`evaluate_health` 全体）内 `add()` 名称全集 = `rounds/fetch/data/coverage/capability/api/sse/memory/browser/alerts/delivery_accounting`（源码正则枚举，11 项）。字面 token `universe`/`setup`/`universe_size`/`fell_back_to_watchlist` 在 `evaluate_health` 体内**均不出现**（实测 `False`）。

**真实运行**：健康基线 `quotes={min:5000,max:5913,last:5913}` 下，把 universe 缩到 4100 而 return 率不变 → `healthy=True exit=0`，与基线**逐项相同**。缩到 500（严重）同样 `healthy=True exit=0`。

**控制组（关键的鉴别）**：仅把 coverage 降到 0.30（universe 字段不动）→ `healthy=False exit=1 fail=['coverage']`。

→ **`coverage` 是"我决定扫的回来了多少"的传输率，不是市场覆盖面**：它读 `capabilities.py:867-869 self.returned/self.requested`。当扫描范围**整体**缩小时分子分母同时缩小，比率**不变**，故 `coverage` 结构性地看不见范围塌缩。

### 3.2 根因（本轮把报告的说法精确化）

报告 §R-21 只说"universe 仍然没有判决项"。真正的根因是：**数据早已在生产路径传入，只是没有读者。**

- 生产路径 `tools/live_session.py:2056-2058 `universe_size = int(engine.refresh_universe() or 0)``，`:2064 fell_back = bool(codes) and len(codes) <= len(watch)`；
- `:2174-2179` 把它们装进 `setup={"universe_size": universe_size, "fell_back_to_watchlist": fell_back, ...}` 传给 `finalize_metrics`；
- 而 `evaluate_health` **从不读 `setup`**（实测）。

即：**这不是"缺一个生产者"，而是"缺一个消费者"**。修复不需要新增采集、不需要改引擎、不需要联网。

### 3.2b 可达性：从"理论上会"升级为"生产路径真的会"（**已确认错误**）

产品轮把 `R-21` 列为"待验证风险"。本轮用**真实 `Engine.refresh_universe`**（未 monkeypatch 任何逻辑，仅注入符合契约的源）证明它**在生产路径可达**：

`src/arad/engine.py:968-969` 的守卫是 `prev = len(self._codes_raw)` / `if prev and n < prev: return 0`（拒绝更小的部分池）。**会话的第一次刷新 `prev == 0`，`prev and ...` 为假 ⇒ 守卫不生效。**

实测（`it_h00_reach2.py`，exit 0）：

| 场景 | `_codes_raw` 前 | `refresh_universe()` | `_codes_raw` 后 | 采用的 `_universe_meta` |
|---|---:|---:|---:|---|
| **A. 冷启动 + 部分池（complete=False, 4100/5913）** | 0 | **4100** | **4100** | `complete=False, expected_total=5913, shortfall=1813` |
| B. 热池 5913 + 同一部分池 | 5913 | 0 | 5913 | 不变 |
| C. 冷启动 + `complete=True` 自报总量 4100 | 0 | 4100 | 4100 | `complete=True, expected_total=4100, shortfall=0` |

→ **A 证明守卫在冷启动时形同不存在**：一个 30% 缺口的池子会被采纳，并在整个 soak 期间保留；`_universe_meta` 如实记下 `shortfall=1813`，但**没有任何生产读者消费它**（§5 `R-12`），且**没有 `universe` 判决项**（§3.1）⇒ **静默采纳 + `healthy=True / exit 0`**。
→ B 同时证明：报告所称赞的"更小部分池会被拒绝"这条守卫**确实工作** —— 但只在热池路径上工作。

**诚实边界**：本轮**没有**在真实联网长跑中观测到 `5913→4100` 事件本身；可达性是用**符合契约的桩源**证明的。`evidence_type = SYNTHETIC`。

### 3.3 可落地补丁（scratch 验证，**未写入仓库**）

在 `evaluate_health` 的交付块之后插入一个 `universe` 项，判据用**本会话自身的观测上限**（`m['quotes']['max']`）做基准，**不发明绝对阈值**（实测 `DEFAULT_TOLERANCES` 里**没有** `min_universe`）：

| 情形 | OLD | PATCHED |
|---|---|---|
| 健康基线（helper `universe_size=0`，未测得） | `True/0` | `True/0`（跳过，**不算失败**） |
| 真实健康会话 5913 / 上限 5913 | `True/0` | `True/0` |
| **报告所述 5913→4100** | `True/0` | **`False/1 fail=['universe']`** |
| 严重 5913→1500 | `True/0` | **`False/1`** |
| `fell_back_to_watchlist=True` | `True/0` | **`False/1`** |
| 无 `setup` 键 / `setup` 非 dict | `True/0` | `True/0`（容错） |
| 无 `quotes` 键但 universe 已测 | `False/1`（原有他项） | `False/1`（`universe` 仍 ok） |

**边界精确**：阈值 0.70 时 `0.70 x 5913 = 4139.1`，故 `4139` 判失败、`4140` 判通过 —— 恰好覆盖报告举的 4100。
**零回归**：补丁后 `tests/test_verdict_layer_and_pin.py` + `tests/test_delivery_false_green.py` 共 **34 passed**（原 18 + 16）。
**注意**：阈值只能经 `evaluate_health(metrics, tolerances={...})` **参数**注入；写成 `metrics["tolerance"]` 会被静默忽略（`tol = dict(DEFAULT_TOLERANCES); tol.update(tolerances or {})`，`:1016-1017`）。

**证据状态**：`5913/4100` 是**合成机制夹具**（`LATEST.md:115` 已自我声明），**不是**重测的真实市场覆盖率 ⇒ `evidence_type = SYNTHETIC`，本轮**没有真实实股结论**。

---

## §4 第三类实例：同一 bug 类仍有存活实例（**已确认错误**）

产品轮 §1.3 自述"同一 bug 类的第二个实例"，并写下教训"修一个可观测性缺陷必须走到有人改变结论为止"。本轮按此教训在全仓扫描**"有界容器/累计缓存的长度被当作完整总量"**，找到**存活实例**：

### 4.1 `tools/live_session.py:1971`：把**从不裁剪**的累计行情缓存当作**逐轮**行情数（P1）

**文件/行**：`tools/live_session.py:1971 quotes=len(getattr(state, "quotes", {}) or {})` → `:1966 make_round_sample(...)` → 落进 `metrics['quotes']`。

**触发条件/反例**：provider 连续多轮返回 0 条（真实断供），`state.quotes` **不会缩小**。

**真实运行**（真实 `EngineState` + 真实 5913 只，5 轮空批，`it_h00_third_instance2.py`，exit 0）：

| 轮 | `len(state.quotes)` | `sample['quotes']` | 应为 |
|---:|---:|---:|---:|
| 1（5913 条） | 5913 | 5913 | 5913 |
| 2（0 条） | 5913 | **5913** | **0** |
| 3（0 条） | 5913 | **5913** | **0** |
| 4（0 条） | 5913 | **5913** | **0** |
| 5（0 条） | 5913 | **5913** | **0** |
| 6（0 条） | 5913 | **5913** | **0** |

`st.prune(keep_codes={'sh600001'})` 丢弃 5912 只历史后，`len(state.quotes)` 仍 = **5913** ⇒ **`state.quotes` 从不裁剪**。

**机制**：`src/arad/engine.py:302 self.quotes[q.code] = q` 是**唯一**写入；`engine.py:417-446 prune()` 回收 `history/last_price/watermark/first_seen/...` **但没有 `self.quotes`**。累计缓存被逐轮取样。

**影响（具体计数）**：`tools/live_session.py:671 "no_data_rounds": sum(1 for r in rows if int(r.get("quotes") or 0) <= 0)` —— 因为 `quotes` 恒 > 0，**该计数器在断供时结构上恒为 0**；`evaluate_health:1088 add("data", quotes_max > 0, ...)` 的 `quotes_max` 同样恒为旧值。实测 6 轮全断供：`no_data_rounds=0`、`error_rounds=0`、`item data: ok=True detail='单轮最多拿到 5913 只行情'`。**两个本应报警的探测器同时失效**。

**旁证**：`tests/test_live_session_observation.py:5` 的 docstring 自己写着这个坑（"_soak_loop() 以前把 len(state.quotes) 当'行情数'…那是累计缓存"）—— 说明该认识**已存在**，但修复只落在 docstring/`finalize_metrics`，**没有落在写入点**。

**修复建议**：写入点改用**本轮的**返回计数（`len(quotes)`/`admitted`，来自 `:1096/:1148` 的 per-round 变量），或把 `state.quotes` 的裁剪纳入 `prune()`。**验收测试**：连续 N 轮返回 0 条 ⇒ `no_data_rounds == N` 且 `data` 项判失败。

### 4.2 `src/arad/store.py:359`：`_acked` 裁剪保留的是**任意** 2000，不是**最新** 2000（P2，用户可见）

**文件/行**：`src/arad/store.py:359 self._acked = set(list(self._acked)[-2000:])`（`_acked: set[str]` 声明于 `:56`，写入 `:357`，阈值 `:358`）。

**触发条件/反例**：用户 ack 的告警**仍留在** 300 条 ring buffer 内，但 `_acked` 被 5001 次 ack 触发裁剪。

**机制**：`list(set)` 的顺序是**哈希序**，不是插入序 ⇒ `[-2000:]` 取的是**任意** 2000 条，**系统性地丢掉刚 ack 的那条**。

**真实运行**（真实 `Alert` + 真实 `AlertStore`，`it_h00_acked4.py`，exit 0）：

| 判别式 | OLD | NEW（有序裁剪） |
|---|---|---|
| `len(_acked)` | 2000 | 2000 |
| `set(_acked) == 最新 2000` | **False** | **True** |
| 最新那条 key 存活 | **False** | **True** |
| 最旧那条被丢弃（证明裁剪确实运行） | True | True |

**跨哈希种子**：`PYTHONHASHSEED` ∈ {0,1,7,12345,99999} 下 OLD 的 `==最新2000` 全部为 **0**，且 seed=0 时连**最新 ack 都被丢**。

**用户可见形式**：`VISIBLE-KEY` 在 ring buffer 内**可见**、用户已 ack，但 `recent_alerts()` 报 `acked=False`（OLD），修复后 `True`。消费端为 `src/arad/server/dashboard.html:474 if(a.acked) el.classList.add("acked")`（`:123-124` 变暗并**隐藏 ack 按钮**）⇒ 用户会看到**自己刚确认过的告警重新变成未确认**。

**测试覆盖**：全仓**无**任何测试触及 5000 裁剪阈值（实测 grep `5000|_acked|trim` 无命中）。

**修复建议**：改为保序容器（`dict[str, None]` 或 `OrderedDict`），使 `[-2000:]` 语义为"最新 2000"。**验收测试**：灌 5001 个 key 后断言 `set(_acked) == set(keys[3001:])` 且最新 key 存活（该断言在 OLD 上失败、NEW 上通过）。
**零回归**：补丁后 `tests/test_server_web.py` + `tests/test_engine.py` = **129 passed**（exit 0）。

### 4.3 `src/arad/store.py:304`：`status()['universe'] = len(quotes)` 同样用累计缓存（P3）

`status()` 导出 `"universe": len(quotes)`（`engine.py:103` 的 `state.quotes` 累计缓存）。看板"股票池"芯片（`dashboard.html:250/423`）读它 ⇒ 扫描范围缩 30% 后仍显示旧规模（本轮实测缩 30% 时**高报 1813**）。与 §4.1 同根因，用户可见度较低。

---

## §5 复核报告其余新增风险

| ID | 报告说法 | 本轮复核 | 证据 |
|---|---|---|---|
| `R-22` | `_scope_note` 是死键 | **成立** | 定义 `src/arad/capabilities.py:764`；`tools/live_session.py:323` 以 `dict(acct)` **不透明拷贝**；唯一读者 `tests/test_delivery_false_green.py:224`。**零生产读者**。且它指向的 `delivery_session` 键**全仓不存在**，只有该说明字符串本身。 |
| `R-23` | 仓库没有任何 CI | **成立** | 228 个 tracked 文件；无 `.github/`（磁盘与 `ls-files` 均无）、无 `*.yml/*.yaml`（仅 `config/settings*.yaml`）、无 Jenkinsfile/Makefile/tox/nox/pre-commit。`README.md:319` 只有散文建议。`validate_latest.py` 亦**不存在**（见 §7）。硬性边界内不能改，如实报告。 |
| `R-21` | universe 没有判决项 | **成立（升级为"已确认错误"）** | `evaluate_health` 11 项全集实测，token `universe/setup/universe_size/fell_back` 均**不出现**；真实运行 5913→4100 得 `healthy=True exit=0`；**冷启动可达性已用真实 `Engine.refresh_universe` 证明**（§3.2b） |
| `R-12` | `_universe_meta` 引擎已算出覆盖率却零出口 | **成立** | 写入 `src/arad/engine.py:961`、`:980`；读取仅 `:877 _universe_meta_of`（同模块内部）与 `tests/test_engine.py:929-930`。**无产品出口**。且 `capabilities.py:867 def coverage()` docstring 自述是"本轮返回率"=`returned/requested`，**不是**市场覆盖面。 |
| `R-13`/`R-14`/`R-15`/`R-17`/`R-20`/`R-01`~`R-11`/`R-16`/`R-18`/`R-19` | 上轮遗留 | **未触碰**（本轮聚焦判决层与 bug 类，未对每条重做复核）。如实记录为未推进，**不冒充已复核**。 |

---

## §6 对产品轮结论的下调/上调

1. **上调**：`e57bb3b` 的判决层修复**确实是真修**，不是"字段有了"。我独立复现了"父提交注入坏账本后 10 项逐字节不变 / HEAD 变 11 项且 `fail=['delivery_accounting']`"，并用"新测试跑在未打补丁源码上 7 failed"证明了检测力（非空转）。产品轮 §3 的"7 条行为级 RED / 0 结构性 / 无 ImportError"逐字可复现。
2. **精确化**：§R-21 的根因不是"缺判决项"，而是"**数据已传入 `setup`、只差一个读者**"。这把它从"需要新采集的设计任务"降级为"**一个小补丁即可闭合**"，且我已给出通过全部 18 条新测试的原型。
3. **新发现（产品轮未提）**：同一 bug 类**仍有 3 个存活实例**（§4.1 最严重：判决层两个探测器同时失效；§4.2 用户可见）。产品轮 §1.3 自述"同一 bug 类我漏了第二个实例"——本轮说明**漏的不止第二个**。
4. **新增下调**：`IT-P1-ALERT-TOTAL-KIND-TRUNCATION-001` 从"已经修复"下调为"**核心已修 + 退化分支回归**"（§2.3）。
5. **新发现（产品轮未提）**：本轮新加的判决块**自身**带两处残留缺陷 —— D2（脏 `inconsistent_rounds` 使 `evaluate_health` 崩溃，**对父提交的回归**）与 D1（判据转述生产者 label、不复算 `named <= total`）。见 §2.4。

### 审计方自我更正（第 8 次同类失误）

本轮派发给子 agent A 的提示词里，我把父提交 SHA 手写成了 `...1d6842f9b...`，**真实值是 `eca980ec6f1c8c01d6857842f9b6f084ffb12e7d`（我漏了一位）**。子 agent A 独立用 `git rev-parse e57bb3b^` 复核并纠正了我。

**这是本会话第 8 次同类缺陷**（前 7 次是手抄 40 位 SHA 时丢字符）。本报告内所有 SHA 均改为**从 `git rev-parse` 派生**（如 §0 表内的 HEAD / 父提交、§2.4 内父提交 blob `d469ec7b...`），不再手写期望值。如实记录，不隐藏。

### 统计口径（三者不得混算）

| 轴 | 本轮值 |
|---|---|
| `execution_status` | `COMPLETED`（报告 + 索引 + 推送） |
| `research_verdict` | `已确认错误` 3 项（R-21、§4.1 逐轮行情数、§4.2 `_acked` 裁剪）、`待验证风险` 2 项（§2.3 web 退化分支 P3、§2.4 D1 纵深防御 P2）、`已经修复` 3 项（`e57bb3b` 的三条）、`修复后回归` 2 项（§2.3 web 退化分支、§2.4 D2 崩溃）、`未复现` 0 项 |
| `evidence_status` | 全部来自**真实代码执行 + 真实退出码**；`evidence_type` = `SOFTWARE_SAMPLE`（合成/夹具） |
| 真实实股结果 | **本轮没有新增实股结果**（无真实盘中网络、无真实 notifier、无真实多日标签） |

---

## §7 本轮未做 / 明确的盲区

- **`python docs/audits/validate_latest.py` 未运行** —— 该文件在此仓库**不存在**（实测 `Test-Path` = False，`docs/audits` 下无 `validate*`）。手册 §2 步骤 5 是**条件动作**，此处条件不成立。按纪律**不伪造 PASS**。
- 本轮**未启动**真实盘中网络、真实通知、真实交易、任何训练。
- 未跨仓库改动；未创建/修改任何排程；未 push 任何代码。
- `R-13`/`R-14`/`R-15`/`R-17`/`R-20` 等遗留项本轮未逐条重做。
- `5913/4100` 是合成夹具，**不构成真实覆盖率测量**。

---

## §8 1–3 条有依据的研究/改进想法

### 想法 1〔**程序修复**〕：判决项注册时必须声明"数据来源是本轮的还是累计的"

**依据**：本轮三条发现**同根因** —— `no_data_rounds`（`:671`）、`_acked` 裁剪（`store.py:359`）、`status()['universe']`（`store.py:304`）都是把**累计/有界**容器的长度当作**逐轮/完整**量。产品轮 §1.3 的教训是"必须走到有人改变结论"，但那是**症状级**纪律。根因级的做法是**让 `add()` 带上来源标注**（`scope="round"|"cumulative"`），并对 `scope="cumulative"` 的项**禁止**用于"本轮是否断供"一类判据。可证伪：把 §4.1 的 `quotes` 与 `no_data_rounds` 改成 per-round 后，6 轮全断供必须报 `no_data_rounds=6` 且 `data` 判失败（当前是 0 / ok=True）。

### 想法 2〔**任务定义变更**〕：`evaluate_health` 必须显式声明自己的**盲区清单**，并让盲区可被测试

**依据**：`evaluate_health` 有 11 项，但 `R-12`（`_universe_meta` 零出口）与 `R-21`（`setup` 无人读）说明**"没有判决项"这件事本身不可观测** —— 用户看到的 `healthy=True` 无法区分"全都好"与"这块根本没测"。做法：返回体加 `unmeasured: ["universe", ...]`，并在 `LATEST.md`/看板显示。这不是程序 bug 修复，而是**判决契约的定义变更**。

### 想法 3〔**真实模型增益：无**〕

本轮全部证据为软件样本（合成夹具 + 真实代码路径），**没有任何模型层面改进**，也没有可归因的真实市场增益。不冒充。

---

## §9 边界与合规声明

- 本轮仅**读取** `D:\ccc\ashare-radar`；所有写入限于 `D:\ccc\_sched\work\` 下的 scratch 脚本与日志。
- 未 `git add/commit/push/checkout/reset/stash/clean` 任何仓库（提交本报告时按纪律只提交 `docs/audits/intraday/` 文档）。
- 未创建、修改、删除任何排程；未启动任何训练或远程任务。
- 未触碰 `D:\xm\60日预测\reference\eventnet_test_results.json` 等禁改文件。
- 报告内每个数字均有本轮真实命令与退出码支撑；无法复现的怀疑已标 `未复现` 或直接不报。

---

## §10 归档与回读

- `report_created_commit_sha`：**`99c2cb2ce555ed8313dd7fff84e7b97d225e62d2`**（本轮报告所在提交；`git ls-remote origin refs/heads/main` 回读 = 本地 HEAD，**EQUAL**；提交范围 = 仅本报告 + `LATEST.md` 两个文档，whitelist PASS）。
- 索引：`docs/audits/intraday/LATEST.md`（**增补式**更新）。
  - 加性证明：`after.replace(inserted, anchor, 1) == before` **PASS**。
  - 索引指针：committed 版 11 个 → 工作版 12 个，**丢失 0 个**，新增仅 `2026-09-22_00-43-31_JST.md`（硬规则 ii 与 oo 均已验证）。
  - 「关键历史接续」段与全部历史指针**逐字节保留**。
