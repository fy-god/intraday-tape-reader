# 盘中预警**新增回归**与子 agent 交叉裁定 — 2026-09-23 18:17:39

- `audit_time_jst`: `2026-09-23 18:17:39`
- `doc_kind`: **新增回归（regression）+ 交叉裁定**
- `follows`: `2026-09-23_17-58-08_JST.md`（原始）、`2026-09-23_18-07-42_JST.md`（更正）、`2026-09-23_18-13-57_JST.md`（修复后回归）
- `reviewed_repo`: `https://github.com/fy-god/intraday-tape-reader.git`
- `reviewed_source_sha`: `9ec40c6740abdf9f480f4219490a891dcf7c0a11`（v2）；回归对照 `bf0b83bf212f169c720ca8a1c5108405c18a9712`（v1）
- `main_head_at_audit`: `788810084cd7ca4853707c02be4bc3058e2e390a`
- `report_created_commit_sha`: `c06b21bd9a921caebdb5eb1ab6147122eb6b9f97`
- `evidence_type`: **软件样本 = 有；实股结果 = 0**（`new_real_market_fits = 0`）

---

## 0. 本轮最高价值发现：**v2 在修「测到 0」与「没测到」时，引入了「刷新失败但池子保留」被误判成「测到 0」的假红（N1，我独立双对照确认）**

`IT-P1-REFRESH-FAILED-POOL-RETAINED-READ-AS-MEASURED-ZERO-001`（**P0**，`已确认错误`，**回归**）

### 0.1 静态链（我逐环实读 + AST，非引述）

| 环节 | 证据 |
|---|---|
| 调用点 `tools/live_session.py:3263` | `universe_size = int(engine.refresh_universe() or 0)` |
| 生产者 `src/arad/engine.py:1177-1334` | **AST：函数内共 4 个 `return`，无一个 `return None` / 裸 `return`**：`:1285 return len(codes)`、**`:1302 return 0`**、`:1317 return len(codes)`、**`:1326 return 0`** |
| `:1302` 的语境 | `rejected_smaller`：「部分池比现有池更小 -> 拒绝覆盖，**保留现有池**」，随后 `return 0` |
| `:1326` 的语境 | `all_failed` / `empty`：「**保留原股票池**」之后 `return 0` |
| 池子是否被改 | 这两条路径**都不重写** `self._codes_raw`（仅 `:1273/:1304` 在成功路径重写）→ **池子确实被保留** |
| 唯一能得到 `None` 的路径 | 只有**逃逸异常**（`:1327-1334` `except BaseException` → `_finish_attempt("crashed"...)` → `raise`），被 `:3264` 的 `except` 吞下，`universe_size` 保持初值 `None` |
| 消费者 `:1942` | `elif _abs_eff <= 0:` → `add("universe", False, f"扫描池为 **0 只**（明确测到，非未采集）...本场**一只票都没扫**", level="fail")` |

→ **「刷新失败但池子保留（非空）」与「确实一只票都没扫」在生产者源头被合并成同一个 `0`**，而消费者按「明确测到 0」处理 → **假红**。

### 0.2 端到端双对照（**我亲自驱动，两臂同一构造**）

输入：30 轮、每轮活跃扫描 **5563** 只、provider 声明分母 6000；`setup.universe_size = 0`（即 `or 0` 的产物）。旧臂用 v1 自己的 `_row_scope` + 旧 blob，新臂用 HEAD 的。

| 臂 | `tools/live_session.py` blob | 会话键数 | `universe` 判决 | `healthy` | `exit_code` | `fail` |
|---|---|---|---|---|---|---|
| **OLD** `d8627633` | `eb12ef35d5d363705279a31bf8973cb8822b37a9` | 25 | `ok`「无股票池规模记录（旧报告/未采集），无法判定」 | **True** | **0** | **0** |
| **NEW** `9ec40c67` | `b2f791f0f4c8fb91b1a41a5382bee806679a6e77` | 31 | `fail`「扫描池为 **0 只**（明确测到，非未采集）—— 本场**一只票都没扫**」 | **False** | **1** | **1** |

**我实测会话聚合值**：`universe_abs_min = 5563`、`universe_abs_measured_rounds = 30` —— 即**会话里有 30 轮、最小 5563 只的真实证据**，却因为一个 `0` 而判「一只票都没扫」。

### 0.3 三方对照（证明消费者能区分三态，是**生产者**把信息丢了）

| 输入 | `universe_size` | 判决 | 说明 |
|---|---|---|---|
| 同一会话（5563×30） | `0` | **fail / exit 1** | ← **假红**（N1） |
| 同一会话（5563×30） | `None` | **ok / exit 0** | 诚实：「扫描池 5563 只（下限 3000）（t0 未测，取自会话 30 轮最小值）」 |
| 无会话证据 | `0` | fail / exit 1 | 真 0，判红**正确** |

→ 消费者**有能力**区分；坏在 `:3263` 的 `or 0` 把「失败但保留」压成了 `0`。**这正是 v2 自称修好的那条（`IT-P1-UNIVERSE-T0-MEASURED-ZERO-READ-AS-MISSING-001`）在生产者半边留下的同构缺陷。**

### 0.4 为什么是 P0 而不是 P2

**假红与假绿同样致命**。假绿让运维漏掉真问题；**假红让退出码失去可信度** —— 一次全源抖动就会让整场判「一只票都没扫」，运维学会忽略红，此后真假红不再可区分。且它与本轮另外两条（假绿）**同因**：**一个 `0`/一个计数承载了两个语义状态**。

### 0.5 无测试覆盖（我实测）

`:3263` 位于 **`def run`（`:3204-3485`）**内（AST 确认），**不在 `_soak_loop`（`:3086-3201`）**；`git grep` 全 `tests/` 对 live_session 的 `run` 入口点命中 **0 次**。→ **没有任何测试执行到这一行**，所以它在 `1830 passed / exit 0` 下安然存活。

### 0.6 修复建议（照抄仓库已有的三态纪律）

`:3263` 改为按**返回值与池子的联合状态**判定，而不是 `or 0`：
```
if universe_size == 0 and codes:      # refresh 失败但池子保留 -> 未生效，不是"测到 0"
    universe_size = None              # 或引入显式三态/`refresh_applied` 标记
```
更稳的做法是让 `refresh_universe` 返回**三态**（成功 N / 失败但保留 `None` / 真 0），或让调用方读 `engine._universe_meta` 的 `applied` 字段。**注意**：`:3255-3259` 的注释已经写明「异常 -> None；正常返回 0 -> 真的 0」——**该注释漏掉了「正常返回 0 但池子被保留」这第三种情况**。

### 0.7 验收测试

新增一条**驱动 `run`/生产者**的测试（当前为 0 条）：构造「`refresh_universe()` 返回 0 且 `engine._codes` 非空」，断言**不**出现「一只票都没扫」且 `exit_code == 0`；再断言「返回 0 且 `_codes` 为空」**必须**判红。**两支必须有不同结论**（否则无判别力）。

---

## 1. 子 agent 交叉裁定：C 与 A 对「5 个死字段接线」**互相矛盾**，我实测裁定 **A 对**

- **C 的说法**：逐个扰动这 5 个字段会让判决**发生变化**（其中 `_last` 会翻转 `仍在恶化`/`已恢复`，混合分母种类会加 `⚠ 会话内分母种类不统一`）。
- **A 的说法**：扰动这 5 个字段**不改变任何 check 级别，也不改变 `healthy`/`exit`/`fail`**，接线是**文字层面**的。

**我实测（在诚实基线 `universe_size=None` 上逐个扰动，比对新旧 level 集合）**：

| 字段 | 原值（截断） | 扰动后 | `healthy` 变? | `exit` 变? | **level 变?** |
|---|---|---|---|---|---|
| `coverage_active_denominator_kinds` | `{'provider_declared_total': 30}` | `{'__probe__': 1}` | 否 | 否 | **否** |
| `coverage_active_p05` | `0.927167` | `0` | 否 | 否 | **否** |
| `coverage_active_last` | `0.927167` | `0` | 否 | 否 | **否** |
| `scope_counts` | `{'broad_scan_unquantified': 30}` | `{'__probe__': 1}` | 否 | 否 | **否** |
| `universe_active_scan_codes` | **在会话记录里不存在** | — | — | — | — |

**裁定**：**A 对，C 过度表述**。扰动确实可能改变**文字细节**，但**不改变任何 level / `healthy` / `exit`** —— 按「接线是否影响判决」的口径，这 5 个字段**没有判决级读者**。**C 的「每个扰动都改变判决」不成立**（它测的是 detail 文本；把文本变化等同于判决变化是**测错了变量**）。A 的「prose-only」表述**在判决级层面成立**。

**另**：我把 A 与 C 关于死字段**绝对数**的分歧（A 未给数、C 给 27→22、我给 14→10）**并列保留**，不作单一裁决 —— 三者的读者集合边界不同。**三者一致的结构结论**是：**delta 4–5、零字段失去读者、HEAD 仍有死字段（不为 0）**。

---

## 2. A 的其余裁定：我采信 / 我限定

| A 的结论 | 我的裁定 |
|---|---|
| 数值声称全部复现：两个 blob、20 行为性 RED / 0 结构性 ERROR、`65 passed`、38→65、`1830 passed` | **与我 17:58 报告独立测得的一致**（我测 `1830 passed` ×6 @HEAD、`1802 passed` ×5 @`d8627633`）。**采信**。 |
| 第 1、2、4、5 项 **CONFIRMED FIXED**（各带 FAILS@OLD / PASSES@NEW 对照） | **采信**（与我 §2.4 之外的实测无冲突；这些恰是 v2 真正修好的部分） |
| 第 3 项 **PARTIAL**：消费者已修、**生产者半边被否证** | **采信**，且这正是我 §0 独立确认的 **N1** |
| 第 6 项 `FULL-MARKET-IS-MAGNITUDE-BLIND` **INSTANCE-ONLY**：只修了**相对**量级（占 provider 自己声明分母的比例），**绝对量级仍盲** | **采信为限定**。A 的反例是「provider 声明 total=3000、扫 3000 → 五轴全绿、`healthy=True exit=0`，而报告说『全程全市场扫描』而真实市场约 6000」。**这是真的局限**，但我要标注：它**不是假绿**（相对口径确实是全扫），而是**分母本身来自被审对象**（provider 自报），即**结论强度上限**问题。我**未独立重跑** A 的 probe9/probe10，按 A 的定级记为 **INSTANCE-ONLY / 待验证风险**。 |
| 第 7 项 **REFUTED** | **我实测证实**（见 §1）。**采信**。 |
| N2：第 4 项的 `min()` **没有 k/N 量词** —— 30 轮里 1 轮 `universe=0` 就判红 | **采信为同因族**（与我 §0 同源：单点异常轮 + 无覆盖量词）。我**未独立重跑** A 的 probe7。 |
| N3：`:1817-1818` 退化重开第 1 项类 | **与 C 一致**，我按**潜在**（两侧都未复现出现场实例）记录 |
| N7：`:2020 _cov_a_f = _f(_cov_a)` 死存储；`:2016 _pct` 无调用者 | **未独立复核**，按 A 记录标 `待验证风险` |
| N8：`cov_ok` 死参数 | **我已独立确认**（`:508-514` 是唯一调用点且未传；`:582` 恒取 `0.95`） |
| N9：`scope_counts={'full_market':1}` + `scope_measured_rounds=30` → ok 级自相矛盾文本 | **未独立复核**，标 `待验证风险` |
| DOC-ACCURACY：审计文档 `+474/−98` 是 `--stat` 条形宽度误读；`--numstat` = `382 92` | **采信**：我本轮实测 `+382/−92`，与之一致 |

---

## 3. 状态三轴

| 轴 | 值 |
|---|---|
| `execution_status` | 实跑：AST 生产链 4 return 复核；`:3263`/`:1942` 与 OLD 对应行实读；**双臂端到端对照**（旧 blob `eb12ef35…` / 新 blob `b2f791f0…`，各自 `HEAD` 已断言）；**三态消费者对照**；**5 字段扰动矩阵**（诚实基线 + level 集合比对）；`git grep` 全 tests/ 对 `run` 命中 0 复核。 |
| `research_verdict` | v2 修好的 5 项**确实修好**；但**在生产者半边引入 N1 假红（P0）**，且第 6 项只修相对量级、第 7 项接线为文字级。**假红与假绿同因**：一个 `0`/一个计数承载两个语义状态。**模型真实增益 = 0。** |
| `evidence_status` | §0 = **`已确认错误` + `回归`**（静态链逐环 + 双臂端到端 + 三态对照）；§1 = **`已确认`（裁定 A）**（5×扰动 level 不变）；§2 中标 `未独立复核` 者 = **`待验证风险`**（我未重跑 A 的 probe7/9/10、N7、N9）。 |
| 未跑 | 全套 pytest、`check_*.py`、真实 soak、真实 provider 回放（stub 源不出网）、模型指标。 |

---

## 4. 诚实边界

- **N1 的「真实 Engine」那一步我用的是 stub 源**：我用仓库自己的行夹具 + 真实 `_universe_session` + 真实 `evaluate_health` 驱动了**消费者半边**，并用**真实函数体**（AST + 源码）证明了**生产者半边**的返回值语义。**我没有**让真实 `Engine` 去打真实 provider 触发 `rejected_smaller`（那需要出网/构造多源）。**A 声称用真实 Engine 复现过**，我**未复现那一步**，故记 A 的该细节为**未独立复核**；N1 本身的两半我都独立证实了。
- 我**未重跑** A 的 probe7 / probe9 / probe10 / probe6 / probe8 / N7 / N9；这些一律标 `待验证风险`，**不升级为已确认**。
- 我 §1 的裁定只针对**"是否改变判决"**这一口径；C 若以其"文字可观测"口径主张，则两说不冲突 —— 我把这一点写在 §1 里，不把它读成"C 造假"。
- 我**未改任何源码**；未创建/修改定时任务；未 push 代码/权重/配置；未读 `D:\xm\60日预测`。
- **并发写入者**仍在活动；远端本轮**多次前进**，我每次都重读 tip 再挂接。

---

## 5. 研究 / 改进想法（1–3 条）

1. **给"三态生产者"加一条机器可查的纪律**（依据 §0.6）：`refresh_universe()` 的**返回值**无法表达三态，是 N1 的根因。可操作：把返回值改为 `int | None`（`None` = 未生效/失败但保留），或强制调用方读 `_universe_meta["applied"]`；再加一条**类型层不变量测试**——凡消费 `refresh_universe()` 的地方必须处理 `None`。这一条若存在，`:3263` 会在 CI 里红。
2. **凡 `*_min` / `min()` 型判据必须配一个 k/N 量词**（依据 §0.3 与 A 的 N2）：`universe_abs_min` 现在是「30 轮里的最小值」，1 轮异常就能判红整场。可操作：仿照已修好的 `_full_covers_all`（`:1819-1822`），要求 `abs_measured_rounds == rounds_total` 才允许**全称**结论，否则降级为「部分轮次异常」的 warn。
3. **把「假红」纳入审计的对称清单**（依据 §0.4）：本轮三条 finding 里**两条假绿 + 一条假红**，而假红对**退出码可信度**的破坏不亚于假绿。可操作：在判决测试里对每个 `level="fail"` 分支配一条「**该红时才红**」的阴性对照（当前 `:1942` 只有"真 0 判红"的阳性对照，**缺**"失败但保留**不**判红"的对照）。

---

## 6. 声明

- 只读仓库操作；所有执行在丢弃式克隆内（`h16_fixclone` @ `9ed75efb`、`h16_oldclone` @ `d8627633`，两者 `HEAD` 与 blob 均已断言并打印）。
- 未修改产品源码；未创建/修改/删除任何定时任务；未 push 代码/权重/配置/Actions/PR；未 force push。
- 所有 SHA / blob / 计数 / 退出码均为实读实跑；未独立复核的项已逐条标 `待验证风险`；未复现项未升级；子 agent 结论**不因引述而采信**，逐条重测或明确标为未复核。
